# M21 implementation prompt

Execute `plans/M21_GENERIC_SEMANTIC_REGISTRY.md` end to end using the local
`$schemabridge-milestone` workflow.

Preserve all repository invariants. Work only on M21. Treat the current dirty tree as operator work:
do not reset, discard, stage, commit, push, or rewrite it.

The decisive outcome is not a renamed north-star fixture. Implement one atomic governed registry
boundary, expand the deterministic database to the planned eleven-table/two-domain corpus, and
prove both the unchanged Customer path and a genuinely different commerce path through the same
planner/compiler/guard contracts.

Required behaviors:

1. Separate registry capacity from per-query capacity.
2. Validate full cross-artifact integrity and fingerprint the complete snapshot.
3. Load only manifest-listed, checksum-matched, bounded files with no hidden fallback.
4. Derive physical allowlists from approved registry content.
5. Re-resolve against the current registry immediately before execution/retry.
6. Preserve exact north-star results and every SQL/source-write/DataHub-approval control.
7. Generate no random or proprietary data; verify two clean scoped resets by canonical hashes.
8. Run the internal-browser journey before recommending acceptance.

Do not claim M22 live DataHub registry reconstruction, M23 migrations, M27 dynamic Query Studio, or
overall production readiness.
