"""Durable control-plane adapters.

Concrete PostgreSQL adapters are imported from their defining modules so that
path-only packaging helpers do not accidentally require the optional database
runtime.
"""
