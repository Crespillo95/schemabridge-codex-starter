# Decision log

Use this lightweight log for milestone-level decisions. Architectural decisions that change a lasting constraint also require an ADR under `docs/adr/`.

| Date | ID | Decision | Reason | Consequence |
|---|---|---|---|---|
| 2026-07-21 | D001 | Use GPT-5.6 Sol; High daily, Extra High for hard modules, Ultra only for M16 | Match reasoning cost to task complexity | Model level is recorded per milestone |
| 2026-07-21 | D002 | Use ports and adapters | Isolate DataHub, database, LLM, SQL, UI changes | Enforced by nested AGENTS and tests |
| 2026-07-21 | D003 | Never execute raw LLM SQL | Safety and deterministic testing | LLM output is typed intent only |
| 2026-07-21 | D004 | Human approval governs mappings, joins, and metadata writes | Semantic evidence is not proof | Confidence never grants approval |
| 2026-07-21 | D005 | PostgreSQL-only executable MVP | Finish a safe complete vertical slice | Other dialects remain future work |
| 2026-07-21 | D006 | DataHub is both read context and approved knowledge store | Align with category and judging | Demo must prove read, write, and reuse |
| 2026-07-21 | D007 | Use CPython 3.13.13 for the operator M00 environment and probe supported interpreters explicitly | The default `python3` is unsupported CPython 3.9.6 while Homebrew 3.13.13 is available | POSIX and PowerShell bootstrap fail closed outside `>=3.11,<3.14` |
| 2026-07-21 | D008 | Keep M00 free of `.env` files and external service startup | Package, CLI, and quality checks require no credentials or services | Docker runtime starts in M01; DataHub and MCP start in M04 |
| 2026-07-21 | D009 | Record unavailable tooling instead of treating documentation as execution evidence | PowerShell is absent and the standalone Codex CLI native binary is missing | Windows bootstrap remains unverified; Codex CLI repair is an operator prerequisite only if terminal use is needed |
| 2026-07-21 | D010 | Pin the demo to PostgreSQL 16.13 Alpine multi-platform digest and loopback host port 55433 | The operator already has PostgreSQL services on 5432 and 55432; an observed digest makes resets reproducible | Compose clients must use 127.0.0.1:55433 unless both port and DSN are overridden together |
| 2026-07-21 | D011 | Enforce reader safety through explicit ACLs, a role-level read-only default, and an adapter-level read-only transaction | A single setting can be overridden or regress; independent controls prove defense in depth | Integration tests must reject writes both with the default enabled and after a session disables it |
| 2026-07-21 | D012 | Keep the north-star SQL under `demo/reference/`, outside initialization and production source | Ground truth must be executable without becoming compiler logic or running during database creation | Tests and operator tooling may execute the fixture explicitly; application code must not import or copy it |
| 2026-07-21 | D013 | Normalize canonical identifiers through an explicit immutable policy and typed accepted/rejected outcomes | Leading zeros and `NULL` are semantic choices, while fractional, non-finite, boolean, malformed, negative, and precision-unsafe values must fail without repair | Strip, preserve, and fixed-width padding are configured explicitly; rejection codes are stable and floats outside the exact integer range are rejected |
| 2026-07-21 | D014 | Serialize transformations as a closed discriminated union with no code, SQL, callbacks, or free-form expressions | Mapping plans must be deterministic, reviewable data and cannot become an execution bypass | M02 interprets only the identifier specialization; later interpreters must implement bounded named operations rather than expanding to arbitrary expressions |
| 2026-07-21 | D015 | Keep SQLGlot in the optional `sql` adapter extra and use observed version 29.0.1 for M03 verification | SQL parsing and PostgreSQL rendering are adapter concerns and must not enter the domain or application dependency surface | CI and M03 integration install `.[dev,postgres,sql]`; domain purity remains enforced |
| 2026-07-21 | D016 | Require compiler output to pass an independent final-SQL reparse before becoming a `ValidatedQuery` | A deterministic compiler can still contain bugs, so its output and metadata cannot be trusted as the sole control | The guard rechecks statements, assets, columns, joins, functions, placeholders, table count, and limit; only its result reaches preview execution |
| 2026-07-21 | D017 | Enforce preview bounds again in the psycopg adapter | SQL policy alone cannot prove database identity, transaction mode, runtime timeout, or fetched-row behavior | Every preview sets read-only mode and a transaction-local timeout, caps fetched rows, and returns observed safety facts |
| 2026-07-21 | D018 | Pin DataHub Core `v1.6.0`, CLI/connector `1.6.0.15`, MCP `0.6.0`, and the official quickstart Compose checksum | M04 must reproduce the environment without floating `latest`, while DataHub's connector SQLGlot constraint conflicts with M03's bounded compiler dependency | DataHub connector and MCP run in isolated uv/CPython 3.11 environments; application/domain dependencies and SQLGlot 29 remain unchanged |
| 2026-07-21 | D019 | Enable local DataHub authentication and logical models but expose MCP reads only | M04 needs a dedicated catalog identity and native UI capability without advancing write-back or leaking credentials | Signing material and one-month tokens stay in ignored mode-0600 files; MCP mutation, document-save, and document-search tools are forced off and checked |
| 2026-07-21 | D020 | Scope destructive DataHub reset to the Compose project named `datahub` | The operator has unrelated Docker projects, and a generic Docker/DataHub prune would exceed milestone authority | `make datahub-reset` deletes only the pinned stack's containers, network, and volumes, then restarts empty; re-ingestion and identity provision are explicit follow-up steps |
| 2026-07-21 | D021 | Expose catalog reads through vendor-neutral application values with explicit `present`, `missing`, `partial`, and `unavailable` evidence states | Absence of lineage, query context, or a read tool is materially different from a service failure and must never become fabricated evidence | DataHub/MCP models remain in adapters; pagination is bounded and partial pages advance only by returned items |
| 2026-07-21 | D022 | Pin MCP client `1.28.1` in the optional DataHub extra and require an explicit recorded-adapter choice | The application needs the same client protocol as the verified MCP server while live outages must not trigger hidden hard-coded context | Live reads fail closed with sanitized typed errors; offline demos are visibly labeled and use a secret-free three-asset snapshot |
| 2026-07-21 | D023 | Rank bounded candidates with seven normalized weighted signals while keeping recommendation and approval separate | Deterministic evidence must be explainable, missing evidence must remain visible, and confidence cannot establish semantic truth | Weights sum to one without redistribution; name alone cannot reach the review threshold; every candidate remains `needs_review` |
| 2026-07-21 | D024 | Treat recorded profile/overlap signals and evaluation labels as synthetic fixtures, and constrain optional model interpretation to typed explanations | Offline reproducibility must not fabricate live evidence or let unvalidated model output influence scores, transformations, or status | Live mode reports absent signals, fixture metrics are labeled non-production, and M06 adds no LLM SDK or metadata writes |
| 2026-07-21 | D025 | Keep canonical drafts and immutable decisions in local SQLite, and require a separate exact fingerprint approval for publication | Editing, semantic approval, and external mutation are distinct transitions; confidence is not authorization | Optimistic revisions reject stale decisions, edits invalidate affected approvals, and neither a use case nor concrete writer can publish without `PublicationApproval` |
| 2026-07-21 | D026 | Keep Codex MCP mutation-disabled and publish through a bounded DataHub SDK/aspect writer with the marker written last | The pinned Core lacked the logical-link OpenAPI route, while DataHub documents `LogicalParent` aspects as the faithful fallback; MCP mutations would bypass the application approval port | A dedicated ignored mode-0600 writer identity can mutate only synthetic canonical assets; partial writes remain visible/retryable and only complete read-back is `already_current` |
| 2026-07-21 | D027 | Discover joins only from explicit bounded proposals and aggregate read-only evidence, with declared FK direction taking precedence when observed rows are uniquely inconclusive | Join discovery is a semantic-risk boundary; unrestricted all-pairs scans, raw samples, or row uniqueness alone could produce misleading contracts | PostgreSQL allowlists exact keys/transformation plans and returns counts only; catalog lineage/query absence remains explicit; AccountHolder-to-Account records the currently-unique seed limitation as a risk |
| 2026-07-21 | D028 | Require separate semantic and fingerprinted publication approvals, reject many-to-many execution, and persist both versioned and current DataHub join documents | Confidence is not authorization, the MVP has no safe many-to-many mitigation, and a new process needs a stable reusable context identity without losing its audit version | Every executable contract carries normalized keys, cardinality, fanout, evidence, risks, version, and approval decision; the current marker is written last and MCP mutation remains disabled |
| 2026-07-21 | D029 | Validate guided selections against an explicitly sourced vendor-neutral approved logical context before allowing a typed planner handoff | Guided input must not guess fields, operations, paths, or fanout policy, and M09 must prove the request seam without pulling physical planning or execution forward | Only a `ValidatedAnalyticalRequest` with a unique shortest approved path reaches the M09 fake planner; local drafts are revalidated on reload, and recorded context remains visibly synthetic rather than a live fallback |
| 2026-07-21 | D030 | Resolve M10 from one fingerprinted, explicitly recorded approved planning context until complete mappings are available through live read-back | The current live DataHub state does not contain every physical mapping and role-code transformation required by the synthetic acceptance case, and a hidden fallback would fabricate governance | `governed-demo` names the recorded source; stale, unapproved, type-incompatible, disconnected, ambiguous, and over-limit context fails before compilation |
| 2026-07-21 | D031 | Apply fanout changes only when the approved join contract defines the exact mitigation | Guided intent and relationship metrics must remain distinguishable while duplicate Customer rows cannot inflate entity counts | Customer `count` across the approved one-to-many contract becomes visible `count_distinct`; explicit relationship counts stay unchanged and unsupported mitigations fail closed |
| 2026-07-21 | D032 | Report invalid join-key sources through a separate exact-allowlist read port with its own reader, read-only, and timeout checks | Query results alone do not expose rejected records, while unrestricted sampling would violate least privilege and data-minimization boundaries | The M10 PostgreSQL reporter returns only typed rejected values for approved normalized keys and cannot inspect arbitrary fields or write source data |
| 2026-07-21 | D033 | Constrain natural-language parsing to a minimum logical vocabulary and a no-tools structured-output schema, with exact fingerprint confirmation before planning | Business language is untrusted and model output cannot establish schema truth, policy, approval, or executable behavior | The deterministic fake is key-free; live mode is explicit and never falls back; hallucinations, SQL-like payloads, unknown values, injection text, stale confirmation, and unresolved ambiguity fail before the unchanged planner |
| 2026-07-21 | D034 | Persist M12 as an explicit typed state machine whose checkpoints and retries are bound to immutable fingerprints | A UI boolean, implicit call chain, or automatic resume could skip ambiguity, approval, version, or SQL-validation boundaries and repeat external effects | Resume is inert; every human choice is a typed decision; the exact stored plan is recompiled and independently revalidated before execution; completed previews and publications are reused instead of rerun |
| 2026-07-21 | D035 | Separate observable workflow traces from private reasoning and publish only fingerprinted execution context through a bounded DataHub document prefix | Operators need action/status evidence without prompts, credentials, SQL, parameters, rows, or chain-of-thought, while publication retries require a stable external identity | External actions emit sanitized start/result/error facts; live publication requires exact typed approval, writes under `urn:li:document:schemabridge-workflow-`, and returns `already_current` on the deterministic idempotency key |
| 2026-07-21 | D036 | Persist versioned query recipes as SQL-free typed context under a deterministic, bounded DataHub document prefix | Reusable semantic knowledge needs provenance and idempotency without creating a saved-SQL execution path or a marketplace | Publication requires an exact `RecipePublicationApproval`; versioned and current documents link only approved synthetic assets and store normalized intent, versions, fingerprints, validation summary, and limitations |
| 2026-07-21 | D037 | Treat a prior recipe as compatible evidence only after comparing current model, mapping, join, source-schema, compiler, plan, and query identities | A current document can become stale and cannot replace planning or safety validation | Every repeated request replans and reparses generated SQL; compatible reuse records provenance, while any changed boundary yields typed staleness and review/replanning instead of saved-SQL execution |
| 2026-07-21 | D038 | Keep Streamlit as a thin entrypoint over immutable application view models and a workflow facade | UI callbacks and session booleans must not become a second business-rules engine or bypass typed M12 checkpoints | Session state holds only workflow identity, navigation, and a sanitized transient error; confirmations, execution, retry, and publication remain typed use-case calls built at the composition root |
| 2026-07-21 | D039 | Pin Streamlit `>=1.59,<2` and NumPy `>=1.26,<2.3` in the optional UI extra | The verified width API requires current Streamlit, while NumPy 2.3 stubs use syntax newer than the project's Python 3.11 mypy target | Bootstrap and CI install the UI extra; Streamlit 1.59.2 and NumPy 2.2.6 pass the full Python 3.13 gate without changing domain/application dependencies |
| 2026-07-21 | D040 | Treat M15 as a raw-count synthetic evaluation with no confidence interval or regression threshold | Seven candidate labels and two query/join/recipe cases are too small and too tuned to justify statistical or production-quality claims | Every metric exposes numerator, denominator, skips, failures, and difficult cases; the intentional false positive and false negative remain visible and non-gating |
| 2026-07-21 | D041 | Compare query results as type-tagged, column-keyed, order-independent row multisets and keep live-LLM runs separate from deterministic evidence | SQL string equality does not prove result correctness, while mixing live provider observations with fake output would make reproduction misleading | Duplicate rows and value types remain observable; malicious SQL is guard-only; default evaluation is key-free and a live run is explicitly requested, separately labeled, and never falls back |
| 2026-07-22 | D042 | Require an existing clean `HEAD` for release evidence and keep `--allow-uncommitted` explicitly development-only | The supplied repository has no commit, so even a complete zero-state run cannot identify a reproducible release candidate | Strict audit and clean-room targets fail closed; development results retain missing-commit/dirty-tree warnings and cannot authorize release |
| 2026-07-22 | D043 | Bound rejected-source materialization and apply one fail-closed negative-identifier rule at every SQL-backed interpretation boundary | Unbounded `fetchall` could exhaust memory, while inconsistent handling could count or report an identifier the deterministic compiler rejects | The adapter fetches only a hard-capped sample plus exact total/truncation metadata; compiler, relationship evidence, and rejection reporter share stable negative rejection behavior |
| 2026-07-22 | D044 | Verify observable DataHub privileges through the pinned authorization-cache window and make each approved write family independently establish its decision property | Fresh resets exposed authorization propagation and hidden publication-order dependencies that repaired-state tests could not reveal | Provisioning retries boundedly and rejects excess privileges; canonical, relationship, and recipe publication no longer depend on another writer running first |
| 2026-07-22 | D045 | Keep M16 at NO-GO despite a passing development clean-room run | Release identity is absent, a high publication-audit gap remains, and the operator live-browser journey is unperformed | All critical/high findings are either fixed or explicit blockers; M17 cannot start until the operator resolves/accepts them and strict clean-commit proof passes |
| 2026-07-22 | D046 | Require one common append-only per-target publication audit contract and SQLite ledger across canonical, join, workflow, and recipe writes | A governance reviewer must answer actor, approval time, old/new fingerprint, operation, decision references, and outcome from one durable target record | Every publisher returns approval-bound audit records; the application validates them before an atomic ledger append, and approval IDs have immutable identities |
| 2026-07-22 | D047 | Evaluate join cardinality in the direction actually traversed by the query plan | A declared one-to-many contract becomes many-to-one when traversed in reverse; applying the forward mitigation changed a correct relationship count from 6 to 5 | Reverse one-to-many preserves plain relationship counts; reverse many-to-one is treated as an unmitigated one-to-many expansion and fails closed |
| 2026-07-22 | D048 | Group and order parameterized normalized dimensions by their validated projection position | Recompiling equivalent `CASE` mappings created distinct PostgreSQL bind parameters and a visible `GroupingError` | The compiler emits a safe ordinal only for an identical parameterized projection; the final SQL still passes the independent AST guard |
| 2026-07-22 | D049 | Keep M16 at NO-GO until this remediation is committed, run through strict clean-room, and manually recorded | The earlier clean-room predates the current audit/fanout changes, and DataHub plus SQLite have no distributed transaction | GOV-001 is functionally remediated, its cross-system reconciliation limitation is retained as medium `GOV-002`, and release evidence must be regenerated from the reviewed commit |
| 2026-07-22 | D050 | Bind every query-plan join predicate and transformation to its approved contract and limit automatic distinct mitigation to the exact one-side key | Dataset-only contract matching and `COUNT(attribute) → COUNT(DISTINCT attribute)` could validate the wrong physical join or silently change metric meaning | Forged predicates/reversed `LEFT` joins fail closed; unsafe chained aggregates fail, while explicit duplication-invariant `COUNT DISTINCT`/`MIN`/`MAX` remain supported |
| 2026-07-22 | D051 | Treat old fingerprint as verified target evidence, never as a copied publication marker, and require post-write read-back before success | Canonical targets and no-op acknowledgements could otherwise create audit facts for state that was not observed | Fingerprintable targets carry their own old state; un-fingerprintable aspects record null and are re-ensured; no-CAS concurrency and cross-system atomicity remain `GOV-003`/`GOV-002` |
| 2026-07-22 | D052 | Require workflow proposal hashes to be self-validating, immutable deterministic targets, and current-state proof to include the exact embedded approval and target audit | Fingerprints alone allowed a false successful read-back, a manually forged reused idempotency key could bind different content, and a conflicting target could be overwritten | Invalid proposal identity fails in the domain; conflicting targets fail before mutation; DataHub replay/post-write success requires exact workflow, approval, and validated audit evidence |
| 2026-07-22 | D053 | Package the free public judge path as a Hugging Face Docker Space with explicit recorded catalog/result evidence and retain full DataHub locally | Official free-tier comparison favored the Space's Docker support and 2 vCPU/16 GB headroom, while nested ephemeral DataHub would not be a stable or honest single-container deployment | Hosted runtime needs no data/LLM secret, exact query/check fingerprints gate replay, sleep is disclosed, and public rollout remains blocked on the clean release/manual tests |
| 2026-07-22 | D054 | Generate M18 examples through the current typed planner/compiler/guard and fail closed on release identity, while keeping media and public-link evidence operator-gated | The working tree is not a release commit and has no public M17 URL or video; hand-edited artifacts or placeholder URLs would misrepresent acceptance | `make submission-package` requires a clean commit; development output is labeled non-release, write-back generation is an explicit fake contract, and final Devpost/video sign-off remains blocked |
| 2026-07-23 | D055 | Close every SQLite adapter connection through one shared managed lifecycle and require resource/unraisable warnings plus full service-backed coverage to gate release work | Hundreds of connection warnings could coexist with green tests, and unit-only coverage omitted the live PostgreSQL/DataHub paths that materially define this product | Pytest fails on leaked resources/unraisable exceptions; `make check` remains service-free, while CI and the strict clean room must pass the complete suite at `>=80%` coverage |
| 2026-07-23 | D056 | Authenticate production browsers with provider-neutral Streamlit OIDC, then map only exact validated claims to deployment-scoped HMAC-pseudonymous principals and a closed deny-by-default role policy | OIDC proves identity but does not authorize product actions, while editable actors, raw claims, and mutable PII cannot establish durable audit identity | Staging/production fail closed without OIDC; unknown groups grant nothing; decisions use the authenticated actor; token/email/raw claims never enter SchemaBridge persistence |
| 2026-07-23 | D057 | Bind every new workflow and immutable workspace/owner grant in one SQLite transaction, keep inspection/resume inert, and require an explicit operation-bound authorized recovery action | Global workflow IDs and mutating resume allowed horizontal access or a view-only principal to change interrupted state merely by opening it | Scoped lookup happens before workflow I/O; legacy/orphan/conflicting state cannot be adopted; an auditor inspection is byte-for-byte inert; recovery rechecks the exact operation and role |
| 2026-07-23 | D058 | Require an exact deployment tenant allowlist and fixed managed integration profiles, while treating tenant-specific live data-plane credentials, quotas, migrations, and multi-node operation as later production gates | A signed arbitrary tenant label must not create a workspace, but M20 cannot honestly claim data-plane multi-tenancy or scale from workflow isolation alone | One deployment may serve only tenants authorized for its shared DataHub/PostgreSQL scope; M23–M25 must add migrations/reconciliation, authenticated API/workers, tenant-aware routing, quotas, and measured scale |
| 2026-07-23 | D059 | Supply logical models, definitions, mappings, joins, provenance, and versions through one immutable workspace/catalog/registry-scoped semantic-registry snapshot | Independently loaded M09/M10 fixtures could drift, mix versions, or silently fabricate a complete planning context | The manifest adapter verifies exact file SHA-256 and full-snapshot fingerprint with duplicate-key, size, path, symlink, format, scope, and referential-integrity checks; every failure is closed and there is no legacy fallback |
| 2026-07-23 | D060 | Keep registry capacity independent from query capacity and re-resolve every confirmed request against the current registry immediately before preview or retry | A reusable enterprise registry must contain many domains without weakening the three-table/two-join safety policy or executing a plan after its governance context changes | The global 7-model/5-contract graph loads, but a fourth model/third join is rejected; changed/revoked state returns `stale_registry` before PostgreSQL I/O, and rejected-source allowlists derive only from current approved join keys |
| 2026-07-23 | D061 | Track the complete synthetic corpus with canonical per-table, schema, constraint, permission, and global hashes and require two project-scoped reset reproductions | Small happy-path tables could conceal overfitting, nondeterminism, invalid-key handling, fanout errors, and accidental writer privilege | The public corpus is 465 deterministic rows across 11 tables/eight schemas, including commerce/fulfillment invalids and ungoverned support homonyms; two resets reproduce global SHA-256 `487388495115265d5fac5a17675e05c236fd4eb4c430613ef02050a5f6010654` |
| 2026-07-23 | D062 | Treat names and brief descriptions only as candidate evidence, never as sufficient semantic equivalence or executable query intent | The expanded support schema deliberately contains homonymous identifiers, and the small candidate fixture retains a description/name false positive | M21 stores and displays governed definitions and rejects unknown homonyms; the M11 parser stays Customer-focused, ambiguous decisions require human confirmation, and registry-wide description matching remains explicitly assigned to M27 |
| 2026-07-23 | D063 | Represent a live governed registry as one immutable, workspace-hashed DataHub version document loaded by exact URN | Independent artifact reads or search cannot prove one complete revision, and fallback would mislabel recorded evidence as live state | The live port reconstructs and validates one atomic 7/31/5 snapshot, approval, audit, decision closure, and related assets; active-pointer changes remain M23 |
| 2026-07-23 | D064 | Durably reserve the exact registry-publication approval before any DataHub I/O and derive its idempotency ID from actor, scope, registry version, and payload fingerprint | An external mutation must not start without a persisted audit identity, and a caller-selected ID could be rebound to different content on retry | Reservation failure produces zero publisher calls; exact retries resume the original timestamp and may return `already_current`, while incompatible history fails closed before DataHub |
| 2026-07-23 | D065 | Retain the deprecated three-file M10 adapter only as an exact read-only historical compatibility shim | Historical fixtures must remain readable without reintroducing an implicit legacy planning fallback | New and live composition uses the atomic registry port; the shim is fixed to its legacy scope, cannot publish, and fails closed when an exact historical input is absent or invalid |
| 2026-07-23 | D066 | Make a separately migrated PostgreSQL pointer the managed registry authority and treat the DataHub active document only as a reconciled projection | DataHub cannot compare-and-swap or atomically commit SchemaBridge transition, audit, and retry state | Pointer, monotonic transition, HMAC audit, and outbox commit together; planning reads the exact PostgreSQL-selected immutable version, while projection failures remain visible and repairable |
| 2026-07-23 | D067 | Keep result rows transient and bind any in-session preview to the exact principal, workspace, workflow revision, registry generation, pointer, count, and fingerprint | Persisted result rows increase sensitive-data exposure, while a browser session must never display rows from another identity or stale governance context | PostgreSQL stores only row count and preview fingerprint; legacy rows are stripped or quarantined, and every context mismatch purges session rows |
| 2026-07-23 | D068 | Resolve identity-rotation completion and replay from the immutable reserved plan fingerprint plus the exact verified derivations | Re-preparing against the source workspace after a successful rotation fails because that binding is historical, before the durable completion can be read | First completion still revalidates current state; exact replay loads the approved plan/completion without reactivating or trusting a historical workspace, and changed evidence fails closed |
| 2026-07-23 | D069 | Require signed owner-only backups and restore only into a configured distinct fresh database before complete state verification | An archive alone does not prove migration, audit-chain, pointer/history/outbox, identity, or quarantine integrity, and an in-place restore could destroy current evidence | Backup binds archive checksum and state digest; restore rejects altered material or non-fresh targets, verifies exact read-back, and leaves cutover external and explicit |
| 2026-07-23 | D070 | Expose only an exact approved preview command through a signed-bearer API and process it with PostgreSQL at-least-once leases plus monotonic fencing | A generic task/SQL endpoint, unverified claims, in-process background work, or an exactly-once claim would bypass existing review/safety boundaries or misstate external-read delivery | API and worker use separate roles/processes; raw keys/tokens/SQL/rows are absent; ambiguous reads require human retry; schema v2 migration `0002` is bound to SHA-256 `4e828aacfc9cc0db35a5b05838556e35360d7e0322c16e1ed008c94faf4f4efc`; M24 uses no OpenAI key |
| 2026-07-23 | D071 | Make tenant connection/table inventory dynamically sized and keep it independent from executable query width | One company may expose 10 tables and another 5,434 or more; a hardcoded inventory or full-memory scan would invalidate scale tests, while raising query width would weaken an unrelated safety control | M25 must prove tenant-scoped indexed cursor pagination, incremental refresh, and bounded memory at 10 and 5,434 tables; M27 matching consumes that catalog, while each query remains limited to three tables/two joins |
| 2026-07-23 | D072 | Supervise every synchronous worker operation with periodic fenced heartbeats and one final renewal before transition | A single pre-execution heartbeat can expire during a bounded but long source read and allow a reclaimed worker to race the original owner | Losing the capability, fence, or heartbeat fails closed before any later worker transition |
| 2026-07-23 | D073 | Bind jobs to both the current submitter and immutable historical workflow scope, resolving rotation only through verified opaque lineage | Requiring the worker to recompute browser OIDC identity would either break rotated workflows or force tokens, JWKS, and pseudonymization secrets into the worker | API authorization uses verified OIDC lineage; managed workers use persisted `verified-oidc` lineage without raw identity material; owner and workspace-wide grants validate the exact historical workspace+submitter pair, while ambiguous/unknown lineage fails closed |
| 2026-07-23 | D074 | Preserve an exact rejected-row total while bounding per-code materialization and reporting the omitted residual explicitly | A bounded code sample alone could make a large rejection population appear complete or undercount data-quality risk | Durable/API summaries expose sampled counts, exact unclassified residual, and completeness/truncation flags; 25,001 rejections can be represented without rows or unbounded state |
| 2026-07-23 | D075 | Enforce job-authorization expiry inside PostgreSQL success transitions and advance the M24 control schema to version 3 | Final audit proved that an application-level check could race or omit the terminal expiry boundary even though the earlier suites were green | `0003_reject_expired_job_success.sql` SHA-256 `fba8bf2fec35c56be95487c21452ac46a45b3598c5b56cf382414aa57c7240f0` is applied; fresh unit/PostgreSQL/process/socket evidence passed before local acceptance |
| 2026-07-23 | D076 | Dead-letter workflow-access store failure as unexpected external state | An unavailable authorization store does not prove a genuine authorization mismatch and a normal failed result would conceal operational ambiguity | `STORE_FAILURE` must never map to `AUTHORIZATION_MISMATCH`; terminal output is sanitized `dead_lettered` without retry or protected I/O |
| 2026-07-23 | D077 | Accept M24 locally with explicit at-least-once delivery, monotonic fencing, and internal-browser evidence while retaining the global production/release NO-GO | Fresh schema-v3 gates, real-socket tests, a real `SIGKILL`/attempt-2 reclaim, indistinguishable tenant/role denial, summary-only success, and clean desktop/mobile browser scans satisfy all 25 M24 criteria; the browser's ephemeral same-origin relay kept its bearer server-side and exposed only sanitized real-API responses | M25 is now eligible but not started; the dirty tree, unoperated Kubernetes/TLS/NetworkPolicy/external secrets, M25 dynamic scale, M27 description matching, and M29 operations/security and supply-chain gates still block a production release |
| 2026-07-23 | D078 | Give catalog inventory its own tenant/connection identity, generation pointer, and signed keyset cursor without changing SQL-facing dataset identity | `PhysicalDatasetRef(schema.table)` collides across connections, current cursors are decimal offsets, and a 5,434-table catalog cannot be a request-sized tuple | Schema v4 will persist workspace/connection-scoped assets and fields, promote complete generations atomically, and bind external cursors to tenant, scope, filter, generation, and last key; queries remain one connection and at most three tables/two joins |
| 2026-07-23 | D079 | Add a separately credentialed catalog indexer and keep API, execution worker, and OpenAI boundaries isolated | API-side DataHub reads would mix long metadata scans with request handling, while giving the execution worker DataHub credentials would violate the accepted M24 capability matrix | API only manages metadata/refresh requests; the indexer streams bounded DataHub scroll pages into invisible generations and receives no source-write, execution, publication, migration, or LLM capability |
| 2026-07-23 | D080 | Treat quotas as tenant data and scale thresholds as reproducible local regression budgets | A compile-time table count cannot represent companies with 10 versus 5,434+ tables, and one-machine latency is not production SLO evidence | PostgreSQL enforces cross-replica rate/job/catalog admission from durable policies; reports record pool, concurrency, hardware, heap/RSS, p95/p99, and freshness without claiming production availability or SLOs |
| 2026-07-23 | D081 | Scope live DataHub catalog discovery with an exact dataset-URN prefix and stable URN scroll order | The operated v1.6.0 GraphQL endpoint returned all 11 synthetic datasets for `urn START_WITH` while the equivalent dataset-name prefix returned none; the URN also binds platform, catalog scope, and environment | The adapter uses mutation-free `scrollAcrossEntities`, an exact platform/scope URN prefix, bounded pages, and full-reconciliation labeling; missing progress or source failure remains typed and never falls back |
| 2026-07-23 | D082 | Keep an asset fingerprint independent from its field collection and fingerprint every field from its complete persisted metadata | Adding or changing a field must not change an asset's own identity, while native type, definition, key/nullability flags, tags, and terms must participate in field-change detection | Full reconciliation compares stable asset facts separately from field facts; source adapters must reject incompatible metadata rather than relying on PostgreSQL truncation or silent normalization |
| 2026-07-23 | D083 | Expose refresh request/status and private-route lookup through narrow PostgreSQL `SECURITY DEFINER` functions instead of direct table grants | Direct API access to refresh rows or catalog access to every route would exceed each process contract and make row-scope enforcement depend on caller discipline | Functions fix `search_path`, validate scope/ownership/lease/fence, hash transient route capability inside PostgreSQL, revoke `PUBLIC`, and grant only the exact API or catalog operation |
| 2026-07-24 | D084 | Apply each tenant capacity policy through an expected-version, exactly confirmed platform-admin command backed by a migrator-only function and immutable revisions | Per-company connection/table/field capacity must be dynamic and operable without a release, while direct ad-hoc writes would lose actor/version evidence and permit stale concurrent changes | Policy creation starts at expected version 0, revisions compare-and-swap the current version, every accepted value set is retained, and missing or over-capacity state continues to fail closed without deleting inventory |
| 2026-07-24 | D085 | Align long-running acceptance-test lease fixtures with their real runtime bounds without weakening fail-closed expiry | Coverage instrumentation made valid process/socket paths outlive artificial two-second test leases; the resulting stale-owner fencing was correct and exposed a fixture-runtime mismatch rather than a production bypass | The recovered-owner fixture uses a 60-second lease while its explicit first-expiry and stale-owner probes remain four and two seconds; socket worker/cooperative claims use the 120-second runtime default; runtime behavior is unchanged |
| 2026-07-24 | D086 | Bind each governed physical field to an explicitly approved workspace/connection/asset/field observation and gate only the exact affected dependencies | `schema.table.field` cannot distinguish M25 connections, catalog names/descriptions are evidence rather than equivalence, and blocking every request after an unrelated change among 5,434 assets would be unsafe operationally | M26 resolves initial review through a bounded reconciler-only locator capability with at most two exact witnesses per mapping, observes only approved locators, profiles approved joins with aggregate-only evidence, fingerprints the complete workflow/recipe blast radius, allows exact unaffected plans, and requires a new immutable registry for type/key/nullability/cardinality/removal remediation |
| 2026-07-24 | D087 | Bind each aggregate profile worker to one exact workspace plus catalog connection and scope claim/reclaim to that pair | A connection label may be reused by two tenants, and one global source DSN cannot safely profile work from another workspace or connection | `SCHEMABRIDGE_SEMANTIC_PROFILE_SOURCE_WORKSPACE_ID` plus `SCHEMABRIDGE_SEMANTIC_PROFILE_SOURCE_CONNECTION_ID` are mandatory; the worker revalidates both before heartbeat/source I/O, equal cross-workspace connection IDs cannot cross-claim, and one isolated worker is deployed per governed pair until M28 adds connector routing |
| 2026-07-24 | D088 | Fan out catalog-generation scans to every matching active registry and renew the scan lease throughout paginated dependency reconciliation | A global configured registry scope would miss another active registry, while a lease renewed only around the full scan could expire during a 5,434-artifact traversal and allow a stale owner to advance coverage | Catalog requests carry workspace/catalog/registry scope, registry transitions enqueue independently, dependency pages are bounded to 50, and lease loss prevents the dependency watermark/report from committing |
| 2026-07-24 | D089 | Namespace active DataHub query-recipe documents by semantic scope fingerprint as well as intent | Two tenants can submit the same business intent, so the historical intent-only current marker can collide and corrupt blast-radius coverage or recipe reuse | Current/version URNs include the scope-fingerprint prefix, active publication and lookup validate the full scope, and the dependency inventory scrolls only that exact reserved namespace |
| 2026-07-24 | D090 | Bound one atomic dependency replacement to 10,000 artifacts/100,000 edges and fail coverage closed beyond either cap | A complete blast radius must be deterministic and atomic without unbounded memory/transaction work or silently truncating a large tenant | Sources still page at 50, PostgreSQL inserts edges in batches of 500, same-scope stale artifacts project conservatively to exact current logical fields/contracts, and cross-scope, unresolvable, or over-cap artifacts make the manifest incomplete |
| 2026-07-24 | D091 | Accept M26 locally and make M27 eligible while retaining the global production/release NO-GO | All 30 M26 criteria now have fresh schema-v5, six-role, cold 5,434-table locator, real PostgreSQL/DataHub, API/worker, full integration/acceptance, package, release-audit, quality, 81.64% coverage, checksum, diff, and desktop/mobile presentation evidence; every failed intermediate run is preserved with its correction | M26 is the accepted semantic-drift baseline and M27 may implement bounded short-description matching with human-confirmed ambiguity; the browser session predates backend-only locator/CLI fixes whose final bytes are covered by automated gates, and the dirty tree plus later infrastructure/operations milestones still block production release |
| 2026-07-24 | D092 | Build M27 as a bounded governed-retrieval and signed-confirmation slice, with optional nano-first structured AI and no M24 API expansion | Tenant catalogs may contain 10, 5,434, or more tables, but complete-catalog prompting, name-only equivalence, browser-trusted selections, and provider-generated SQL would violate scale and safety invariants | Governed executable search and physical discovery remain distinct; the prompt closure is at most 3 models/12 fields/2 joins; every match is recomputed and human-confirmed; `gpt-5-nano-2025-08-07` is evaluated first using the existing environment key; Streamlit/runtime alone receives the optional provider boundary |
| 2026-07-27 | D093 | Reduce M27 external AI to minimal server-owned slots and validate signed campaign digests against the raw historical report payload before schema normalization | The prior provider contract asked the model to repeat roles, types, operations, grains, ordering, and coverage that the server already knew; adding a content-free optional failure field later proved that recomputing an old signature after Pydantic defaults is not schema-evolution safe | Expansion may return only slot ID, atomic query, and source span; interpretation may return only opaque slot selections, explicit filter values, and closed ambiguity kinds; the server reconstructs all executable semantics and binds approved public metadata to tenant scope plus registry. Signed v4/v5 payloads retain their original byte-semantic digest, an unsaved failed v6 attempt is not model evidence, and policy remains disabled until a fresh Nano campaign after budget reset |
| 2026-07-27 | D094 | Supersede D093's provider-owned grounding and selection values with the v16 locally reconstructed contract, and evaluate it only as a fresh holdout-bound v7 campaign | The signed terminal v6 campaign showed that asking a model to copy source spans, opaque candidate IDs, and filter literals remained unstable; one valid but noncanonical alternative also changed an otherwise aligned fingerprint, while repeating the identical critical wording three times overstated robustness | Expansion emits only `slot_id`/atomic `query`; interpretation emits only complete `slot_id`/`option_index=1` selections plus ambiguity. SchemaBridge uniquely derives source span, candidate, and filter value, and a top-score tie is ambiguous before provider interpretation. Signed v4-v6 evidence remains immutable and non-resumable under v7; each critical case uses its original text plus two frozen paraphrases. External AI remains versioned, disabled at rest, and Nano-first with no runtime cascade |
| 2026-07-27 | D095 | Make analytical expansion entirely local, persist the server-derived semantic owner in every probe, and permit provider use only for the separately admitted typed interpretation stage | The terminal v8 campaign made the same correct candidate impossible for Nano, 5.4 Nano, and Luna because the server derived `Customer` ownership but discarded it before ranking; the exposed option `1` was therefore `AccountHolder.customer_key`. This was a deterministic authority defect, not evidence that a quality threshold or budget should be relaxed | Owner, role, and canonical type are hard constraints before ranking and join closure; value-aware filter variants remain deterministic evidence; source-span conditionals are allowed only for filter purposes. Analytical expansion performs no reservation or egress, while interpretation can select only server-numbered option `1`. V8 remains immutable and terminal; any later provider run must first pass the exact 15-variant provider-free oracle and start a new non-resumed campaign under fresh fingerprints and policy |
| 2026-07-27 | D096 | Accept the schema-v2 ordered campaign/ledger attestation, treat the URL-policy browser block as a pre-provider failure, and return external AI immediately to disabled policy v86 | Signed v11 selected Nano, but exact accounting required a separately authenticated witness. The accepted content-addressed v2 artifact proves one unique chronological matching across all 16 signed attempts and durable policy-v83 reservation/audit pairs while explicitly avoiding a native content-identity overclaim. During the separately authorized one-submission UI smoke, the internal browser rejected `http://127.0.0.1:8510` under its URL policy before page load, form, click, submission, or provider activity; a repeatable-read/read-only policy-v85 ledger snapshot confirms zero reservation, interpretation, settlement, audit, request-window, or daily-usage rows | The v1 attestation remains superseded; v2 is the accepted historical accounting record and still verifies after later policy revisions. The blocked UI attempt is recorded as `blocked_before_provider`, not product/provider success or quality failure, and is not retried through a bypass. Policy v86 is current and disabled with zero active/open/stale reservation or reserved tokens. M27 may rely on the separate signed live synthetic campaign plus completed fake/runtime browser matrix, but production still requires provider DPA/retention/region approval and M28–M31 |
| 2026-07-27 | D097 | Accept M27 locally after the final schema, service, package, quality, coverage, scale, and browser matrix passes, while retaining the global production/release NO-GO | All 50 criteria now have exact evidence: schema v8 and six-role separation, 552 focused tests, regenerated 10/75 integration fixture, 148 final integration and 35 acceptance tests, deterministic evaluation, runtime wheel, 697-file release audit, 2,281-test quality gate, 2,459-test coverage at 81.47%, reviewed 41,028-field index/cold plans, diff hygiene, signed Nano campaign, v2 ledger attestation, and desktop/mobile fake/runtime browser acceptance. The only full-integration defect was a test fixture using `clock_timestamp()` inside an exact fenced transaction; changing only that fixture to `statement_timestamp()` preserved the production guard and the complete rerun passed | M27 is the accepted local synthetic Query Studio baseline and M28 is eligible but not started. The release audit's dirty-tree warning remains, the live-UI URL-policy block remains accurately non-provider evidence, external AI remains disabled at policy v86, and provider DPA/retention/region review, tenant connector routing, operated infrastructure, M28–M31, clean release identity, and external sign-off still block production/release |
| 2026-07-28 | D098 | Start M28 with a target-bearing plan, separate versioned connector routes, PostgreSQL-only executable capability matrix, and bounded `EXPLAIN` without `ANALYZE` | M26 already proves one common connection but discards it, while the current plan/compiler/guard/jobs use no target and source/catalog adapters use deployment-wide credentials. Adding a route only inside the executor would leave approval vulnerable to target substitution; adding unverified dialects or `EXPLAIN ANALYZE` would weaken accepted safety controls | `QueryPlan` stays vendor-neutral; the governed resolved-plan fingerprint binds workspace/connection/dialect/route/reader/type/budget; route state stores only opaque references; unsupported dialects and federation fail closed; route/semantic/cost checks repeat before source I/O; M29 still owns operated remote secrets and infrastructure |
| 2026-07-28 | D099 | Bind every executable catalog generation to an immutable source/catalog/type identity triple and keep connector credential bindings out of all public contracts | A target-bearing plan is still unsafe if its fields came from a legacy, differently routed, or differently typed catalog generation. Conversely, putting an opaque secret reference on `CatalogConnectionRoute`, a job, cursor, generation, API response, or UI model would let private routing authority cross the domain/public boundary and make capability separation unenforceable | Schema v9 records the exact source-identity, catalog-identity, and type-contract fingerprints on refresh bindings and promoted generations; staging inherits them, delta bases must equal them, and legacy-null or mismatched generations are non-executable until a fresh refresh. Public targets/routes expose only approved identity and revision fingerprints. The catalog port alone may carry a `repr=False` private binding, and PostgreSQL/DataHub secret loaders remain separate for `preflight`, `catalog`, `execution`, and `profile`. Secret-only rotation may retain generation evidence but invalidates the old target; identity-changing rotation requires a fresh generation. This is an implementation decision, not an M28 acceptance decision |
| 2026-07-28 | D100 | Support only the reviewed PostgreSQL native-type contract v1 and its canonical fingerprint throughout M28 routing | Accepting an arbitrary positive version plus a syntactically valid SHA allowed a route to claim normalizer semantics that the compiler and catalog do not implement | Domain targets, route snapshots/proposals, operator writes, PostgreSQL loaders, persistence checks, job guards, and generation activation all reject any other identity before route mutation or protected I/O. A future type contract requires an explicit code and schema migration; this is not an M28 acceptance decision |
| 2026-07-28 | D101 | Keep the current job workspace distinct from the immutable historical workflow/connector workspace after verified identity rotation | Collapsing both scopes during worker composition could redirect an already approved workflow to a connector in the current identity scope | Job visibility/idempotency/lease use the current scope; the target FK, route/generation/private binding, worker context, and execution loader use the persisted historical scope. Cross-scope substitution fails closed; this is not an M28 acceptance decision |
| 2026-07-28 | D102 | Bound PostgreSQL `EXPLAIN` JSON before decoding and preserve exact numeric semantics | Measuring a fully decoded object permits avoidable allocation, while the default JSON `float` conversion can round a just-over-budget cost or fractional row estimate into an accepted value | A connection-local raw loader enforces the hard byte bound before `json.loads`; the governed budget is checked before decoding, duplicate/non-finite payloads fail, decimals parse as `Decimal`, and only bounded sanitized aggregates survive. This is not an M28 acceptance decision |
| 2026-07-28 | D103 | Accept M28 locally after the complete schema, service, package, quality, coverage, scale, and internal-browser matrix passes, while retaining the global production/release NO-GO | Final evidence includes schema v9 and the six-role matrix, 164 passed/1 skipped integration tests, 47 acceptance tests, 2,714 quality-gate tests, 2,920 passed/1 skipped at 81.76% coverage, deterministic evaluation, runtime wheel, release/scale/diff postflight, and all nine browser scenarios at desktop and mobile with exact cleanup | M28 becomes the accepted local connector-routing and cost-governance baseline; M29 is eligible but not started. The dirty tree, operated remote controls, M30/M31, clean signed release identity, and external sign-off continue to block production/release |
| 2026-07-29 | D104 | Treat a clean hosted checkout as an independent reproducibility boundary and seed only explicitly approved synthetic external state in CI | The first draft-PR run showed that locally installed DataHub/MCP packages, ignored immutable campaign evidence, persistent DataHub documents, and ANSI terminal behavior could coexist with green local gates but fail from zero state | `bootstrap` installs the existing reviewed DataHub extra; only content-addressed signed M27 reports/attestations are tracked from the ignored runtime directory; CI uses color-free logs and an exact `publish-approved-registry-version` confirmation plus read-back check. No source write, secret, unsigned report, or implicit DataHub fallback is introduced, and production/release remains NO-GO |
| 2026-07-29 | D105 | Align hosted synthetic control-admin authentication through the test-only DSN and separate the registry seed principal from the replay-test principal | The second clean run proved that changing the control-container placeholder without propagating it to administrative test DSNs causes authentication failure, while seeding and replaying the same payload with the same deterministic actor but different approval timestamps correctly causes an immutable-facts collision | Every affected test and browser-runtime fallback uses the Compose file's explicit `local-only-not-a-secret` loopback placeholder while retaining the existing test-only override; the exact approved registry seed alone uses `ci-registry-bootstrap`, preserving explicit confirmation/audit while the later `operator` replay retains an independent approval ID. No trust authentication, real secret, actor CLI assertion, or production identity is added |
| 2026-07-29 | D106 | Normalize terminal styling before asserting the semantic CLI-help contract | Both hosted Linux quality jobs retained Rich/Typer ANSI sequences despite `NO_COLOR=1`, splitting long option names while the same command and option set remained valid | The three affected tests use `click.unstyle` before checking required and forbidden flags. Production CLI output, option names, security boundaries, and negative assertions remain unchanged; CI no longer treats renderer bytes as command semantics |
| 2026-07-29 | D107 | Scope generic source credentials to their exact CI consumer and pause coverage only for the dedicated wall-clock scale test | The third hosted run passed quality, DataHub, control-plane, integration, and acceptance gates, then exposed two coverage-only effects: job-wide `DATABASE_URL` correctly violated 31 component credential-separation tests, and coverage instrumentation made one 64-read latency smoke nondeterministic while its error, row, and materialization contracts passed | The integration job keeps only the dedicated test source DSN globally; the generic source DSN exists only on the evaluation step, and control-admin tests use their explicit test-only fallback/override. The scale smoke still runs with unchanged 250/500 ms limits, but pytest-cov pauses instrumentation for that test; a separately executed deterministic gate proves every exact limit and failures now print sanitized metrics plus the exact failed checks |
| 2026-07-29 | D108 | Run the wall-clock scale smoke only in the clean quality gate and exclude its `performance` marker from service-loaded coverage | On the same commit, the push integration job passed 2,925 tests plus one retained skip at 81.75% while the duplicate PR job passed every functional check but measured p95 292.712 ms on a contended hosted runner and failed the unchanged 250 ms smoke budget. Per-test coverage pausing removed instrumentation but could not remove concurrent DataHub/PostgreSQL and shared-runner contention | The hosted quality job remains the mandatory clean-runner home for the 64-read smoke and its unchanged 250/500 ms limits; release clean-room now also runs `make check` before it starts services. Full and unit coverage deselect only `performance`; the pure regression gate remains selected there and proves the exact error, percentile, row, and materialization limits. The explicit 5,000-read concurrency-16 PostgreSQL benchmark remains the authoritative operated performance gate |
| 2026-07-29 | D109 | Start M29 with one provider-neutral remote-secret port, distinct projected workload identities, default-deny network paths, governed telemetry, exact build provenance, and verified fresh-target recovery | M28 makes routing and local secret files safe enough for synthetic evidence but does not provide remote rotation, infrastructure isolation, operational detection, reproducible transitive bytes, or rehearsed retained recovery. Treating static manifests, action tags, a generated SBOM, or an existing archive as operated evidence would preserve production blockers | Managed composition must use exact-version HTTPS remote secrets and cannot fall back locally; shared all-tenant secret mounts are forbidden; telemetry and artifact metadata are closed/sanitized contracts; CI inputs and images are immutable; only a signed backup plus verified distinct-target drill counts as recovery. M30/M31 remain blocked and provider-only steps remain NO-GO until actually operated |
| 2026-07-29 | D110 | Store one explicit immutable external provider version per private connector capability and never derive it from the public route revision | A route revision describes approved SchemaBridge state, while a Vault/OpenBao KV version identifies external secret material; treating them as equal or requesting `latest` permits secret substitution behind an unchanged approval | Binding format v2 and schema v11 bind all four provider versions into route approval state. Legacy v9/v10 routes receive no inferred backfill and are non-executable until an operator verifies external versions and performs a newly approved rotation |
| 2026-07-29 | D111 | Keep managed Streamlit planning-only by requiring execution and publication mode `disabled` | The browser runtime has no authenticated client for the existing API/job/worker lane and no durable publication approval queue/publisher worker; composing synchronous live or fake adapters would bypass the production capability topology or misrepresent recorded evidence | Managed web may retrieve governed context, match fields, compile deterministic SQL, apply the independent guard, and run preflight, but it receives no execution or DataHub-writer capability. Browser execution/publication remain explicit NO-GO actions until their typed durable lanes exist |
| 2026-07-29 | D112 | Isolate migration, check, backup, restore, and other reviewed control commands under an explicit `operator` runtime component with a clean process environment | Reusing managed web configuration would either require browser credentials for offline control work or allow ambient `.env`, OIDC, OpenAI, DataHub, API-auth, and connector-secret capabilities to leak into privileged operator commands | Operator commands select only their exact control/source credentials, never load the developer `.env`, and fail closed on unrelated capabilities. Make targets clear ambient variables before supplying each reviewed operation's minimum environment |
| 2026-07-29 | D113 | Treat the M29 repository outputs as production-shaped local contracts, not operated production or release evidence | Static manifests, mocked provider replies, local metrics, generated SBOM policy, and local retention planning cannot prove provider IAM/revocation, cluster admission/CNI, alert delivery, immutable remote storage, fresh-target cutover, protected signing, production SLOs, or external review | M29 remains in progress and every acceptance checkbox stays open until final-byte local gates are recorded; external/provider criteria remain NO-GO until actually operated, and M30/M31 cannot start from implementation presence alone |
| 2026-07-29 | D114 | Make candidate secret/artifact/architecture scanning part of every static gate and require an environment-only sentinel before any release action | Repository inspection found that a missing GitHub environment would otherwise be created without reviewer rules, while the prior CI static gate tested scanner fixtures but did not run the scanner against commit-visible files | `make check`, CI, and release evidence run the non-network release audit; release additionally requires a clean exact revision. The published-release job validates the protected-environment sentinel before checkout, login, build, push, or attestation, so an absent environment/configuration fails closed |
| 2026-07-29 | D115 | Accept the reproducible local M29 baseline while retaining production and release NO-GO | Schema v11/seven-role, full PostgreSQL, acceptance, deterministic evaluation, wheel/frozen install, local recovery, scale, browser, 3136-test quality, and 3325-test/81.09%-coverage evidence pass. The prepublication audit additionally closed hidden artifact-upload and interrupted-coverage gaps with 54 focused regressions | The M29 local contracts may be committed and reviewed on draft PR #1. Provider rotation/revocation, target-cluster enforcement, alert/SIEM delivery, immutable remote retention, external cutover/rollback, protected release evidence, M30/M31, and independent approval remain required before any production or release claim |
| 2026-07-29 | D116 | Baseline secret-scan false positives only by exact immutable fingerprint and mark current synthetic fixtures line-locally | Independent Gitleaks review found eight historical synthetic/non-secret matches plus two current operational-name matches; broad path/rule allowlists would hide future credentials, while rewriting public history is unnecessary and unsafe | `.gitleaksignore` binds only the eight reviewed commit/path/rule/line fingerprints; current fixtures and non-secret names use exact `gitleaks:allow` markers. Final history scan reports zero findings and the directory scan reports only ignored environment subjects |
| 2026-07-29 | D117 | Constrain every Trivy cache to ignored `.local/trivy-cache` and enforce the path through static workflow policy | Hosted supply-chain run `30493061776` reached both image scans, then failed closed before provenance because Trivy's default `.cache/trivy` made the exact checkout dirty | CI and release declare the same reviewed `cache-dir`; absence or deviation fails as `trivy_cache_path_invalid` in both workflow regressions. The corrected hosted result remains external to this commit, and production/release remain NO-GO |
| 2026-07-29 | D118 | Audit every frozen Python runtime/build input without a resolver environment and build the runtime from a hash-bound reproducible Alpine wheelhouse | Hosted run `30495413406` proved that default `pip-audit` omitted already-present `packaging`, while the old Debian runtime exposed high/critical OS findings. Alpine removed those findings, but `watchdog==6.0.0` has no musllinux wheel and an unconstrained sdist build was nondeterministic. The initially pinned `setuptools==81.0.0` then audited as vulnerable, while DataHub constrains its unrelated application copy below 82 | CI/release use `pip-audit --disable-pip` across runtime, project-build, and isolated watchdog-build inputs. Both stages use the exact Python 3.13.14/Alpine 3.24 digest; isolated hash-bound `setuptools==83.0.0`, the upstream sdist upload epoch, and `requirements/runtime-built.txt` reproduce the same watchdog wheel SHA-256 twice on arm64 and on amd64. BuildKit mounts the wheelhouse read-only; exact final-stage RUN policy and Docker-context exclusions reject added resolution or key material. The final local image passes smoke and current 0-HIGH/CRITICAL scanning; protected hosted evidence and production/release remain separate NO-GO gates |
| 2026-07-30 | D119 | Accept only the explicit CycloneDX 1.5 and 1.6 schema set at the evidence boundary | Hosted run `30520807060` passed image construction, scanning, complete dependency audit, vulnerability policy, and evidence generation, then failed because pinned Trivy emitted a valid 1.6 runtime-image SBOM while the verifier allowed only the project-generated 1.5 form | Wheel generation remains deterministic 1.5; runtime-image evidence may be 1.5 or 1.6. Both retain complete component, artifact-digest, source-revision, lock, and provenance checks, while every other schema version fails closed |
| 2026-07-30 | D120 | Require a manual fail-closed prepublication release state machine and treat registry exclusivity as an external release prerequisite | A workflow triggered after a public Release, an admin-only branch-protection query, or a registry lookup that treated every inspection failure as absence could publish before evidence was complete, fail under the job token, or overwrite a reference after authentication/network failure. GHCR also cannot prove from repository YAML that no PAT, App, repository, workflow, or human has competing package-write authority | Promotion starts only from an existing annotated SemVer tag through the protected environment. The job uses `GITHUB_TOKEN`-readable branch/effective-rules endpoints, requires exact `quality`, `postgres-integration`, and `supply-chain` push-CI contexts, completes local gates before login, publishes only a previously absent candidate, scans/attests and recovers evidence by remote digest, creates a non-public draft, rechecks the final reference immediately before same-digest promotion, verifies it, and publishes as the terminal step. Protected `main`, an independent environment reviewer with self-review/bypass disabled, eligible tag rules, exclusive GHCR writer authority, and GitHub immutable Releases remain external NO-GO prerequisites. This extends D114 while retaining its sentinel and candidate scans |
| 2026-07-30 | D121 | Give scheduled control-plane backup its own schema-v12 identity and install the PostgreSQL client from an exact five-APK offline closure | Reusing the migrator/operator identity grants unnecessary control mutation, inheriting the complete process environment can pass OpenAI/audit/web credentials to `pg_dump` or `pg_restore`, and either installing from the network or copying an untracked filesystem closure makes the final runtime difficult to audit and reproduce | Schema v12 grants the eighth `schemabridge_backup` credential complete read-only control-schema access with write denial. `schemabridge-backup` and the hourly CronJob receive only the versioned backup DSN/audit key, external PVC, and `control-backup` egress; the PostgreSQL subprocess receives only `PATH` plus required `PG*` variables. For amd64/arm64, a digest-pinned Alpine stage fetches exactly `libpq`, `lz4-libs`, `postgresql-common`, `postgresql16-client`, and `zstd-libs` at reviewed versions and SHA-256 values; the final stage installs only the read-only mounted APKs with `apk --no-network`. This supersedes only D118's former two-stage image topology, not its frozen Python wheelhouse policy |
| 2026-07-30 | D122 | Activate only observability with a real composed producer and make managed Operations explicitly unavailable without an operated source | A closed metric name, alert file, SIEM adapter, or synthetic operations scenario does not prove that the deployed process emits the input or that a destination receives it. Presenting those contracts as active would create false dashboards/pages and could show synthetic health in staging or production. Separately, a direct Streamlit command did not prove managed configuration/auth/schema readiness or listener availability | The active bundle is exactly eight producer-backed metric families, six alerts, three SLOs, and eight dashboard panels, mirrored by one exact PrometheusRule. SIEM and uncomposed backup/release/integrity/capacity artifacts remain schema-validated under `deploy/observability/inactive/` until lifecycle-owned producers/exporters and operated delivery exist. `schemabridge-web` preflights managed configuration, authentication, and schema before launch; readiness repeats those checks and requires the fixed bounded loopback 200/`ok` health response. Staging/production Operations renders unavailable without an operated source, while synthetic scenarios remain development/hosted-demo only. This extends D111; final-byte local acceptance remains pending and no new acceptance decision is recorded yet |
| 2026-07-30 | D123 | Accept the final-byte M29 local baseline and use a split replay-safe release state machine while retaining production and release NO-GO | Final evidence passes `make check` with 3335 tests and 214 deselected, current-byte coverage with 3534 passed/14 external skips/1 deselected at 81.17%, 154 release/supply-chain tests, schema v12/eight credentials, integration/acceptance/browser/image/recovery/scale gates, zero-leak worktree/staged/history scans, and a seven-job workflow at SHA-256 `9979c54be6ba39d1d7b606e6d882aa10e9868bb9d003482b30a780ee104d1f26` with Actionlint, ShellCheck, static/adversarial policy, and independent P0/P1/P2 = 0 review | M29 is accepted locally. Release mechanics are `audit → prepare → candidate → scan → attest → promote → release`; protected GET-only audit and exact immutable-publication no-op supersede only the release mechanics in D114/D120, while D115's earlier local baseline is superseded by the current evidence. No `production-release` environment, unprotected `main`, zero rulesets, Immutable Releases disabled, unproved exclusive Release/GHCR writers, and all provider/cluster/operations gates keep release and production NO-GO and M30/M31 blocked |
| 2026-07-30 | D124 | Bind the pinned Python base image's virtual `.python-rundeps` SBOM identity exactly per platform | PR run `30560980711` showed that the same pinned multi-architecture digest contains `20260616.002547/noarch` on `aarch64` and `20260616.002554/noarch` on `x86_64`. A wildcard would hide real drift, while treating the arm64-generated virtual timestamp as universal rejected valid x86_64 evidence | Accept only that closed two-entry mapping. Retain exact inventory, hashes, architecture, dependency-graph validation, and the five fetched PostgreSQL package URL/SHA bindings for every real APK. Local regression/static gates pass; the corrective exact revision and hosted rerun remain required |
| 2026-07-30 | D125 | Accept M32 locally as a bounded deterministic/synthetic natural-language-to-copyable-PostgreSQL capability while retaining manual-browser, live-quality, production, and release NO-GO | The exact typed-preview/confirmation path, v1/v2 representability routing, complete approved logical-registry traversal with bounded closure, deterministic pre-resolution/re-resolution, parameterized compiler, independent AST guard, typed copy renderer, second guard, CLI/Streamlit contracts, 304 targeted tests, stable SQL SHA-256, five-row read-only integration, and final 3,587-test quality gate pass. The in-app browser list was empty and the deterministic fixtures are not a live-provider holdout | M32 may return standalone SQL only for the same governed PostgreSQL database/context after exact human confirmation and always reports `executed=false`. Physical-only context stays in the separate M27 `needs_mapping_review` lane; unsupported families return no SQL. Other dialects/destinations, browser manual evidence, live-provider production evaluation, M30/M31, and all release/production gates remain open |
| 2026-08-02 | D126 | Accept M33 locally as generic governed semantic onboarding that stops at an immutable non-executable handoff | Exact tenant/catalog preflight, schema-v13 PostgreSQL authority locks and persistence, ordered type validation, closed regex/padding bounds, fail-closed `parse_date`, alias-safe physical identity, verified identity-lineage replay, CAS/idempotency, separation of duties, 225 focal unit tests, 7 PostgreSQL integrations, 2 acceptance tests, a 3,682-test first final-byte quality gate, desktop/mobile internal-browser evidence, and independent P0=0/P1=0 review pass | M33 may create/review/prepare `ready_for_publication` only and performs zero source/DataHub/LLM/SQL/activation I/O. M34 dedicated publication/readback and separate activation are next; M30/M31, combined real-PostgreSQL rotation coverage, operated controls and every production/release gate remain NO-GO |
| 2026-08-02 | D127 | Keep signed control-plane backup/restore evidence bounded at 128 base tables and record M33's active-pointer read as an exact API capability | Hosted PostgreSQL run `30765372081` failed closed because schema v13 contains 67 base tables while both evidence models still capped the complete inventory at 64; the same run showed the legacy jobs permission matrix had not incorporated M33's intentional read-only active-pointer preflight grant | Backup and restore now accept and verify all 67 current tables, reject 129 without truncation, and retain a read-only backup identity. Growing beyond 128 requires an explicit contract/scale migration. The API gains no pointer write or registry-payload authority, and production/release remain NO-GO |
| 2026-08-03 | D128 | Accept M34 locally as a bounded governed registry-v2 publication and activation-ready handoff while retaining global production/release NO-GO | One exact M33 proposal passes target reservation, database-time lease/fencing, complete-candidate approval, isolated DataHub writer, explicit absent-target privilege handling, immutable exact read-back, opaque observed URN preservation and a strict M23 activation-ready authority check. Required M34 tests pass 118 with 5 explicit external DataHub skips; fresh PostgreSQL/M23/observer pass 17; deployment/runtime pass 215; wheel, publisher readiness, internal-browser paths, full quality gate and independent P0/P1 review are recorded in the handoff | API/web never obtain the writer secret and publication never changes the active pointer. M34 supports only initial/additive v2 model publication. Exact live DataHub, hosted/external IAM and operations, live-provider quality, other SQL dialects/engines, batch/scale evidence, M30 and M31 remain mandatory before any commercial or production GO |
| 2026-08-03 | D129 | Accept M35 locally as the bounded immutable registry-v2 join and one-model replacement/remediation lifecycle while retaining commercial/production NO-GO | Phase A adds one explicitly approved same-connection join from aggregate-only evidence. Phase B consumes one immutable M33 source, exact active base/dependencies and optional complete blocking M26 report, accounts for every incident join, and passes a closed `replace_model_v1` publication witness. PostgreSQL v15 has 88 backup-visible tables; the vertical reaches `activation_ready` with exact handoff and unchanged pointer, four candidate tampers fail, 165 focal unit and 6 M34/M35 PostgreSQL tests pass, browser Phase A passes, and the full gate passes 3,881 tests with 245 explicit deselections | One M33 replacement source is deliberately one-shot; rejection or staleness requires a new immutable source. Local DataHub read-back is simulated and no activation is executed. PostgreSQL remains the only output dialect, one query remains limited to three tables/two joins, and operated M29 controls, M30 blind quality/security, M31 pilot, legal/service and per-dialect certification remain mandatory |
| 2026-08-03 | D130 | Prepare M30 with an offline fail-closed candidate-readiness contract that can never produce release GO | The accepted local features do not form one candidate identity, the required corpus is 1,000 blind cases, external M29/security evidence is absent, and PR run `30804919220` uploads supply-chain subjects for merge ref `792e1e7f1ec0c2eed75dfba08302304f17d9c9ff` rather than branch head `11c2f8527d3e935cb44292fb58f8c23b9611b8eb`. Accepting local reports, self-asserted JSON or merge-ref artifacts as operated candidate evidence would create a false commercial claim | The contract fixes typed-plan-v2 PostgreSQL limits, 500 ES/500 EN slices, SQL families, thresholds and 24 evidence controls. Git inspection ignores replace refs and inherited Git configuration, rejects hidden/sparse/symlinked state, binds the tag to `v{package_version}` and rechecks one stable snapshot. This schema reports only `blocked_prerequisites`: external controls remain `missing_external`, `release_decision` is always `no_go`, and any future signed-evidence verifier requires a separately reviewed contract/adapter rather than a local status field |
| 2026-08-03 | D131 | Measure the synthetic wall-clock percentile gate in one exact fresh process over 1,000 reads while retaining the 250/500 ms limits | Hosted runs `30811612188` and `30816314566` retained a correlated latency tail after one and then four preconditioning reads. At 64 observations with concurrency four, nearest-rank p95 is the fourth-largest sample and p99 is the maximum, so a tail compatible with the synchronized four-worker launch can control both declared percentiles. The second run passed 3,930 other tests with zero load errors and p50/p95/p99/max 26.420/330.102/355.132/355.132 ms, refuting the earlier cold-worker explanation without proving GC, GIL, scheduler or product causality | `make check` runs only the exact performance node in a fresh service-free Pytest process, then starts the functional suite separately. The smoke increases from 64 to 1,000 concurrency-four reads, keeps per-worker preconditioning and unchanged budgets, and deterministically proves the percentile boundary: at least 51 observations over 250 ms fail p95 and at least 11 over 500 ms fail p99. Four correlated transients are only 0.4%; no sample is discarded. Sanitized reports add exact tail counts and identify nearest-rank. Coverage and correctness targets exclude the wall-clock node. This supersedes D108's 64-read mechanism only; the explicit 5,000-read concurrency-16 PostgreSQL benchmark remains authoritative operated evidence and production/release remain NO-GO |

## New entry template

```text
| YYYY-MM-DD | Dxxx | decision | evidence/reason | consequence |
```
