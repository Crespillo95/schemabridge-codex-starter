# Project state

Last updated: 2026-08-03

## Accepted milestone

M24 is complete and accepted locally as the authenticated API/durable-worker baseline; M20 through
M23 retain their prior local acceptance evidence. All 25 M24 criteria, fresh full/service/socket/
package gates, and Codex internal-browser desktop/mobile acceptance passed on 2026-07-23.

M25 is complete and accepted locally. Its full quality/integration/acceptance/evaluation/package/
audit matrix, Codex internal-browser desktop/390x844 record, and clean 1,538-test coverage gate at
81.98% all pass.

M26 is complete and accepted locally. All 30 criteria pass, including statistics-independent cold
locator evidence over 5,434 assets, exact multiconnection/tenant gates, complete blast radius,
real PostgreSQL/DataHub remediation, package/release/quality gates, 81.64% coverage, and the
desktop/mobile presentation record.

M27 is complete and accepted locally. `gpt-5-nano-2025-08-07` passed the complete 136-case
synthetic campaign and was selected without a model cascade. The accepted schema-v2 attestation
binds its 16 signed attempts to one unique chronological matching of 16 durable reservation/audit
pairs. Desktop and 390x844 browser evidence covers dynamic 10/75 and 5,434/41,028 inventories plus
the governed safety states. The one separately authorized live-UI reload was blocked by the
internal browser URL policy before page load or submission, was not bypassed, and made no provider
request; policy v86 is disabled and the historical v2 verifier still passes.

All 50 M27 acceptance criteria and the final focused/schema/service/evaluation/package/release/
quality/coverage/scale/diff matrix pass. Coverage is 81.47%.

M28 is complete and accepted locally. Schema v9, the six-role matrix, exact two-tenant routing,
PostgreSQL-only compilation/guarding, bounded cost preflight, dynamic 10/75 and 5,434/41,028
inventory paths, 164-test integration, 47-test acceptance, 2,714-test quality, 81.76% coverage,
package/release/scale/diff postflight, and all nine desktop/mobile internal-browser scenarios pass.
M29 is complete and accepted locally under D123. Its focused, schema/service,
release/supply-chain/static, integration, acceptance, deterministic evaluation, runtime-wheel,
local-image, recovery, scale, browser, quality, and coverage evidence passes after the final audit
remediation. The tree advances the control plane to schema v12 and eight credentials, adds
scheduled-backup and managed-web process boundaries, closes the Kubernetes graph at 63 resources,
activates only producer-backed observability, and installs an exact five-APK amd64/arm64
PostgreSQL-client matrix offline from a read-only BuildKit mount.

The seven-job release-topology redesign and `make check` are closed locally. The workflow SHA-256
is `9979c54be6ba39d1d7b606e6d882aa10e9868bb9d003482b30a780ee104d1f26`;
Actionlint 1.7.12, ShellCheck 0.11.0, static policy, 158 adversarial release/supply-chain tests, and
independent review pass with zero local P0/P1/P2 findings. Current-byte coverage passes 3534 tests
with 14 external skips and 1 deselected at 81.17%, superseding D115's 81.09% historical baseline.

M32 is complete and accepted locally under D125 for its bounded deterministic/synthetic product
capability. Natural-language preparation traverses the complete approved logical registry, builds
one 3-model/12-field/2-join closure, resolves approved datasets/mappings/joins/fanout into an exact
review preview without compiling SQL, and routes by typed v1/v2 representability. Exact
confirmation reloads/revalidates/re-resolves the signed request, then produces deterministic
parameterized PostgreSQL, passes an independent AST guard, renders typed standalone literals, and
passes the complete guard again with zero bindings. The copy artifact is always
`executed=false`.

The M32 targeted matrix passes 304 tests, including four simple/route-control Spanish end-to-end
journeys, 14/14 governed retrieval cases, all 14 closed window operations, all seven conditional
aggregate operations, automated Streamlit acceptance, the stable reference SQL SHA-256
`ec589a1527d0d641f4f7f7eb7e052ca1ace5b092013b437da72542ce4145d4f3`, and one optional
read-only PostgreSQL integration returning five exact rows. The final quality gate passes 3,587
tests with 224 explicit deselections. The in-app browser list was empty, so the manual
desktop/mobile/browser matrix is documented but not claimed as PASS. M32 adds no source write,
DataHub write, arbitrary SQL, cross-dialect, production, or release authority.

M33 is complete and accepted locally under D126. An authenticated tenant can resolve one exact
active PostgreSQL catalog/registry snapshot, create a tenant-bound semantic draft, record explicit
append-only steward decisions, and let a distinct publisher prepare an immutable audited
`ready_for_publication` proposal. Physical authority is bound to exact
workspace/connection/generation/`schema.table.column` coordinates and metadata fingerprints;
aliases cannot approve one coordinate for two meanings. Opaque identity rotation retains only
verified historical workspace/actor pairs, including exact concurrent idempotency replay.

The final M33 matrix passes 225 independently reviewed focal unit tests, 7 PostgreSQL integration
tests, 2 acceptance tests, Ruff, mypy and `git diff --check`; independent review reports P0=0 and
P1=0. Codex internal-browser acceptance passes the empty → 3-mapping draft → 4 decisions →
revision-6 handoff path at 1280×720 and 390×844 with zero horizontal overflow, empty console,
`external_writes_performed=false`, no SQL and no publication/activation/execution control. The
first final-byte `make check` passed 3,682 tests with 233 explicit deselections. The hosted
PostgreSQL gate then exposed that schema v13 has 67 base tables while backup/restore evidence was
still capped at 64. The corrected bounded contract accepts all 67, rejects 129 against a maximum of
128, preserves the read-only backup posture and passes the final 3,685-test quality gate plus all
11 control-plane PostgreSQL cases. M33 adds no source, DataHub, LLM, compiler, preview or activation
authority. M34/M30/M31 and all operated external release controls keep production/release **NO-GO**.

M34 is complete and accepted locally under D128. One exact M33 proposal now moves through a
tenant-bound schema-v14 reservation, fenced preparation lease, approval of the complete candidate,
an isolated DataHub writer, exact independent read-back, and a terminal `activation_ready`
handoff. Registry format v2 retains the exact workspace, connection, generation/vector, locator,
opaque observed dataset URN, physical type and metadata fingerprints for every active mapping;
names and platform defaults never create authority.

The API can submit, inspect, authorize and cancel but cannot resolve the DataHub writer secret. The
publisher process has its own PostgreSQL role, exact-version writer binding, probes, metrics,
ServiceMonitor, NetworkPolicy and publication queue aggregate, but no source, LLM, OIDC or active
pointer capability. A pre-write database heartbeat observes cooperative cancellation. Once the
write boundary is crossed, exact read-back wins over a cancellation label. M23 accepts only the
strict v2 `activation_ready` handoff and rechecks catalog authority atomically before its separate
approved CAS; rollback and reconciliation retain their prior contracts.

The required M34 matrix passes 118 tests with 5 explicit live-DataHub skips because local reader
and writer credentials are absent. Fresh schema-v14 PostgreSQL publication, full M23, recipe and
operational-observer regressions pass 17 tests; deployment/runtime/metrics tests pass 215; the
installable wheel covers migrations 1–14 and every runtime entrypoint. The exact commit candidate
passes supply-chain/release policy, Ruff, mypy over 343 source files and the full 3,779-test unit
gate with 238 explicit deselections. Codex internal-browser
evidence passes the exact queued → leased → awaiting-approval → approved → leased →
activation-ready path with one immutable write, one opaque related asset and an unchanged active
pointer. A separate advanced M32 Spanish path produced a reviewed 106-line standalone PostgreSQL
statement with two CTEs, joins, `COUNT DISTINCT`, `HAVING`, ranking, percentage and running sum,
while remaining `executed=false`. The final full-gate and independent-review evidence is recorded
in `tasks/M34_HANDOFF.md`.

M34 does not establish a production or commercial GO. The exact live DataHub v2 integration is
checked in but not exercised locally; live-provider holdout quality, other SQL dialects and target
engines, batch onboarding, large-catalog/load evidence, external IAM/TLS/secret rotation,
independent security review, operated SLOs, legal/privacy controls, M30 and M31 all remain open.

M35 is complete and accepted locally under D129. Phase A creates one tenant-bound join change over
two exact active mappings, obtains only aggregate read-only relationship evidence, requires an
explicit steward decision and assembles the next immutable registry-v2. Phase B consumes one
immutable M33 replacement proposal, exact active base/catalog/dependency authority and, for
remediation, one complete current blocking M26 report. It replaces exactly one model and accounts
for every incident join as exact preservation, freshly profiled upsert or explicit removal.

Control-plane schema v15 adds closed profile/change history and generalizes M34 publication with
`add_join_v1` and `replace_model_v1` sources while preserving historical M33/M34 payloads. The API
exposes strict tenant-derived Phase A/B commands but no source/DataHub credential; the worker
rechecks exact authority before source I/O; the publisher verifies the outer proposal, M33 source,
decisions, dependencies, M26 state and current profile witnesses. PostgreSQL accepts the model
replacement handoff only when its typed witness matches model/fields, mappings, bindings,
approvals and every incident-join effect.

The final M35 evidence includes 165 focal unit tests, 17 HTTP boundary tests, 6 fresh M34/M35
PostgreSQL publication tests, one isolated v15 backup proof over all 88 base tables, 7 acceptance
passes with one explicit historical-v1 skip, Ruff, mypy over 357 source files and the full 3,881
test gate with 245 explicit deselections. A final migration-expectation audit corrected stale v14
lists in historical upgrade fixtures; all 64 affected PostgreSQL tests pass against v15. The
PostgreSQL Phase B vertical reaches
`activation_ready`, rejects model/mapping/binding/incident tampering and leaves the active pointer
unchanged. Codex internal-browser Phase A passes the join journey and self/cross-connection/stale/
many-to-many hostile cases at desktop/mobile with zero source writes, SQL, rows or credentials.
Final independent security review found and corrected one invalid equality between the immutable
M33 source owner and the separate outer change owner; the exact authority regression uses distinct
owners and the closing review reports P0=0 and P1=0.

M35 does not establish a commercial or production GO. Its DataHub publication/read-back vertical
uses a closed simulated receipt, not live external IAM. PostgreSQL is still the sole output
dialect; each request remains bounded to one connection, three tables and two joins; unsupported
SQL families return no SQL. Operated M29 controls, M30 blind bilingual quality/security, M31 pilot,
large-scale customer evidence, legal/service controls and separate certification for every future
dialect remain mandatory.

M30 Phase 0 and the fail-closed Phase-1a manifest-authentication vertical are implemented locally
under D130/D132; M30 itself is not accepted. The exact machine
contract now fixes the PostgreSQL typed-plan-v2 copy-first SKU, one connection/three tables/two
joins, request/window/context/preview/timeout/`NULL`/fanout limits, supported and unsupported
families, five corpus classes totalling 500 Spanish plus 500 English cases, zero-tolerance and
quantitative thresholds, and the evidence class for all 24 external controls. The offline preflight
binds Git HEAD/tree, lock/build/workflow files, migrations 1–15, schema v15 and contract/source
digests, and produces deterministic ignored JSON/Markdown.

The preflight cannot accept repository fixtures, self-authored pass documents or PR merge refs as
hosted, operated, independent or owner evidence. Its focal unit/acceptance suite passes 49 tests.
The recorded pre-commit run on the dirty non-main untagged development tree correctly reported four repository
failures plus 24 missing external controls, `campaign_executable=false`, zero external actions and
`release_decision=no_go`. That is preparation evidence only; the candidate must later be merged,
tagged, built and attested on its exact subject before a blind campaign may start.
The local implementation gate passes 3,931 tests with 246 explicit deselections; the runtime wheel
validates migrations 1–15 and all entrypoints. Independent post-remediation review reports P0=0,
P1=0 and P2=0. None of those local facts satisfies an external M30 control.

Phase 1a adds canonical external JSON, a structural schema plus authoritative validator, exact
artifact/provider/target/corpus/owner/control freezes and workflow-attested byte authentication.
Every corpus slice is equally Spanish/English and every supported advanced family now has
standard/high/critical cases, preventing monolingual slices and a vacuous critical threshold. The
split workflow SHA-256 is
`8944a48a3a14fe0c2aca4edca2d0a7bed00d0a0ce7fc699f5ff420ff5668d3d0`; candidate code runs only in
the read-only validation job. The signing job never checks out or executes candidate code. The
detached-bundle verifier pins exact GitHub attestation facts and official platform-specific `gh`
2.96.0 executable bytes, records verifier/bundle/certificate/timestamp hashes and uses private
snapshots plus sanitized config. Even success is only workflow-attested bytes:
`campaign_executable=false`, `release_decision=no_go`, zero controls passed and 24 remaining.

A pre-final isolated Phase-1a local gate passed Ruff format/lint, strict mypy over 364 source files,
the isolated performance node and 3,988 functional tests with 248 explicit deselections in
1,132.41 seconds. It predates the final trust-fact revalidation, injected-clock expiry check and
verifier-TOCTOU closure. The exact final-byte local `make check` reached 65% with no recorded test
failure before the execution environment sent SIGTERM 15, so it is not a pass. The exact
current-byte M30/supply-chain selection passes 52 tests with 158 deselections, and the clean
`bf7d18a` preflight reports only the expected `main`/annotated-tag repository failures plus all 24
external controls missing. Exact-head hosted CI remains the pending full gate. This is local
implementation evidence only and does not satisfy any external M30 control.

Phase 0 commit `c7e72cc97e4226b2d953f5c1e8ef55178a1598f5` and warmup commit
`3f57a2ea89220ff0c68ac58f0f8e668069e93f81` are published on
`agent/ignore-node-modules` and draft PR #1. Run `30816314566` checked out PR merge ref
`04402da8327f08ccc29799a6df4b4f2e9645dc90`, so it is not exact tagged-main candidate evidence.
GitGuardian and supply-chain passed. Quality passed 3,930 other tests and recorded zero load
errors, but the 64-read smoke failed only p95: p50/p95/p99/max was
26.420/330.102/355.132/355.132 ms against unchanged 250/500 ms limits. Its
`postgres-integration` job passed in 1 h 37 min 59 s; the run is red only because of quality.

The second hosted correlated tail remained after every executor worker was preconditioned,
refuting the earlier cold-worker explanation without proving GC, GIL, scheduler or product
causality. At 64 samples with concurrency four, nearest-rank p95 is the fourth-largest observation
and p99 is the maximum, so a tail compatible with the synchronized four-worker launch can control
both claimed percentiles. D131 retains the limits and preconditioning, isolates the exact
performance node in a fresh service-free Pytest process and increases the synthetic quality sample
to 1,000 reads: at least 51 observations over 250 ms fail p95 and at least 11 over 500 ms fail
p99. Four correlated transients are only 0.4%; no sample is discarded and reports add sanitized
tail counts. Coverage and correctness-only targets exclude the wall-clock node. The prior
`3f57a2e` bytes passed local `make check` with 3,931 tests and 246 deselections in 1,235.07 seconds.
On the current D131 bytes, the exact isolated
1,000-read node passes in 2.72 seconds; a direct run records p50/p95/p99/max
9.775/10.761/12.288/19.260 ms, all observations accounted for and zero over both budgets. The 23
non-performance harness tests include tracked-format-v2 rendering compatibility. Combined,
scale/release/evaluation tests pass 42, and `make test-scale-correctness` passes 29 tests with 1
deselection followed by 8 tests with 4 deselections. Final `make check` passes the isolated node
plus 3,933 functional tests with 247 explicit deselections in 943.82 seconds; supply-chain, release
audit, Ruff and mypy pass. Hosted PR run `30826970514` later passed quality, PostgreSQL integration
and supply chain for branch head `e49e5d7d52f9e4005e4d469b24e7b23797a59aeb`, but the executed
evaluation subject was PR merge ref `43b4c21676c7bbffaa7b81c2e98d398692e43e8f`; it predates
Phase 1a and is not exact candidate evidence. The
explicit 5,000-read
concurrency-16 PostgreSQL benchmark remains the authoritative operated gate, and
production/release remain NO-GO.

Initial commit `09c3a2e0f47a7fbadb5297fa6bc4f9aca0d21950` and its 12-commit history pass
the exact secret scan and are published on draft PR #1. Hosted run `30765372081` passed quality,
supply chain and GitGuardian, then failed closed on the backup-evidence ceiling and stale legacy
permission expectation; both are corrected and locally reproduced. Every candidate still requires
its own commit-bound scan and hosted rerun, whose external state is tracked on draft PR #1. No
external provider, cluster, alert/SIEM delivery, immutable retention, external cutover/rollback,
exclusive registry-writer control, protected release, or production operation has been accepted.

Historical hosted M29 findings remain useful development evidence. The first supply-chain run
failed closed before provenance because Trivy created non-ignored `.cache/trivy` after its image
scans. Both workflows now use ignored `.local/trivy-cache`, and static policy rejects any deviation
as `trivy_cache_path_invalid`.

Initial-final-byte run `30560980711` built and scanned the runtime image and generated evidence,
then failed closed during evidence verification. The x86_64 leaf of the pinned Python
multi-architecture base contains virtual `.python-rundeps=20260616.002554/noarch`; the initial
verifier encoded arm64's `20260616.002547/noarch` as universal. D124 replaces that assumption with
the exact closed pair and retains strict checks for every real package and the five fetched
PostgreSQL APK bindings. The run was then cancelled, so its remaining `quality` and
`postgres-integration` jobs are neither passes nor code failures. A corrective hosted result
remains pending.

Hosted run `30495413406` proved the cache correction by generating provenance and uploading all
seven evidence paths, then failed closed on incomplete default `pip-audit` resolution and the old
Debian image findings. The candidate now audits all runtime/build inputs with `--disable-pip` and
uses the exact Python 3.13.14/Alpine 3.24 digest. Its only sdist build uses isolated,
vulnerability-fixed, hash-bound tooling and is reproducible across amd64/arm64; offline BuildKit
mounts prevent retaining build inputs. The replacement pre-commit local image smoke/SBOM/scan now
passes on stable product bytes, but its OCI revision label remains the prior HEAD and the hosted
exact-commit result is pending. Production/release remain NO-GO.

This is not a production or release acceptance. The development branch is versioned on a draft
GitHub PR but has not been reviewed or accepted as a release candidate. The
first clean hosted run exposed missing DataHub/MCP bootstrap dependencies, ignored immutable M27
evidence, CI-only ANSI output, and an unseeded synthetic DataHub registry. The next hosted run
proved two additional fixture boundaries: control-PostgreSQL administrative tests must inherit the
same explicit synthetic loopback credential as the container, and the approved registry seed must
use a different authenticated local subject from the replay contract it prepares. A third hosted
run passed quality and every service gate before coverage exposed that a generic job-wide source
DSN crossed component credential boundaries. A fourth run proved the corrected credential scope
and passed the push matrix completely, while the duplicate PR service job alone measured the
unchanged wall-clock p95 budget on a contended runner after every functional gate had passed. The
publication follow-up tracks only the required signed evidence,
aligns bootstrap with the tested extras, uses color-free CI logs, compares CLI help after removing
terminal styling, aligns every explicit test-admin fallback with the non-secret loopback-only
Compose placeholder, scopes the generic source DSN to evaluation, keeps the unchanged short scale
limits in the hosted clean quality job, runs release quality before starting project services,
excludes only the `performance` marker from service-loaded coverage while retaining exact
deterministic limit checks, and explicitly publishes/read-checks the approved synthetic registry
from a dedicated seed principal before live-registry integration.
Operated
Kubernetes, TLS, NetworkPolicy, external
secrets, production replica/traffic scale, provider governance, and later operations/security
gates remain open. M00 through M16 automated development
gates are complete on the operator machine; M17's recorded deployment package and M18's generated
development submission package pass their focused local gates, but their external/operator
acceptance and a clean release commit remain unrecorded.

The publication follow-up's final local bytes pass the 2,719-test CI-shaped quality gate, all 164
integration tests with one retained fixture skip, all 47 acceptance tests, clean-volume registry
read-back, and the targeted PostgreSQL/CLI regressions. Full corrected coverage passes 2,925 tests
with that one skip and seven expected warnings at 81.75%; the final credential-scope and
exact-limit regressions pass inside that gate. On the same commit, the hosted push matrix passes
completely; the duplicate PR service job's sole failure is the contended wall-clock smoke described
above. Replacement hosted checks after marker isolation remain the authoritative publication
evidence.

## Current capability

- Repository scaffold and architecture documents exist.
- The package installs in a clean Python 3.13.13 virtual environment.
- Package import, CLI version, doctor, bootstrap, CI-equivalent quality commands, and starter
  integrity validation are executable.
- POSIX bootstrap selects a supported Python when the default `python3` is too old; the PowerShell
  equivalent is implemented but was not run because PowerShell is unavailable.
- GitHub publication uses branch `agent/ignore-node-modules` and draft PR #1; generated, runtime,
  and secret paths remain ignored, while the 19 content-addressed M27 campaign reports and two
  signed ledger attestations cited by tests and governance documentation are deliberately tracked.
- A pinned PostgreSQL 16.13 container initializes a deterministic 11-table/eight-schema,
  465-row synthetic dataset on loopback port 55433 with tracked data/schema/constraint checksums,
  UTC, locale `C`, and a hardened reader role.
- A typed application health port, psycopg adapter, composition-root binding, and CLI readiness
  command verify the reader identity, read-only defaults, and statement timeout.
- The reference north-star query is isolated under `demo/reference/` and returns exact ground truth.
- Focused immutable domain modules now define physical fields, logical concepts, mappings,
  transformations, decisions, joins, analytical requests, and validation results without I/O or
  external-system imports.
- A closed serialized transformation algebra forbids arbitrary callbacks and expressions; a pure
  identifier interpreter handles padded strings, integers, decimals, and safe integral floats with
  explicit `NULL`, empty-string, and leading-zero policies.
- Typed outcomes distinguish accepted `NULL` from invalid data and expose stable rejection codes;
  the `normalize-demo` CLI presents the complete synthetic boundary set.
- Typed mapping and join YAML fixtures round-trip deterministically and contain no raw SQL or
  executable code.
- A restricted immutable query IR covers declared scans, mapped expressions, filters, approved
  joins, aggregates, date grains, grouping, ordering, and bounded limits without a raw SQL node.
- The SQLGlot PostgreSQL adapter constructs deterministic parameterized SQL; a separate reparsing
  guard enforces statement, allowlist, join, table-count, placeholder, function, and limit policy.
- A psycopg preview adapter independently opens read-only transactions, applies transaction-local
  timeouts, caps fetched rows, and reports its observed reader identity.
- The approved north-star plan returns exact ground truth through the guarded preview CLI, and the
  malicious guard demo rejects three representative attacks without execution.
- The official DataHub Core `v1.6.0` quickstart is checksum-pinned, loopback-only, authenticated,
  and reproducible through start, health, restart, scoped reset, and stop targets.
- DataHub CLI/PostgreSQL connector `1.6.0.15` runs in isolated CPython 3.11 tooling and ingests all
  11 synthetic tables across eight schemas with descriptions and profiles.
- Logical-model UI support is enabled and verified; M07 has now created the approved synthetic
  Customer logical model and its physical/column links through an approval-gated writer.
- A dedicated one-month DataHub service-account token is held only in ignored mode-0600 state; MCP
  `0.6.0` can search and list schema fields while mutation and document tools are forced off.
- Sanitized catalog expectations support unit checks without committing tokens, raw MCP logs, or
  external/proprietary data.
- Vendor-neutral application ports expose bounded asset search/details, schema fields, governance
  metadata, lineage, query context, and decision-document lookup without MCP or DataHub SDK models
  crossing inward.
- The DataHub MCP adapter maps permissions, availability, missing tools, malformed/partial payloads,
  and offset pagination into typed application errors/results; logs contain only operation names
  and URNs.
- Contract-compatible fake and explicitly labeled recorded adapters support tests and offline
  demos; the sanitized catalog recording contains all 11 synthetic datasets and no fabricated
  lineage/query/document claims.
- `catalog-inspect` reads live DataHub without fallback or uses the recording only when explicitly
  selected; missing lineage/query context and disabled document tools remain visible evidence states.
- A bounded semantic candidate use case derives its catalog query from the logical concept, caps
  asset/field paging, blocks candidates by deterministic metadata, and never performs unrestricted
  all-pairs matching.
- Seven normalized deterministic signals produce typed score breakdowns, evidence, missing evidence,
  risks, and closed transformation suggestions; confidence can only prioritize human review and
  every candidate remains `needs_review`.
- Recorded synthetic evidence ranks the CRM string, legacy integer, and bank float customer keys
  first; the float candidate requires explicit finite and integral validation without truncation.
- A labeled synthetic evaluation fixture reports precision, recall, top-k recall, false positives,
  and false negatives without presenting fixture metrics as production evidence.
- Optional description interpretation is an explanation-only application port with a deterministic
  fake; it cannot alter ranking or status, and no LLM runtime dependency was added.
- Explicit review use cases edit, approve, reject, and mark mappings as a different concept while
  optimistic revisions reject stale updates and edits return affected approvals to review.
- An in-memory adapter and ignored local SQLite adapter persist versioned drafts, append-only
  semantic decisions, and publication attempts without connecting to any source database.
- A fingerprinted canonical publication contains only approved mappings; a separate exact
  `PublicationApproval` is required by both the application port and the concrete DataHub writer.
- A dedicated bounded DataHub writer publishes the synthetic Customer logical model, field
  definitions, glossary terms, structured decision property, three physical/column links, and a
  versioned decision document; the complete publication marker is written last.
- Partial DataHub failures are typed per item and remain retryable without a current marker; live
  read-back verifies actual schema, terms, dataset/field links, document, fingerprint, and four
  decision references before idempotent replay returns `already_current`.
- The Codex DataHub MCP remains mutation-disabled. The writer token stays only in ignored mode-0600
  local state and logs operations/URNs without tokens or unrestricted samples.
- Relationship discovery accepts at most two explicit north-star proposals and combines catalog
  descriptions, explicit lineage/query-context absence, declared constraints, normalized overlap,
  null/invalid quality, uniqueness, multiplicity, and row estimates without all-pairs matching.
- A read-only PostgreSQL evidence adapter allowlists the exact physical keys and closed
  transformations, returns aggregate counts only, and independently verifies reader identity,
  transaction mode, and timeout.
- Pure cardinality logic classifies Customer to AccountHolder as one-to-many from the duplicate
  normalized key and AccountHolder to Account as many-to-one from its declared foreign key plus
  consistent data evidence.
- Join candidates carry eight-signal score breakdowns, evidence, missing evidence, risks, and a
  visible Customer-123 fanout warning; confidence cannot approve, poor overlap is not recommended,
  and many-to-many execution fails closed.
- Local SQLite stores join review revisions and immutable decisions. Approved executable contracts
  include physical/logical normalized keys, closed transformation plans, join type, cardinality,
  version, evidence, risks, fanout policy, and their approval decision ID.
- A separate exact fingerprint approval gates DataHub join publication. Versioned and current
  decision documents link the three physical assets; a fresh process loads and validates the
  current contract set without relying on local draft state.
- A pure approved logical-request context defines synthetic models, canonical field types/roles,
  and logical join summaries without carrying physical dataset or column identities.
- The LLM-free guided builder converts primitive selections into an immutable
  `AnalyticalRequest`, exposes only approved logical choices and closed enums, and rejects unknown
  concepts, incompatible types/aggregates, unsupported grains/operators, missing or ambiguous
  paths, three-model overrun, and absent one-to-many fanout mitigation before planning.
- The exact north-star request requires Customer plus AccountHolder and cites only
  `customer_to_account_holder`; the active-customers-by-country control requires only Customer and
  no join. Both round-trip deterministically against the checked-in ground truth.
- A fake planner accepts only `ValidatedAnalyticalRequest` and explicitly performs no physical
  resolution or SQL execution. Ignored local SQLite drafts are parameterized, revisioned,
  idempotent for unchanged content, and revalidated against current approved context on reload.
- `request-demo` lists the explicit recorded context/closed choices, emits an entrypoint-independent
  confirmation view, supports actionable invalid overrides, and saves/reloads typed drafts without
  Streamlit or an LLM SDK.
- A pure M10 resolver fingerprints the validated logical context and selects only current approved
  mapping/contract versions, declared physical types, closed transformations, connected per-model
  datasets, and a unique shortest approved join path within the three-table limit.
- Typed resolution errors fail closed for stale, missing, unapproved, type-incompatible,
  disconnected, ambiguous, and over-limit context before compilation or execution.
- Fanout analysis orients every approved contract to the actual scan path. Forward Customer
  one-to-many entity counts use the exact approved `count_distinct` mitigation; reverse traversal
  is many-to-one and preserves the explicit AccountHolder relationship count. Reversing an
  unmitigated many-to-one contract becomes a one-to-many expansion and fails closed.
- Application use cases coordinate planning, deterministic compilation, independent SQL guarding,
  bounded preview, and rejected-source reporting entirely through ports. Concrete SQL and
  PostgreSQL adapters remain outside application/domain modules.
- The preview adapter now verifies the exact configured reader, read-only transaction, and applied
  timeout before executing. A separate exact-allowlist source reporter enforces the same controls
  and returns only typed invalid join-key records.
- `governed-demo` exposes the complete resolved plan before optional execution. The north-star
  returns exact `2, 1, 1` ground truth plus `127.5`, `NaN`, and `NULL` rejections; the Customer-only
  control uses no relationship, and the explicit holder-relationship count returns `3, 1, 1`.
- Parameterized normalized dimensions use their validated projection ordinal for grouping and
  ordering, preventing PostgreSQL from treating repeated `CASE` bind parameters as different
  expressions; the live holder-role regression returns `PRIMARY=1` and `SECONDARY=8`.
- A vendor-neutral `IntentParserPort` accepts only untrusted text, current language, and a bounded
  four-field approved logical vocabulary. The structured domain output has no SQL, physical asset,
  tool, approval, or execution field and forbids extras.
- The deterministic intent adapter resolves the Spanish north-star request without an API key and
  exposes explicit alternatives for customer-distinct versus holder-relationship count,
  count-versus-list, date meaning, and unknown role values rather than silently guessing.
- Application validation independently rejects hallucinated models/fields, unsupported enums,
  SQL-like filter values, out-of-vocabulary roles, language mismatch, instruction-like control
  text, stale confirmation fingerprints, unavailable choices, and unresolved ambiguity before
  semantic planning.
- The optional OpenAI Responses adapter uses structured output with `store=false` and no tools. SDK
  2.46.0 and the adapter contract were tested with an in-memory client; no live provider call was
  made and live failures never fall back to the fake.
- `intent-demo` previews before confirmation and never compiles or executes SQL. After the exact
  count-distinct confirmation it returns only the typed request plus request/plan fingerprints;
  acceptance proves these equal guided mode through the unchanged pure M10 planner.
- The LLM optional runtime extra is installed and contract-tested; Streamlit remains unvalidated.
- An explicit immutable M12 state machine covers context retrieval, intent resolution, typed
  decisions, semantic resolution, plan ready, independent SQL validation, execution, publication
  proposal/completion, retry, failure, and terminal states without a background scheduler.
- Human checkpoints are typed and bound to exact interpretation, plan, publication, or failure
  fingerprints. A plain resume is inert; it cannot approve, retry, query, or publish anything.
- Local SQLite persists bounded typed workflow state with optimistic revisions. A new process
  restores the same approved logical/planning versions, preview summary, decisions, checkpoint,
  and trace without persisting compiled SQL, parameters, credentials, prompts, or private reasoning.
- Every external action emits sanitized start/result/error events with operation, URN/fingerprint
  references, counts, stable codes, timing, and safety facts. Trace fact keys explicitly exclude
  prompt, token, password, secret, reasoning, and chain-of-thought surfaces.
- Execution from a resumed approval deterministically regenerates the exact stored plan, reruns the
  independent SQL guard, and persists preview completion before rejection inspection. Retrying a
  later read does not repeat the preview; retrying a preview requires an explicit bound decision.
- Optional M12 context publication is a separate port requiring exact typed approval. The live
  DataHub adapter writes only fingerprints/approval identity under the bounded workflow-document
  prefix and reads it back; replay returns `already_current`. Saved query recipes remain M13.
- `workflow-demo` starts, shows, confirms, approves/declines, retries, publishes/skips, and resumes
  durable state. Recorded/fake defaults and live catalog/publication choices are labeled explicitly.
- A composition-root-built Streamlit application now presents Overview, Semantic Models,
  Relationships, Query Studio, and Decisions without constructing adapters or moving business
  rules into page callbacks or session state.
- Immutable application view models expose integration health, interpretations, evidence,
  confidence, risks, transformations, approvals, join cardinality, fanout policy, SQL checks,
  results, rejections, decisions, and sanitized traces. Execution availability is derived only
  from typed workflow checkpoints and approvals.
- The deterministic three-action judge path loads the synthetic scenario, confirms the explicit
  distinct-customer interpretation, and approves a freshly guarded read-only preview. Missing
  approvals never expose the execute action.
- Result views always show selected assets, approved join path, `COUNT DISTINCT` mitigation,
  validation status, reader identity, limits, limitations, and downloadable rejection/validation
  reports. Recorded/fake/live modes remain visibly labeled and never fall back silently.
- Typed safe states cover invalid requests, catalog/source unavailability, unapproved joins,
  timeout, publication failure, retry, loading, and empty views without browser stack traces,
  credentials, prompts, or private reasoning.
- The clean in-app browser path completed at 1440 × 1000 and 1024 × 900 with no console errors or
  horizontal document overflow. Synthetic screenshots and the remaining operator failure drills
  are recorded under `docs/screenshots/m14/` and `docs/14_BROWSER_ACCEPTANCE.md`.
- M16 adds a strict clean-room runner that refuses a missing or dirty release `HEAD`, recreates the
  Python environment and both synthetic service stacks, executes every quality/integration/
  acceptance/evaluation/UI/persistence gate, and scans candidate sources, licenses, links, and
  secret/runtime paths.
- A separate development-only flag permits evidence gathering before the initial commit while
  emitting explicit warnings; it never converts that evidence into a release-commit claim.
- Rejected-source reporting now enforces bounded materialization with an exact total/truncation
  contract, and negative identifiers fail consistently across the compiler, join evidence, and
  rejection adapters. Preview rows/timeouts are capped independently by the concrete adapter.
- DataHub catalog reads reject cross-environment URNs; health selects the exact GMS container;
  admin/writer provisioning waits through the pinned authorization-cache window; and canonical,
  relationship, and recipe writers independently ensure their governed decision property.
- Canonical, join, workflow, and recipe publication now return one common immutable record per
  target with approval, actor/time, verified old/new payload-fingerprint evidence, operation,
  decisions, outcome, and stable reason. Null old state explicitly means no target fingerprint was
  verifiable. Application use cases validate the exact approval binding before atomically appending
  the attempt to a shared SQLite ledger that survives a new process.
- The ledger prevents one approval ID from being reused for another family, actor, timestamp, or
  payload. Join partial retries read each document target independently, so an already-written
  version document is not falsely reported as a new write.
- The release audit's whole-tree AST check verifies inward dependency direction, while candidate,
  disclosure, license, local/external link, generated-artifact, and secret scans retain their exact
  counts and findings in a tracked report.
- M17 selects a free Hugging Face Docker Space for the public fallback after comparing official
  CPU/memory/disk, sleep, persistence, public-access, and secret behavior with Streamlit Community
  Cloud and Render. Full DataHub remains the local multi-service integration path.
- A pinned Python 3.13.13 image runs Streamlit as unprivileged user `user`, reports the supplied
  release ref, exposes platform/adapter health, and needs no database, DataHub, or LLM secret in its
  hosted recorded configuration.
- The recorded execution adapter accepts only the exact independently guarded north-star query and
  exact governed rejection checks. It returns the versioned synthetic `2, 1, 1` result and three
  rejection observations; any fingerprint drift fails closed instead of replaying another answer.
- **Reset demo** starts a fresh synthetic workflow without a source or DataHub mutation. A
  secret-free smoke script checks the Streamlit root and health endpoint; clean-commit Space
  packaging, sleep/cold-start, troubleshooting, rollback, and full local DataHub paths are
  documented.
- M18 adds one deterministic generator for the final logical model, mapping, join contract,
  analytical request, resolved plan, SQL, validation, rejection, write-back contract, evaluation,
  and checksum manifest artifacts. Strict mode requires an existing clean commit; development mode
  cannot produce a release-ready label.
- The generated SQL is output-only: it comes from the typed plan and current deterministic compiler
  after an independent guard, while the recorded result is accepted only for its exact fingerprint.
  DataHub artifact generation uses an explicit contract-compatible fake and names the separate live
  integration command; it performs no catalog mutation.
- The judge-first README, English Devpost draft, current official-rule summary, timed 2:55 video
  script, SRT captions, rights/privacy checks, and final sign-off checklist all preserve the live
  versus recorded/fake boundary and the small-fixture limitations.
- GitHub reports the repository public with Apache-2.0 detected, and a signed-out HTTP request
  returned 200 on 2026-07-22. No public application/video URL or release tag is claimed.
- An approval-gated publisher now writes one exact immutable, workspace-hashed DataHub semantic
  registry version and verifies its complete post-write read-back. The publication approval is
  durably reserved before publisher I/O and its stable identity is bound to actor, scope, version,
  and payload fingerprint; an incompatible retry fails before DataHub.
- A separate bounded, read-only live adapter reconstructs `synthetic_enterprise` v1 from its exact
  DataHub URN with no search or recorded fallback. It verifies the 7 models, 31 mappings, 5 joins,
  37 decisions, 7 related assets, publication approval/audit, scope, and canonical fingerprint
  `ef480eb7370924ff4c94131a2c6c4063d85652cc83aaec9059538cdc2cf9c1b9`.
- Configuration, bootstrap, CLI, planning, workflow, execution, evaluation, and UI support explicit
  recorded/live registry modes. Managed profiles fail closed on unsafe combinations; the live
  reader exposes no write method and requires absence of exact-target mutation privileges.
- The deprecated three-file M10 adapter remains only as an exact historical read shim at its fixed
  legacy scope. New composition uses the atomic governed-registry port, and no live failure can
  invoke the shim.
- A separate PostgreSQL 16 control database now owns the managed registry pointer and all managed
  workflow/access/request/review/publication state. Forward-only migration version 1 is
  checksum-pinned at
  `65d9447619165a92e9b8d8c6541a76569564931b6dc9c7ccbd73deb0f3fd09cc`;
  runtime, reconciler, and migrator use distinct capabilities, startup only checks the schema, and
  database-observed identity prevents aliasing the source endpoint.
- Registry activation and rollback are exact compare-and-swap transitions. Pointer, immutable
  transition, HMAC-chained audit event, and pending projection outbox commit atomically. DataHub's
  active document is a read-back-verified repairable projection; managed planning reads only the
  PostgreSQL pointer and its exact immutable DataHub version.
- Read-only reconciliation classifies missing/behind/ahead/conflicting/corrupt/audit-gap/pending
  states and mutates only a fingerprint-bound, explicitly approved safe repair. Superseded
  generations cannot overwrite a newer projection.
- Durable workflow results contain only row count and preview fingerprint. Actual preview rows are
  transient and tied to the exact authenticated principal, workspace, workflow revision, registry
  generation, pointer, count, and fingerprint; every mismatch clears them.
- Offline legacy adoption is exact-schema, dry-run-first, fingerprint-bound, idempotent, and
  quarantine-preserving. The operated fixture imported two safe records, quarantined four distinct
  invalid/orphan/ambiguous cases, stripped three preview rows, and left its SQLite bytes unchanged.
- Verified dual-key identity rotation persists only opaque same-lineage bindings, never raw OIDC
  evidence or rewritten workflow bytes. Completion/replay resolves the immutable approved plan by
  exact fingerprint and verified derivations, so a replay does not re-prepare against the now
  historical source workspace.
- Stale recipes require a fresh current-registry workflow, independently guarded read-only
  execution, and a new publication. The live operated migration created version 44 while version
  43 retained its exact historical fingerprint.
- Backup uses one exported snapshot, custom-format archive, state digest, owner-only permissions,
  and a signed manifest. Restore requires a configured distinct fresh target and verifies schema,
  audit, pointer/history/outbox, quarantine, and identity state before any external cutover.
- M24 now has pure background-job authorization/lifecycle contracts for one closed
  `execute_workflow_preview` command, including cancellation, bounded retry/dead-letter,
  database-time leases, monotonic fencing, terminal immutability, and summary-only success.
- A bounded FastAPI surface exposes liveness/readiness plus authenticated submit, inspect, and
  cancel operations. It verifies signed OIDC bearer JWT/JWKS material before strict claim mapping;
  a constant-time fixed bearer is development-only. Actor/workspace are server-derived and safe
  problem responses do not disclose protected resources.
- The outer API boundary buffers responses, catches unexpected exceptions before
  Starlette/Uvicorn, logs only request ID plus error type, and returns bounded `500` problems
  without re-raising. Uvicorn access logging is disabled.
- PostgreSQL migration `0002` adds constrained execution jobs and append-only events; final M24
  schema v3 adds `0003_reject_expired_job_success.sql` as a database guard against success after
  authorization expiry.
  `schemabridge_api` and `schemabridge_worker` are separate least-privilege roles; atomic
  idempotency, `SKIP LOCKED` claiming, lease reclaim/fencing, cancellation, retry, and dead-letter
  behavior are implemented behind the application port.
- A separate periodic heartbeat supervisor renews fenced ownership throughout synchronous
  governed execution, joins before transition, and performs a final heartbeat. Losing lease
  ownership fails closed. Schema v3 independently rejects success after authorization expiry;
  fresh unit, PostgreSQL, process, socket, and internal-browser evidence passed.
- API and worker are independent entrypoints. API composition has no source, registry, LLM,
  DataHub, migrator, reconciler, or restore adapter. Worker composition has no bearer/browser,
  LLM, publication/writer, migrator, reconciler, or restore capability and executes only through
  the current typed planner/compiler/guard/read-only source path.
- Job authorization retains the current submitter and immutable original workflow workspace/owner
  scope. The API and worker resolve only verified opaque identity lineage; managed workers use
  `verified-oidc` without receiving OIDC claims, tokens, metadata, JWKS, or pseudonymization keys.
  Worker validation binds the exact historical workspace+submitter pair across the complete
  verified linear chain for owner and workspace-wide `platform_admin` grants; invalid lineage
  fails before protected I/O.
- M24 durable/API state contains no raw idempotency key, bearer, claim, lease capability, DSN, SQL,
  parameter, prompt, source value, or preview row. Successful inspection returns only status,
  attempts/timestamps, workflow revision/stage, row count, preview fingerprint, exact rejected
  total, bounded rejection-code counts, exact unclassified residual, and explicit
  completeness/truncation.
- Runtime migrations are packaged in the wheel and resolve outside the checkout. A non-root
  UID-10001 runtime image and separate hardened API/worker Kubernetes reference manifests exist.
- M24 uses no OpenAI request and neither component needs `OPENAI_API_KEY`. Fresh automated,
  PostgreSQL, process, real-socket, secret-scan, wheel, container, and internal-browser evidence
  passes under schema v3; M24 is accepted locally.
- M25 adds separate workspace/connection-qualified catalog identities, durable tenant policy,
  public connection metadata, opaque private routes, refresh generations, versioned assets/fields,
  tombstones, and active-generation pointers in additive control schema v4.
- Migration `0004_dynamic_catalog_inventory.sql` has SHA-256
  `45c7d95e56a336f267cbe29ad390f6fd54a59d826ffd33253d290be0257060bb`. The sixth
  `schemabridge_catalog` role owns only the bounded indexer capabilities; API, worker, web,
  reconciler, and migrator retain separate contracts.
- The authenticated API registers/disables public connection metadata, requests/reads refreshes,
  and serves tenant-scoped connection/asset/field pages. It has no DataHub/source credential and
  never performs catalog discovery in the request.
- The independent catalog indexer claims with database time, transient capability digest and
  monotonic fence; commits each bounded source page with its checkpoint; and promotes only a
  complete, quota/count/fingerprint/base-generation-verified generation. A crash leaves the prior
  active generation readable.
- Asset and field pages use compound keysets with `LIMIT page_size + 1`; signed cursors are bound
  to workspace, scope, filter, generation, and final key, limited to 1,024 bytes, and expire after
  15 minutes. Interactive reads return at most 50 items.
- DataHub discovery is a mutation-free, bounded `scrollAcrossEntities` full reconciliation in
  stable URN order. The exact synthetic dataset scope is selected by URN prefix rather than
  dataset-name prefix. A lazy synthetic source exercises full and genuine delta pages without a
  full Python inventory.
- API, worker, and catalog indexer each receive one bounded lifecycle-managed PostgreSQL pool.
  Durable PostgreSQL policy enforces distributed request admission, transactional tenant job
  capacity, and workspace-rotating fair claim.
- A version-checked, exactly confirmed platform-admin command applies each workspace capacity
  policy through a migrator-only database function. Every accepted actor/value/version set is
  retained in an immutable revision ledger; missing policy still fails closed.
- The operated large fixture contains 5,434 assets and 41,028 fields across two active
  connections; the small fixture contains 10 assets/75 fields. Every 997th large asset has 64
  fields with nested paths, Unicode names, heterogeneous types, definitions, null/key variation,
  and tags. Page sizes 1, 17, and 50 traverse both exact inventories without duplicate, omission,
  order drift, deep offset, or more than `page_size + 1` rows.
- Field full-text search includes definitions, native types, tags, and glossary terms in a stored
  GIN-indexed document. Search remains candidate evidence and cannot approve a mapping or query.
- The reference Kubernetes manifest now contains a two-replica hardened catalog-indexer
  Deployment with its own DSN/read token, pod-derived lease identity, schema-only readiness,
  process-only liveness, non-root execution, dropped capabilities, and a read-only root
  filesystem. It has not been operated in a cluster.
- The local M25 scale report passes on Darwin 25.5.0 arm64, CPython 3.13.13 and PostgreSQL 16.13:
  the 110-page large refresh took 29.685331 seconds; heap/RSS deltas were 126,601/0 bytes; and
  5,000 indexed reads at concurrency 16 had zero errors with p95/p99 41.762/54.951 ms. The asset
  and 64-field plans used their expected indexes under the default PostgreSQL planner without an
  override. These are local regression budgets, not production SLOs.
- Final M25 gates pass: `make check` 1,412 tests with Ruff/mypy clean; full
  integration 111, acceptance 19, API 18, worker 24; scale correctness 20 plus the 8-pass
  PostgreSQL cut and synthetic PASS report; deterministic 11-table/465-row evaluation; packaged
  migrations 1–4; release audit over 555 candidate files/23 direct licenses; six-role schema-v4
  readiness/separation; complete internal-browser desktop/mobile acceptance; and clean coverage
  with 1,538 passed, seven warnings, no skips/failures/deselections, and 81.98% versus 80%.
- Catalog cardinality remains independent from query safety. The planner/compiler/guard still
  permit one governed connection and at most three physical tables/two joins, and reject
  cross-connection, fourth-table, third-join, Cartesian, DDL/DML, unsafe-fanout, row-limit, and
  timeout bypasses.
- M25 makes no OpenAI request. Names, definitions, types, tags, and terms are indexed evidence, not
  semantic equivalence. M27 now provides bounded description matching with explicit ambiguity.
- Accepted M26 adds immutable exact mapping-to-catalog bindings, observed
  metadata/join baselines, closed deterministic drift findings, impact summaries, exact decision
  approvals, and dependency-scoped gate assessments without any LLM or raw-SQL path. Final-audit
  corrections now use locator-first evidence, conservative stale-artifact projection, exact live
  dependency state, resulting-head baseline revision, and workspace/connection profile routing.
- Additive control schema v5 introduces immutable generation changes, resource bindings,
  aggregate join profiles, reports/findings/impacts/resolutions, dependency index state, semantic
  heads, scoped scan requests, and workspace/connection-qualified profile jobs. Final migration
  SHA-256 is `8e311f27c881844a1c67a0281391ddc1eb7b863f1d2f8edff382933397412285`;
  current-byte schema and complete six-role gates pass.
- Catalog-generation promotion fans out one idempotent scan per matching active
  workspace/catalog/registry pointer; registry transitions enqueue their own exact scan. Requests
  use database-time leases, capability digests, monotonic fences, retry/supersession, and immutable
  terminal evidence.
- Workflow dependencies are read through PostgreSQL keyset pages and current query recipes through
  DataHub stable-URN pages of at most 50. Reconciliation checks scan continuation around every
  page; lease loss prevents the dependency watermark from advancing. One atomic snapshot is capped
  at 10,000 artifacts/100,000 edges and persists in batches of 500. Same-scope stale artifacts map
  conservatively to exact current logical fields/contracts; cross-scope or unresolvable artifacts
  make coverage incomplete.
- Active DataHub query-recipe current/version identities now include the semantic scope fingerprint
  prefix as well as intent. Active publication, lookup, and dependency inventory validate the
  complete scope so identical intent in another tenant cannot collide with the current marker.
- Aggregate join profiles are durable jobs carrying exact workspace and `connection_id`. The
  reconciler enqueues the complete required batch before waiting; a profile worker claims/reclaims
  only its configured pair and validates it again before heartbeat and aggregate-only read-only
  source I/O. Cross-workspace/cross-connection jobs and cross-connection endpoints fail closed.
- The schema-v5 gate projection compares exact live M25 field evidence, current approved head,
  latest affected report/dependency completeness, and latest join safety profile. Runtime and
  worker require one row per selected mapping/join and one common connection before compilation,
  preview, rejected-source inspection, or source I/O; unrelated drift does not block an unaffected
  plan.
- M26 provides a separately operated semantic reconciler, aggregate profile worker, exact
  inspect/prepare/approve/commit/head/audit CLI, tenant-scoped read-only report HTTP pages, and a
  loopback acceptance panel. The final real PostgreSQL/DataHub lifecycle and real-upstream browser
  record pass locally; package, audit, coverage, current-byte schema, checksum, quality, and diff
  evidence also pass. M26 is complete and accepted locally; this is not a production/release GO.
- M27 replaces the domain-specific query-entry surface with registry-driven guided controls,
  bounded Spanish/English short-description matching, signed previews, edit/re-sign, exact
  confirmation, and a separate non-executable physical-discovery lane. A governed match remains
  evidence until the complete typed request is confirmed.
- Query Studio cardinality is runtime data. The same composition serves the retained
  10-asset/75-field and 5,434-asset/41,028-field profiles while keeping only 31 approved mappings
  executable. One request still uses one governed connection, at most three tables and two joins.
- Local analytical expansion is deterministic and provider-free. The optional live provider sees
  only the bounded typed interpretation surface and can select only server-numbered option `1`;
  source grounding, candidate identity, values, operations, joins, ordering, limits, SQL, and
  approval remain server-owned.
- Signed campaign `m27-cheapest-first-campaign-v11` selected
  `gpt-5-nano-2025-08-07` after 16 interpretation attempts. It passed all 136 planned synthetic
  cases with top-1 `56/62`, top-3/recall@20 `62/62`, MRR `0.946237`, no-match `31/31`,
  ambiguity `18/18`, exact core `15/15`, and adversarial `10/10`; observed cost was EUR
  `0.001394085`.
- The content-addressed schema-v2 attestation proves the signed campaign and policy-v83 durable
  ledger have one unique chronological 16-attempt matching, exact aggregate tokens, and no
  missing/orphan/extra/expansion/open/stale attempt in that historical window. It deliberately
  claims authenticated ordinal correlation, not a native content-derived case/request identity.
- The fake/runtime internal-browser matrix passes at desktop and 390x844 over both cardinality
  profiles, including governed pagination, short description, ambiguity/no-match/stale/rate/
  quota/provider-down states, `needs_mapping_review`, deterministic SQL/AST/read-only preview,
  hidden values, safe hostile text, clean console, no overflow, and protected-data scans.
- The separately authorized live-UI reload was blocked by the internal browser URL policy before
  loading the page or making a submission. No bypass was attempted and the block is not presented
  as provider or product success. External AI was returned to disabled policy v86 immediately;
  the historical schema-v2 attestation still verifies.
- M28 keeps `QueryPlan` vendor-neutral while adding one immutable
  `GovernedExecutionTarget` to the resolved semantic plan. Its fingerprint binds workspace,
  connection, PostgreSQL connector/dialect, route revision/fingerprint, expected reader, source
  identity, DataHub catalog identity, type contract, and the complete cost budget.
- Compiler output and the independent SQL guard carry the exact same dialect and target
  fingerprint. PostgreSQL is the only executable M28 dialect; catalog-only and unsupported
  dialects fail before compiler, route, cost, preview, or rejected-source I/O. Federation remains
  out of scope.
- The accepted schema-v9 slice adds immutable connector contracts, route revisions, separate private
  capability bindings, route heads/audits, and target-bearing execution/profile work. The
  platform-admin CLI implements read-only inspect/fingerprint/prepare, a separate exact approval,
  and compare-and-swap apply for create/rotate/disable.
- Public routes contain no opaque binding. Private `preflight`, `catalog`, `execution`, and
  `profile` references are loaded only through capability-specific lease/fence/expiry functions;
  local PostgreSQL and DataHub resolvers use absolute owner-only directories, SHA-256 filenames,
  mode-0600 strict documents, no-follow reads, and sanitized failures.
- Catalog refreshes/generations now bind an immutable source-identity, catalog-identity, and
  type-contract triple. Staging and delta work preserve the exact triple, legacy null generations
  are non-executable, identity-changing rotation requires a fresh promoted generation, and a
  secret-only rotation still invalidates old target-bearing plans.
- The only supported type identity is the reviewed PostgreSQL native-type contract v1 and its
  canonical fingerprint. Domain construction, operator prepare/write, schema checks, route
  loaders, job admission, and catalog activation reject any invented version or SHA.
- Governed cost admission uses the independently validated SQL and exact parameters in a bounded
  read-only PostgreSQL
  `EXPLAIN (FORMAT JSON, COSTS TRUE, ANALYZE FALSE, BUFFERS FALSE, VERBOSE FALSE, SETTINGS FALSE)`
  transaction. Only sanitized scalar evidence persists; the semantic gate, route, and preflight
  repeat immediately before preview, and any failure produces zero preview.
- Execution, catalog, and aggregate-profile workers resolve routes dynamically by workspace and
  connection rather than one global source/DataHub binding or one statically selected tenant.
  Inventory cardinality remains data: the 10/75 and 5,434/41,028 profiles exercise the same
  bounded path, while one query remains one connection, three tables, and two joins.
- The dedicated M28 browser helper prepares two real temporary PostgreSQL sources with identical
  public labels and distinct roles/results, exercises compiler/guard/routing/preflight/preview,
  then removes sources and secrets before serving sanitized evidence through the product renderer.
  All nine scenarios passed at desktop 1280x720 and mobile 390x844 with distinct tenant results,
  clean consoles, no overflow/XSS/protected-data hits, and exact state/database/role/port cleanup.

## M29 final-byte remediation — accepted locally; corrective hosted validation pending

- The provider-neutral connector-secret application port has owner-only local and HTTPS
  Vault/OpenBao-compatible remote implementations. The remote path consumes a short-lived
  audience-bound projected token, authenticates one closed capability role, requests one exact
  immutable provider version, verifies TLS, rejects redirects and malformed/oversized replies,
  retains no cross-operation secret cache, and returns only sanitized failures.
- Remote connector, DataHub-catalog, and semantic-registry credential paths are composed without
  global source/DataHub fallback in managed mode. API, reconciler, observer, and migrator do not
  receive connector-secret authority.
- Control schema v10 adds `schemabridge_observer` and a security-barrier aggregate queue view.
  Schema v11 adds immutable per-capability provider-version pins and version-bearing route loaders
  without inferring historical values. Schema v12 adds `schemabridge_backup`, producing eight
  distinct control credentials: runtime, API, worker, catalog, reconciler, migrator, observer, and
  backup.
- Runtime composition has separate web, API, execution, catalog, profile, reconciler, observer,
  backup, and operator boundaries. The scheduled backup uses its own read-only identity and
  sanitized entrypoint. Its `pg_dump`/`pg_restore` subprocess environment contains only allowlisted
  `PATH` and required `PG*` values, not ambient OpenAI, audit, OIDC, web, DataHub, or
  connector-secret capability.
- Managed `schemabridge-web` preflights typed configuration, authentication, and exact control
  schema before replacing itself with the fixed Streamlit command. Startup/readiness repeat the
  preflight and require an exact bounded 200/`ok` response from the loopback Streamlit health
  endpoint. Managed web disables synchronous execution/publication and renders Operations as
  unavailable without an operated data source; synthetic scenarios exist only in explicit
  development/hosted-demo profiles.
- Long-running service telemetry uses schema-versioned bounded structured events and a closed
  low-cardinality registry. Exactly eight metric families with real runtime/observer producers,
  six alerts, three SLOs, and eight dashboard panels form the active bundle. The Kubernetes
  PrometheusRule mirrors the active alert group exactly. SIEM plus uncomposed
  backup/release/integrity/capacity signals remain schema-validated under
  `deploy/observability/inactive/`; they are not active delivery, paging, or health evidence.
- The M29 Kubernetes reference base renders a closed 63-resource graph: seven long-running
  Deployments, one hourly non-overlapping backup CronJob, one PrometheusRule, nine ServiceAccounts,
  explicit projected identities only for secret readers, restricted non-root workloads,
  resource/topology controls, PDBs, TLS ingress, namespace default deny, capability-specific
  network paths, and six isolated metrics scrape services. The production overlay deliberately
  retains blocking placeholders. The focused local manifest/render contract passes; target-cluster
  admission and network-policy enforcement have not been operated.
- Dependency inputs are represented by `uv.lock` plus exact hashed runtime/build exports.
  Supply-chain validation covers immutable actions/images, direct-license policy,
  vulnerability-report structure, SBOM/artifact binding, provenance subjects, and
  protected-release OIDC. No clean protected release has been built, scanned, attested, signed,
  or promoted from these bytes.
- The final image design fetches exactly five reviewed Alpine APKs for each supported amd64/arm64
  architecture, verifies their fixed SHA-256 values, and installs them from a read-only BuildKit
  mount with `apk --no-network`. Static policy rejects an extra/mutable APK, changed URL/hash,
  writable mount, raw filesystem copy, or online final-stage resolution. Local image
  `sha256:db2a42ce187243c809a9fcc1272246fb914fbeefc9186b7a83b651014d015b79`
  passes runtime, linkage, entrypoint, SBOM, `pip-audit`, and Trivy policy checks. It was built
  before commit and its OCI label names the prior HEAD, so it is not release evidence. Runtime
  evidence accepts only the base image's exact virtual `.python-rundeps` pair:
  `20260616.002547/noarch` for `aarch64` and `20260616.002554/noarch` for `x86_64`.
- Recovery policy and tooling enforce the hourly/RPO/RTO floor, verify signed archive/manifest
  pairs before retention decisions, default to dry-run, reject links/tampering/orphans, and move
  expired verified pairs into recoverable owner-only quarantine only after exact review. Remote
  encrypted object-lock transfer, external cutover, and operated rollback have not been performed.
  The schema-v12 distinct-target backup/restore cut passes 6 tests with 5 deselected in
  5.96 seconds, and the recovery policy fingerprint is
  `24bdecb8bcb8faab8ba83d64eadf201c0b1d31142ebab773b012735f8f6f34ae`.
- Operator commands use an explicit component boundary and clean environment rather than
  inheriting web, OIDC, OpenAI, DataHub, API-auth, connector-secret, or developer `.env`
  capabilities.
- Release promotion is a seven-job prepublication state machine:
  `audit → prepare → candidate → scan → attest → promote → release`. The protected GET-only audit
  runs without checkout, repository code, or third-party actions; it reverifies an exact
  historical immutable publication as a no-op or admits one fresh current-`main` build after
  branch/ruleset, CI, immutable-Release, monotonic publication, and reference-absence checks.
  Candidate resumption, scan, attestations, same-digest promotion, and the exact ten-asset Release
  are independently reverified. Five approvals and post-readback are bounded to seven calendar
  days; 35-day retention is only an incident-analysis buffer.
- Current focused, schema/service, release/supply-chain/static, integration, acceptance,
  deterministic evaluation, runtime-wheel, local-image, recovery, scale, final browser,
  current-byte coverage, and `make check` evidence is recorded in `tasks/M29_HANDOFF.md`. Initial
  commit `09c3a2e0f47a7fbadb5297fa6bc4f9aca0d21950`, its exact revision scan, and draft-PR
  publication pass. Hosted run `30560980711` exposed the D124 platform-virtual-package correction;
  the corrective exact revision and hosted rerun remain pending. Coverage is 81.17% and supersedes
  D115's historical 81.09%; D123 records final local acceptance.
  Production and release remain **NO-GO**; M30/M31 and external provider, cluster, operations, recovery,
  exclusive Release/GHCR writers, immutable Releases, security, and operator acceptance remain
  blocked.

## Test evidence

On macOS 26.5.1 arm64 with CPython 3.13.13:

- `python -m pip install -e ".[dev]"`: passed in a clean `.venv`;
- Ruff format check and lint: passed;
- strict `mypy src`: passed;
- unit selection: 5 passed;
- `schemabridge version`: `0.1.0`;
- `schemabridge doctor`: all required checks passed;
- `python scripts/validate_starter.py`: passed;
- `make check`: passed before M00 cleanup and twice consecutively after cleanup.

M01 PostgreSQL evidence on the same machine:

- `make demo-reset`: passed twice from a deleted Compose volume after hardening;
- Compose status: PostgreSQL 16.13 healthy on `127.0.0.1:55433`;
- `make demo-health`: passed with the expected reader and 5000 ms timeout;
- `make demo-query`: returned the exact 2, 1, 1 rows;
- `pytest -m integration -k postgres`: 14 passed after each clean reset;
- `make check`: passed with 8 unit tests and 20 strictly typed source files;
- reader write/DDL tests: insert, update, delete, create, temporary create, and alter rejected.

M02 pure-domain evidence in the existing clean development environment:

- focused domain/normalization selection: 58 passed, 8 deselected;
- domain/test Ruff check and strict domain mypy: passed;
- `schemabridge normalize-demo --json`: passed with canonical `123` equivalence and stable rejection
  codes/reasons;
- `make check`: passed with 66 unit tests, 30 strictly typed source files, and 41 formatted files;
- deterministic mapping/join YAML round trips and domain import-boundary checks: passed.

M03 query safety evidence with SQLGlot 29.0.1 and the healthy synthetic PostgreSQL service:

- editable `.[dev,postgres,sql]` installation: passed;
- focused compiler/guard/query-plan selection: 54 passed, 68 deselected;
- query/SQL integration selection: 6 passed; complete integration suite: 19 passed;
- north-star preview: exact 2, 1, 1 rows as `schemabridge_reader`, read-only, at 5000 ms timeout;
- 50 ms cancellation, 60,000 ms timeout reporting, and hard fetched-row-cap paths: passed;
- `make check`: passed with 122 unit tests, 36 strictly typed source files, and 53 formatted files;
- statement smuggling, DDL/DML/utilities and locking reads, destructive/recursive CTEs, unknown
  assets/columns, Cartesian/self/fourth-table queries, joins that ignore the new relation, unsafe
  functions, bad limits, and parameter mismatches: rejected by stable policy codes.

M04 DataHub evidence on the same machine:

- DataHub Core `v1.6.0`, CLI/connector `1.6.0.15`, MCP `0.6.0`, and uv `0.11.30` were verified;
- a complete `datahub` Compose volume reset, pinned restart, admin initialization, ingestion, scoped
  identity provision, catalog check, and MCP check passed;
- ingestion produced 61 aspects with no sink failures and profiles for all five demo tables;
- the catalog check verified four schemas, descriptions, exact fields/row profiles, and the native
  logical-model UI feature flag;
- two persistence restarts passed; the final restart preserved both metadata and the scoped token;
- MCP service-account search and `list_schema_fields` passed while known mutation tools were absent;
- the service token actor matched the dedicated account and all checked administrative/write
  platform privileges were false;
- focused M04 unit tests: 9 passed; `make check` passed with Ruff over 61 files, strict mypy over 36
  source files, and 131 unit tests; the complete 19-test PostgreSQL integration suite also passed.

M05 catalog-read evidence against the same authenticated local DataHub:

- editable `.[dev,datahub]` installation passed with MCP client `1.28.1`;
- focused catalog/DataHub unit selection: 22 passed, 122 deselected;
- the shared live DataHub adapter contract: 1 passed, 163 deselected;
- fake and recorded adapters passed the same north-star, pagination, missing-evidence, and
  unavailable-document contract;
- live `catalog-inspect crm.customers --json` returned the expected asset, four fields, descriptions,
  and explicit missing lineage/query-context states;
- `make check`: Ruff passed over 74 files, strict mypy over 43 source files, and 144 unit tests passed
  with 20 integration/acceptance tests deselected.

M06 semantic-candidate evidence using only the explicit synthetic recording:

- focused candidate/matching/scoring selection: 14 passed, 143 deselected;
- the exact module CLI ranked `crm.customers.customer_id`, `legacy.client_master.client_no`, and
  `bank.account_holders.gf_customer_id` first with all statuses `needs_review`;
- the float representation reported unsafe-float and finite/integral-validation risks;
- fixture evaluation reported precision/recall/F1 `0.750` and visibly listed one intentional false
  positive and one intentional false negative;
- `make check`: Ruff passed over 86 files, strict mypy over 51 source files, and 157 unit tests passed
  with 20 integration/acceptance tests deselected;
- starter integrity validation, `git diff --check`, and an explicit trailing-whitespace scan of all
  M06 files passed.

M07 canonical-review and DataHub write-back evidence on the same local Core stack:

- exact approval/decision/publication unit selection: 6 passed, 162 deselected; the complete M07
  unit modules are also included in the full gate;
- dedicated writer provisioning passed with an ignored mode-0600 token and three bounded policies;
- two superseded tokens from failed provisioning retries were revoked after exact metadata
  resolution; the service account now has exactly one active M07 writer token;
- the first live integration exposed a glossary write failure and a second exposed a schema-field
  link permission mismatch; both returned typed partial results and left the marker absent;
- after correcting only the verified property/read and scoped-policy paths, the same immutable
  publication recovered and the live integration passed;
- the CLI approved all four synthetic mappings, published nine tracked items, read the context back
  from DataHub, and returned `already_current` on an identical replay;
- the live context contains two canonical fields, three dataset plus four column links, two glossary
  terms, one decision document, the structured decision property, and four decision/version refs;
- `make check`: Ruff passed over 101 files, strict mypy passed over 59 source files, and 168 unit
  tests passed with 21 service/acceptance tests deselected;
- the final live integration passed 1 test with 188 deselected; starter validation and diff hygiene
  passed after documentation updates.

M08 relationship and governed-contract evidence on the same PostgreSQL/DataHub services:

- focused join/cardinality/fanout selection passed 24 tests with 157 deselected;
- the PostgreSQL relationship integration returned exact 7/9 and 9/9 row profiles, 5/9 matching
  distinct keys, maximum multiplicities 1/2 and 1/1, and the expected FK direction as the verified
  read-only reader at 5000 ms;
- the three-test join/relationship integration selection passed; the live DataHub path
  published/reused the versioned and current contract documents,
  loaded both contracts through a fresh adapter instance, and returned `already_current` on replay;
- a separate CLI process loaded contract-set version 3, two immutable decision IDs, and the three
  related physical assets from the current DataHub document;
- `make check` passed Ruff over 115 files, strict mypy over 67 source files, and 181 unit tests with
  23 service/acceptance tests deselected; starter and diff hygiene are recorded in the handoff.

M09 guided-request evidence without external services:

- the focused analytical-request/guided selection passed 22 tests with 180 deselected;
- exact north-star and Customer-only ground-truth requests round-tripped deterministically and
  carried no raw SQL or physical identifiers;
- invalid `SUM(customer_key)`, unknown filter fields, unsupported grains, absent/ambiguous paths,
  and one-to-many fanout regressions returned stable actionable findings before planner handoff;
- the exact `request-demo` module command succeeded, while expected malicious/unsupported choices
  exited nonzero; a fresh temporary SQLite draft reloaded revision 1 with the same fingerprint;
- `make check` passed Ruff over 125 files, strict mypy over 74 source files, and 202 unit tests with
  23 service/acceptance tests deselected.

M10 governed-planner evidence against the healthy synthetic PostgreSQL service:

- focused planner/resolution/fanout unit selection passed 13 tests; planner/preview integration
  passed 7 tests; the guided north-star acceptance selection passed 1 test;
- the plan-only CLI emitted the complete labeled context, selected mapping/contract versions,
  assumptions, fanout mitigation, restricted query plan, allowlist policy, parameterized SQL, and
  no database result;
- the executed CLI returned exact north-star `2, 1, 1` rows and the three typed rejected records as
  `schemabridge_reader`, read-only, with a 5000 ms timeout for both bounded reads;
- the no-join control returned `ES=4`, `FR=1`, and `PT=1` from only `crm.customers`; the explicit
  relationship count returned `3, 1, 1` without being changed to a Customer entity count;
- security regressions cover compiler float-range rejection, source-field allowlisting, independent
  guard traversal, application port direction, context approval/version/type/path failures, and
  contract-scoped fanout behavior.
- `make check` passed Ruff over 135 Python files, strict mypy over 80 source files, and 213 unit
  tests with 26 integration/acceptance tests deselected; `git diff --check` passed.

M11 natural-language intent evidence without external services:

- editable `.[dev,llm]` installation passed with OpenAI SDK 2.46.0;
- the focused intent/language/prompt selection passed 19 tests with 212 deselected;
- the natural-language acceptance selection passed 1 test with 257 deselected and proved exact
  guided/natural request and resolved-plan fingerprint equality;
- the Spanish CLI preview returned the expected dimension, distinct-customer metric, SECONDARY
  filter, and two explicit count meanings; exact confirmation remained compilation/execution-free;
- `agrupa clientes` exposed count and unavailable-list alternatives, while an injection-style DROP
  request produced no request, could not be confirmed, registered no tools, and generated no SQL;
- `make check` passed Ruff over 143 Python files, strict mypy over 85 source files, and 231 unit
  tests with 27 integration/acceptance tests deselected.

M12 orchestration evidence on the same local DataHub/PostgreSQL services:

- the focused workflow/orchestrator/trace selection passed 17 tests with 223 deselected;
- SQLite restart, typed checkpoint enforcement, safe trace structure, catalog retry without
  fallback, rejection retry without preview replay, and publication retry/idempotency passed;
- two workflow acceptance paths passed with 267 deselected: recorded catalog plus real PostgreSQL,
  and authenticated live DataHub MCP plus real PostgreSQL and approved DataHub publication;
- both acceptance paths returned exact `2, 1, 1` rows, three stable rejection codes, reader
  `schemabridge_reader`, and read-only execution; the live document read-back and fresh-adapter
  replay returned `already_current` without a duplicate document;
- a six-command CLI run proved start/show restart equivalence, intent and execution checkpoints,
  a second SQL-validation event, exactly one preview, publication proposal, and explicit skip;
- `make check` passed Ruff over 154 Python files, strict mypy over 93 source files, and 240 unit
  tests with 29 integration/acceptance tests deselected.

M13 recipe/context-reuse capability:

- immutable recipes contain the normalized intent, exact approved model/mapping/join versions,
  source-schema/compiler/plan/query fingerprints, validation evidence, limitations, provenance,
  and linked synthetic assets, but no executable SQL;
- explicit approval binds the exact recipe version and fingerprint; persistent fake and DataHub
  repositories return `already_current` for an identical replay and preserve immutable provenance;
- a fresh workflow retrieves the current recipe, compares every compatibility boundary, reports
  typed stale reasons, and still replans, recompiles, independently guards, requests execution
  approval, and runs the read-only preview instead of executing saved SQL;
- the focused recipe/fingerprint/reuse unit selection passed 9 tests, the live DataHub document
  integration passed 1, and the real PostgreSQL restart/reuse acceptance path passed 1;
- `make check` passed Ruff formatting/lint over 163 Python files, strict mypy over 99 source files,
  and 246 unit tests with 31 service/acceptance tests deselected;
- `examples/query-recipe-secondary-holders.yml`, `examples/generated-secondary-holders.sql`, and
  `examples/query-validation-report.md` were generated from the actual synthetic `2, 1, 1` run and
  clearly distinguish inspection output from executable application input.

M14 judge-ready Streamlit evidence on the same synthetic PostgreSQL service:

- editable `.[dev,ui,postgres,sql,datahub,llm]` installation passed with Streamlit 1.59.2 and
  NumPy 2.2.6;
- the focused view-model/UI selection passed 40 tests with 214 deselected;
- the Streamlit acceptance path passed and proved three primary interactions, approval gating,
  exact `2, 1, 1` results, three typed rejected records, and visible safety/context facts;
- the complete acceptance suite passed 6 tests with 280 deselected against the canonical
  `schemabridge_reader` DSN;
- `make check` passed Ruff formatting/lint over 169 Python files, strict mypy over 102 source files,
  and 254 unit tests with 32 integration/acceptance tests deselected;
- POSIX bootstrap syntax and starter integrity validation passed after aligning both bootstrap
  scripts with the optional UI dependency; PowerShell remains unavailable and unexecuted;
- a final headless Streamlit 1.59.2 startup on port 8502 returned `ok` from `/_stcore/health` and
  stopped cleanly; no deprecation warning was emitted;
- the in-app browser verified all five pages, three-action execution gating, exact results and
  rejections, downloads, labels, 1024 px responsive behavior, and an empty browser console.

M15 reproducible evaluation capability and evidence:

- a versioned manifest composes the existing synthetic semantic, query, join, mapping, approved
  context, security, and SQL-free recipe fixtures without accepting manifest-injected paths;
- the `evaluate` use case measures candidate precision/recall/F1/top-k, exact join paths and
  cardinalities, typed-intent equivalence, independently guarded compilation, bounded read-only
  execution, normalized row correctness, source rejections, 38 guard-only attacks, and current/stale
  recipe reuse through injected ports;
- JSON and Markdown reports show every raw numerator, denominator, skip, failure, false positive,
  false negative, and rejected case; the small fixture has no confidence interval or unjustified
  regression threshold;
- deterministic and optional live-LLM runs are structurally separate; the default remains key-free
  and records the live run as `not_run` rather than mixing or fabricating results;
- two independent `make evaluate` clean-volume runs produced byte-identical final artifacts;
  `reports/evaluation.json` remains ignored while `examples/evaluation-report.md` is checked-in
  synthetic evidence labeled as an uncommitted snapshot, not a release-commit claim;
- focused evaluation tests passed 7 with 252 deselected, all 26 integration tests passed, all 7
  acceptance tests passed, and `make check` passed Ruff over 178 files, strict mypy over 109 source
  files, and 259 unit tests.

M16 final uninterrupted development clean-room evidence:

- the complete `.[dev,ui,postgres,sql,datahub,llm]` environment installed into a recreated `.venv`
  on CPython 3.13.13 and `pip check` passed;
- PostgreSQL 16.13 was recreated from its named volume and passed exact reader, read-only, and 5000
  ms timeout checks;
- DataHub Core v1.6.0 was recreated from its three named volumes, initialized after observable
  authorization convergence, ingested 61 events, exposed five described/profiled datasets and the
  logical-model UI, and passed scoped MCP search/schema reads with mutation tools absent;
- `make check` passed Ruff formatting/lint over 182 files, strict mypy over 110 source files, and
  274 unit tests with 33 integration/acceptance tests deselected;
- all 26 integration tests and all 7 acceptance tests passed from the clean service state;
- deterministic evaluation passed with live LLM explicitly `not_run`; headless Streamlit health
  returned `ok`; DataHub restart preserved catalog/MCP/governed documents and 3 focused write-back
  read-backs passed;
- the uninterrupted run's release scanner passed 314 candidate files; after preserving the five
  operator-readable panel reports, the final scan passed 319 candidate files, 19 installed
  direct-dependency licenses, and 20 external links; starter validation, shell syntax, and
  `git diff --check` also passed;
- at the time of that historical clean-room run, strict `make release-audit` and
  `make release-clean` failed closed because `HEAD` did not yet exist.

M16 post-audit fanout and governance remediation evidence:

- reverse traversal of the Customer/AccountHolder contract now preserves `COUNT` and the live
  read-only preview returns 6 instead of the incorrect `COUNT DISTINCT` result 5; an unmitigated
  reverse many-to-one expansion fails closed in both resolution and query-plan validation;
- automatic `COUNT DISTINCT` now applies only to the contract's exact approved one-side key, never
  to an arbitrary attribute; three-table `SUM`/`AVG`/plain `COUNT` after an earlier fanout fail
  closed, while explicit `COUNT DISTINCT`, `MIN`, and `MAX` remain duplication-invariant;
- the independent query IR now rejects a predicate, normalization plan, or scan direction that does
  not exactly match the approved contract, including a reversed `LEFT JOIN`;
- a holder-role grouped preview reproduced and then fixed the PostgreSQL parameter-identity error;
  the live result is `PRIMARY=1`, `SECONDARY=8` through independently guarded SQL;
- the unified audit ledger covers all four publication families, validates exact approval identity,
  survives process restart, rejects approval-ID collision, preserves actual per-target old state,
  and explicitly uses null where an aspect exposes no verifiable fingerprint;
- workflow publication now treats fingerprints alone as insufficient current-state evidence: the
  DataHub target must also contain the exact workflow ID, approval identity/time, and a validated
  successful target-audit record. The domain recomputes workflow proposal hashes and rejects a
  reused idempotency key bound to different content; a pre-existing deterministic target with
  another valid fingerprint fails as an immutable conflict before mutation;
- canonical, join, and recipe immutable targets reject pre-existing content conflicts; join/recipe
  success now requires post-write read-back, including auditable partial failure on a stale/no-op
  acknowledgement;
- current `make check` passes Ruff over 187 files, strict mypy over 113 source files, and 310 unit
  tests; all 29 integration and all 7 acceptance tests pass against live PostgreSQL/DataHub;
- the development release scan passes 324 candidate files, 19 direct licenses, and 20 external
  links; strict
  `make release-audit` checks 20 external links and fails closed only because the remediation tree
  is uncommitted/dirty;
- this remediation remains uncommitted, so strict clean-room release evidence must be regenerated
  after operator review and a focused release-candidate commit.

M17 local deployment-package evidence:

- the pinned multi-platform Python 3.13.13 base image resolved and the final `linux/amd64`
  `make judge-build` completed through Docker Desktop emulation on the Apple-silicon host;
- the container ran as unprivileged `user`, reached Docker health `healthy`, and
  `make judge-smoke` passed against `http://127.0.0.1:7860`;
- a stopped local container restarted and passed smoke in 0.12 seconds; this is not presented as a
  Hugging Face free-tier cold-start measurement;
- the focused UI/recorded adapter selection passed 10 unit tests; `pytest -m acceptance -k deployed`
  passed 2 service-free tests, including explicit live-mode failure without recorded fallback;
- `make check` passed Ruff over 190 Python files, strict mypy over 115 source files, and 313 unit
  tests; all 29 integration and all 9 acceptance tests passed on the unchanged full local path;
- clean-ref Space packaging fails closed against `HEAD` `231187a` because that historical commit
  lacks the required M17 Dockerfile/fixture/adapter; no mixed dirty-tree bundle can be produced;
- the development release audit scanned 334 candidate files and 19 direct dependency licenses with
  no architecture/secret error; its sole finding is the expected dirty-tree release warning;
- no `hf` CLI/token/target Space is available, so public upload, public smoke, incognito/separate-
  network use, free-tier sleep/wake timing, and runtime-setting inspection remain unperformed.

M18 development submission-package evidence:

- `make submission-package-dev` recreated the synthetic PostgreSQL service, executed the complete
  deterministic evaluation, and generated 11 files under `examples/final/` with a package checksum;
  the manifest correctly records `release_ready=false` and the dirty development source label;
- the strict generator was executed without its development override and failed closed because the
  working tree is not a clean release commit;
- focused M18 unit tests pass and prove the exact `2, 1, 1` result, three rejection codes,
  one-to-many mitigation, accepted SQL policy, explicit fake write-back label, and deterministic
  dirty-tree manifest behavior;
- the development release audit scans 350 candidate files and 19 dependency licenses with no
  architecture, secret, local-link, disclosure, artifact, or license error; all 27 unique external
  HTTPS references resolve after replacing one illustrative M17 URL with a non-link placeholder,
  and its only finding is the expected dirty-tree warning;
- repeated development generation produced byte-identical files and package SHA-256
  `41e2681d536edc20a8af78b5b27d272927c59eec6212f7a427525b395717328e`;
- the `linux/amd64` judge image rebuilt successfully, ran healthy as unprivileged `user`, exposed
  `SCHEMABRIDGE_JUDGE_EXECUTION=recorded` and no application/API credential, and passed local
  `make judge-smoke` before the test container was stopped; its release ref is `231187a-dirty`;
- all 29 integration tests and all 9 acceptance tests pass against the live synthetic
  PostgreSQL/DataHub services; `make check` passes formatting/lint over 192 files, strict mypy over
  115 source files, and 316 unit tests;
- the public GitHub repository visibility/license were inspected with `gh repo view`, and a
  signed-out `curl` request returned HTTP 200. The demo/video/incognito links do not exist and were
  not pretended to pass.

M18 production-baseline remediation evidence:

- every local SQLite adapter now obtains its connection through one managed lifecycle that commits
  or rolls back as appropriate and always closes the native handle; focused tests prove both the
  success and exception paths, and source search finds no adapter bypass;
- `ResourceWarning` and `PytestUnraisableExceptionWarning` are fatal under pytest, replacing the
  previously observed hundreds of leaked-connection warnings with a zero-warning unit gate;
- CI now installs and invokes the repository Make targets, provisions the pinned PostgreSQL/DataHub
  services before integration work, and runs the same full-suite coverage gate as the strict
  clean-room workflow;
- `make coverage` passed all 359 tests against live synthetic PostgreSQL/DataHub with 80.10% total
  coverage; its seven remaining warnings are DataHub's explicit idempotent-upsert attribution
  notices, not leaked resources or unraisable exceptions;
- `make check` passed Ruff format/lint over 194 files, strict mypy over 116 source files, and all 321
  service-free unit tests;
- the internal-browser acceptance completed the governed workflow using live read-only PostgreSQL,
  verified the exact `2, 1, 1` result, `schemabridge_reader`, the 5,000 ms bound, the three rejection
  codes (`non_integral_identifier`, `non_finite_identifier`, `null_join_key`), explicit
  `COUNT DISTINCT` fanout mitigation, the fake-local publication transition, semantic/relationship/
  decision workspaces, a 390x844 responsive viewport, and an empty warning/error console.

M20 production identity, RBAC, and workflow-isolation evidence:

- immutable principals now contain only deployment-scoped HMAC-pseudonymous actor/workspace IDs,
  five closed roles, authentication method, and bounded issue/expiry times; OIDC issuer, subject,
  audience/authorized party, timestamps, tenant, and group shape are checked, and the signed tenant
  must also match an exact deployment allowlist;
- pseudonymization keys require at least 32 UTF-8 bytes and eight distinct byte values; email,
  names, tokens, and raw claims do not enter domain values, SQLite, workflow traces, or logs;
- the application policy is deny-by-default: analysts own create/confirm/execute/skip/retry and
  bounded-result access, publishers alone publish workspace context, and steward/publisher/auditor
  views omit result rows and rejection details; unknown groups grant nothing;
- workflow draft creation and its immutable workspace/owner grant share one SQLite
  `BEGIN IMMEDIATE` transaction. Scoped reads/updates survive restart, cross-owner/cross-tenant
  access is indistinguishable from absence, and legacy/orphan/conflicting state cannot be adopted;
- `inspect` and the compatibility `resume` path are now provably inert. Interrupted-state repair is
  a separate explicit action authorized against the exact recovery operation; an auditor opening an
  interrupted workflow leaves its revision and payload byte-for-byte unchanged;
- Streamlit OIDC configuration is preflighted before reading the session. Managed profiles require
  HTTPS callback/discovery, client/audience and issuer/origin binding, independent strong secrets,
  disabled token exposure, fixed integration modes, and OIDC; the unauthenticated legacy CLI fails
  closed in staging/production;
- `hosted-demo` remains key-free and fixed to local pseudonymous identity plus
  recorded/fake/recorded adapters. Development also defaults to recorded execution and requires an
  explicit development-only opt-in for local live reads;
- the loopback synthetic OIDC provider implements authorization code with PKCE S256, state/nonce,
  one-use expiring codes, RS256/JWKS, discovery, and logout without logging credentials or tokens;
- `make check` passed all 472 service-free tests plus Ruff and strict mypy; the focused M20 cut
  passed 149 tests; `make test-integration` passed 29, `make test-acceptance` passed 13, and the
  final isolated `make coverage` passed all 514 tests at 80.07%. The seven coverage warnings are
  DataHub's expected idempotent-upsert attribution notices;
- Streamlit 1.60.0 internal-browser acceptance proved the unauthenticated sign-in-only boundary,
  an OIDC analyst north-star execution with exact `2, 1, 1` and three rejections, denial of analyst
  publication, result/rejection redaction for a distinct publisher, successful publisher
  publication, tenant-B isolation, the no-role `permission_denied` boundary, and
  `tenant_not_allowed` before governed data;
- the same browser completed the recorded local identity flow through publication. A clean stable
  browser tab had no warning/error logs, and a 756x924 viewport had equal 756-pixel client and
  document widths with no horizontal overflow. The long-lived OIDC tab showed only expected
  disconnect messages while the synthetic server was deliberately restarted between identities.

M21 generic semantic-registry and diverse-corpus evidence:

- one immutable `GovernedSemanticRegistrySnapshot` now supplies logical models, field definitions,
  physical types/mappings, join contracts, provenance, and one canonical fingerprint through a
  workspace/catalog/registry-scoped application port;
- the recorded adapter verifies a bounded manifest, duplicate-free YAML, path containment, no
  symlinks, exact SHA-256, scope, format, and full registry fingerprint without legacy fallback.
  The synthetic bundle has 7 models, 31 approved mappings, 5 contracts, raw SHA-256
  `4f086f8b9f0679d6405beab45d48c6601520b9b6e0c5f20d7dc0ad987485a723`, and registry fingerprint
  `0710148874049078f751ac96f0a18131cf01daa41f27dc034ca52a8b212dd966`;
- active guided, intent, planning, workflow, execution, evaluation, and UI composition use that one
  registry. Rejected-source allowlists come from its approved join keys. Every execution/retry
  reloads and re-resolves the current registry; changed/revoked state yields `stale_registry`
  before preview or rejection I/O;
- registry capacity is independent from query capacity: the global 7-model/5-contract graph loads,
  Product no-join, Shipment one-join, and two-join/three-table commerce plans pass, while a fourth
  model/third join fails in both guided validation and the typed request model. The ungoverned
  support homonyms are never inferred from their names;
- PostgreSQL now contains 465 deterministic rows across 11 tables/eight schemas. Two complete
  project-scoped resets reproduced global hash
  `487388495115265d5fac5a17675e05c236fd4eb4c430613ef02050a5f6010654`, schema hash
  `15f5305fa070ba41fe912906cdbb8aec629c7b30b579f6324ebddd166968f3f7`, and constraint hash
  `b5a6a49b03784eafa558a0f74f2435eb968ad47f8dbd1246a5a7b89fd7998c5c`. The reader can select all
  11 tables and has zero DML/DDL privileges;
- exact live PostgreSQL regressions prove eight commerce-revenue rows over two approved joins,
  delivered-order `COUNT DISTINCT` results `22/11`, new malformed/negative/empty/NULL rejection
  records, and the unchanged north-star `2/1/1`, all as `schemabridge_reader`, read-only, with a
  5000 ms timeout;
- live DataHub ingestion profiled/described all 11 datasets and wrote 121 records with no failure;
  the catalog check passed and the MCP search/schema check retained no mutation tools;
- evaluation now passes five query paths, five compilations/executions/results/rejection sets,
  `38/38` safety cases, and `2/2` recipe cases. Natural-language equivalence remains `1/1` with four
  explicit M27 skips rather than pretending registry-wide language support;
- internal-browser acceptance showed registry identity/fingerprint/counts, Customer and Product
  field definitions, all five relationships/evidence/risks, and the unchanged three-action
  north-star result. A clean tab had no warning/error logs, and a 390x844 viewport had 390-pixel
  client/body/document widths with no horizontal overflow;
- `make check` passed Ruff, strict mypy, and 501 service-free tests; `make test-integration` passed
  31; `make test-acceptance` passed 13; `make coverage` passed all 545 tests at 80.48%. The seven
  warnings are DataHub's expected idempotent-upsert attribution notices. The development release
  audit passed architecture, secrets, local links, artifacts, and dependency licenses; only the
  expected uncommitted-tree warning remains.

M22 complete live DataHub registry evidence:

- the exact live registry check passed with `synthetic_enterprise` v1, 7 models, 31 mappings,
  5 joins, 37 decisions, source `datahub:`, and fingerprint
  `ef480eb7370924ff4c94131a2c6c4063d85652cc83aaec9059538cdc2cf9c1b9`;
- focused publication, live-reader, configuration, and governed-execution tests passed 71; exact
  CLI replay returned `already_current` for approval
  `registry-publication-v1-317d364ead8a8f4d946a91a699e37c60cfd5438dec5163342a06d56ad2c5859f`;
- `make test-integration` passed 35, `make test-acceptance` passed 15, and deterministic evaluation
  passed with corpus digest
  `487388495115265d5fac5a17675e05c236fd4eb4c430613ef02050a5f6010654`;
- `make check` passed 577 tests; `make coverage` passed all 627 at 80.66%; the development release
  audit passed 402 candidate files and 19 direct-dependency licenses with only the expected
  dirty-worktree warning;
- the internal browser at `http://127.0.0.1:8510` showed `live:datahub`, the exact registry
  identity/fingerprint/counts, and workflow `m20-0c1928cea6a54f98b37fd013d47d8bbf`. Revision `r21`
  confirmed `distinct_or_relationship_count`, returned dated rows `2, 1, 1`, exposed all three
  rejection classes, and reported `schemabridge_reader`, `read_only=True`, `truncated=False`, and
  `5000ms`;
- the browser console result was `[]`; at 390x844,
  `documentElement.scrollWidth == documentElement.clientWidth == 390`; final diff hygiene passed.

M23 durable control-plane evidence:

- `make control-plane-check` reported migration version 1/current 1/pending none for runtime,
  reconciler, and migrator and verified source `schemabridge` differs from control
  `schemabridge_control`;
- live activation/projection failure/reconciliation/rollback finished at generation 3/version 6,
  transition
  `registry-transition-v1-175299e04d6f9179733792f3d3c8b724807953533be5b2b8ca5b1ec881e7b187`,
  pointer fingerprint
  `85f131dd53bb5cf2da8538cc171277c4d988b77101e0e5b37da46fed2ef24757`, and projection
  `delivered`; a following inspection reported only `in_sync`;
- the real-CLI isolated legacy and identity drill passed with 2 imported/4 quarantined resources,
  four exact quarantine reasons, 3 stripped preview rows, unchanged source bytes, v1→v2 active
  lineage, 2 verified bindings, 3 valid audit events, unchanged historical payload/access, exact
  replay, and complete removal of its temporary database and artifacts;
- governed recipe migration prepared without exposing SQL/result rows and published live DataHub
  version 44; read-back preserved historical version 43 byte-for-byte;
- a signed owner-only backup restored to a separate fresh target with exact state SHA-256
  `22b8fdc5568b81e2bdd8626b3315e525c16651f19d3966c538b87cc7948ec2e5`,
  schema/version/checksum, pointer, 3 transitions, pending outbox, quarantine, identity bindings,
  and audit head; tampered archives/manifests, non-fresh targets, and post-restore tampering fail
  closed, and the acceptance target was dropped afterward;
- the final internal-browser path showed authoritative generation 3/version 6, projection
  `delivered`, pointer fingerprint above, and 7 models/31 mappings/5 joins. Console output was
  `[]`; at 390x844 root/body scroll and client widths were all 390 with no horizontal overflow; a
  post-gate 1280x720 reload read the same authoritative state;
- final post-fix gates passed: `make check` 767 tests; `make test-integration` 58;
  `make test-acceptance` 15; `make evaluate` with 11 tables/465 rows and global SHA-256
  `487388495115265d5fac5a17675e05c236fd4eb4c430613ef02050a5f6010654`;
  `make coverage` 840 tests at 81.03%; release audit 458 candidate files/19 dependency licenses;
  and `git diff --check`. The only release-audit warning is the expected dirty working tree.

M24 automated and operated local evidence:

- the direct API dependencies are explicit: FastAPI, Uvicorn, PyJWT with asymmetric crypto
  support; HTTPX is test-only. Final dependency lock/SBOM remains M29;
- `make check` passed Ruff format/lint, strict mypy over 180 source files, and 1,074 tests with 99
  deselected;
- `make test-integration` passed 84 with 1,089 deselected and six expected DataHub warnings.
  `make test-acceptance` passed 19 with 1,154 deselected and one expected warning. The real-socket
  path exercises authorization code/PKCE, signed RS256 access JWT and live JWKS, real
  control/source PostgreSQL, deterministic compilation, SQLGlot guarding, summary-only output,
  replay/collision, queued/cooperative cancellation, retry/dead letter, tenant denial, and
  response/log/state secret scans;
- `make evaluate` passed over 11 tables/465 rows with global SHA-256
  `487388495115265d5fac5a17675e05c236fd4eb4c430613ef02050a5f6010654`; live LLM remained
  `not_run`. `make coverage` passed 1,173 tests with seven expected warnings at 81.62%;
- migration `0002_authenticated_api_jobs.sql` has exact SHA-256
  `4e828aacfc9cc0db35a5b05838556e35360d7e0322c16e1ed008c94faf4f4efc`;
  schema v3 applies `0003_reject_expired_job_success.sql`, SHA-256
  `fba8bf2fec35c56be95487c21452ac46a45b3598c5b56cf382414aa57c7240f0`;
  control-plane checks reported API, worker, runtime, reconciler, and migrator at
  `current=expected=3`, no pending migration, and verified source/control separation;
- `make runtime-wheel-smoke` loaded packaged migrations 1, 2, and 3. Image
  `schemabridge-runtime:m24-final` ran as UID 10001, discovered schema v3, used the copied reader
  secret at mode `0600`, and passed the worker readiness probe;
- release audit passed 507 candidate files and 23 direct licenses with only the expected dirty-tree
  warning; `git diff --check` passed;
- Codex's internal browser observed API readiness `200`, submit `202`, same-job replay `200`,
  changed-payload collision `409`, attempt-0 queued cancellation, transient `retry_wait`, real
  holder `SIGKILL`, attempt-2 reclaim, and summary-only success with stage
  `publication_proposed`, row count 3, rejected count 3, `truncated=false`, and preview fingerprint
  `e7df94d70592eb7be8cccb130bdc8b857d27d59c8609425b46461f35eddafddb`;
- wrong-role and cross-tenant callers received the identical
  `404 execution_job_unavailable`. Desktop 1280 and mobile 390x844 had no warning, error, or
  horizontal overflow. Scanning browser responses and three jobs/events/logs found zero tokens,
  idempotency material, DSNs, SQL, parameters, prompts, source values, or rows;
- direct browser navigation to port 8520 was blocked by `ERR_BLOCKED_BY_CLIENT`. An uncommitted
  ephemeral same-origin relay on 8510 kept the bearer server-side and projected sanitized
  responses from the real API. It was acceptance instrumentation, not product UI; Streamlit
  itself created and confirmed both workflows;
- no OpenAI request was made. The existing API key was not read, printed, copied, logged, or
  persisted for M24. All 25 acceptance criteria pass and M24 is accepted locally.

M25 implementation and operated scale evidence:

- migration `0004_dynamic_catalog_inventory.sql` is additive to immutable versions 1–3 and has
  SHA-256
  `45c7d95e56a336f267cbe29ad390f6fd54a59d826ffd33253d290be0257060bb`;
- fresh schema-v4 reset/migration/check and focused schema, privilege, refresh, cursor, capacity,
  pool, API, DataHub, storage-alignment, browser-panel, and query-safety cuts passed during
  implementation; final control-plane check reported all six roles at expected/current v4, no
  pending migration, and verified source/control separation;
- the operated PostgreSQL evidence profile reports 10 small-tenant assets and exactly 5,434
  large-tenant assets/41,028 fields across two active large-tenant connections; the small tenant
  has 75 fields;
- every 997th large-fixture asset is a 64-field sparse-wide case with nested paths, Unicode names,
  heterogeneous native types, definitions, null/key variation, and tags;
- page-size 1/17/50 traversal passed. The large case used 5,434/320/109 pages and read at most
  2/18/51 rows while returning at most 1/17/50 items;
- the 110-page full refresh completed request-to-terminal in 29.685331 seconds on PostgreSQL 16.13,
  below the 60-second local budget;
- Python heap delta was 126,601 bytes and process RSS delta was 0 bytes, below the 16/64 MiB
  local budgets;
- 5,000 indexed reads at concurrency 16 completed with zero unexpected errors at p50/p95/p99/max
  26.5/41.762/54.951/81.658 ms. Pool-wait p95/p99 was 12.266/21.651 ms over 5,000
  observations;
- asset and field keyset plans used their expected indexes under the default PostgreSQL planner
  without an override; the field probe naturally selected one 64-field asset;
- tenant policy creation/revision now has an expected-version, exact-confirmation operator path,
  a migrator-only `SECURITY DEFINER` function, and immutable revision rows;
- tags and glossary terms participate in the field `search_document` GIN index, and the deployment
  reference includes a separate hardened two-replica catalog-indexer workload;
- the final `make check` passed Ruff format over 362 files, Ruff lint with zero findings, mypy over
  194 source files with zero errors, and 1,412 tests/126 deselected in 83.29 seconds;
- full integration passed 111 tests/1,427 deselected with six expected DataHub idempotent
  attribution warnings in 145.02 seconds; acceptance passed 19/1,519 deselected with one expected
  warning in 56.04 seconds; API and worker cuts passed 18 and 24 tests in 44.97/38.12 seconds;
- scale correctness passed 20 tests in 11.16 seconds, its PostgreSQL cut passed 8/4 deselected in
  6.37 seconds, and the synthetic report passed; deterministic evaluation completed over 11
  tables/465 rows with seed SHA-256
  `487388495115265d5fac5a17675e05c236fd4eb4c430613ef02050a5f6010654` and
  `live_llm=not_run`;
- runtime wheel smoke discovered packaged migrations 1–4; release audit passed 555 candidate
  files, 23 direct-dependency licenses, and zero external links with only the expected
  `release_tree_dirty` warning; diff hygiene passed after final non-coverage documentation
  consolidation;
- the internal browser passed at desktop 1440x900 and mobile 390x844 with exact 10/75 and
  5,434/41,028 inventory views, bounded first/middle/final pages, 404-safe tampered/filter/
  cross-scope and genuinely expired cursors, early-expiry 409, request-four 429/`Retry-After=41`,
  exact `leading zeroes`→`contract_id` field search, zero protected-data hits, a clean monitored
  console action, and no horizontal overflow;
- the live DataHub adapter refreshed generation 1 with exactly 11 assets/59 fields. After DataHub
  stopped, PostgreSQL remained stale-but-readable and exposed `bank.account_holders`; refresh
  `refresh-00359c5c1c534e018cc71e0c8a82f22b` failed `source_unavailable` at zero pages, retained
  active generation 1, and performed no fallback. DataHub was restarted healthy;
- the clean full coverage run exited 0 with all 1,538 collected tests passed, zero failed/skipped/
  deselected, seven warnings, and 81.98% coverage versus the 80% threshold. Totals were 25,387
  statements/3,668 missed and 6,608 branches/1,705 partial, with 69 completely covered files
  omitted from detail; pytest/real/user/sys times were 316.33/319.02/130.54/9.89 seconds;
- two earlier instrumented runs correctly failed closed when test-only lease fixtures expired
  during their own slow workload. Only tests changed: the recovered-owner lease became 60 seconds
  while its 4-second first expiry and 2-second stale probe remained, and socket acceptance aligned
  worker/cooperative leases to the runtime-default 120 seconds. Focused cuts passed before the full
  rerun, which included 4/4 socket acceptance and 4/4 M24 lifecycle tests;
- M25 is complete and accepted locally. This is not a global production/release GO.

M26 complete; accepted locally on 2026-07-24:

- fresh `make control-plane-reset`, `make control-plane-migrate`, and
  `make control-plane-check` pass on the final locator schema-v5 bytes in 11.72 seconds for all six
  credentials, with no pending migration and source/control separation verified. Final migration
  SHA-256 is `8e311f27c881844a1c67a0281391ddc1eb7b863f1d2f8edff382933397412285`;
- the workspace/connection routing slice passes 43 unit and 6 PostgreSQL integration tests,
  directed Ruff/diff hygiene, and strict mypy over 225 source files; the real dependency regression
  persists 5,434 artifacts/5,434 edges in 5.50 seconds including control-plane migration/setup;
- the browser-helper/panel/schema slice passes 52 tests in 3.74 seconds, its M26 PostgreSQL focused
  integration passes 2 in 16.20 seconds, and its directed Ruff/strict-mypy/diff checks pass;
- the unbounded initial-candidate lookup exposed by integrated acceptance is replaced with
  reconciler-only fixed-`search_path` `load_semantic_initial_catalog_candidates` and the exact
  `catalog_assets_semantic_lookup_idx`. It validates at most 2,000 six-key requests/2 MB with
  contiguous ordinals and all-null/all-present selected locators and returns at most two exact
  witnesses per mapping. Migration tests pass 12; one exact/fail-closed PostgreSQL E2E passes in
  11.97 seconds; final-byte six-role ACL passes 1 in 1.20 seconds; and real 5,434-table second
  inspect plus `EXPLAIN ANALYZE BUFFERS`/index assertions passes one warmed run in 126.89 seconds.
  Directed Ruff, strict mypy over 225 source files, and diff hygiene pass without raising the
  timeout or changing migrations 0001–0004. The scale fixture contains 5,434 assets/5,458 fields
  and observes exactly 31 governed fields with `candidate_count <= 2`; the function plan is a
  31-row `Function Scan` intended to remain below 5,000ms and 4,096 shared hit+read blocks. A
  clean-base rerun invalidates the warmed result: 1 test fails in 22.82 seconds at 59,615 touched
  blocks, and a retained reproduction still touches 16,333. Without fresh `ANALYZE`, PostgreSQL
  selected the asset-ID index and filtered 5,433 unrelated rows per mapping. A deterministic
  statistics-independent lookup was required; timeout/buffer bounds were not raised. The final
  solution uses length-prefixed asset/field locator helpers, two expression indexes, equality-only
  lateral probes, and external exact raw-value rechecks. With autovacuum disabled from migration
  and both relation estimates still unknown, cold acceptance passes 1 in 126.66 seconds over
  5,434 assets/5,458 fields/31 mappings. Its 31-row function plan names both indexes and remains
  within 4,096 touched blocks and the unchanged 5-second bound. The final combined
  schema/dependency unit cut passes 23. An intermediate field-phase reproduction had touched
  54,975 blocks by filtering about 5,457 fields for each mapping; the final second expression index
  closes that phase. An independent second cold run passed the scale assertion but then ended
  FAIL/ERROR after an external database removal caused `AdminShutdown`, so it is not counted as a
  full pass;
- on the final locator bytes, 190 focused unit tests pass in 6.39 seconds; semantic report
  integration passes 2 in 15.90 seconds; scan/profile integration passes 12 in 87.00 seconds; API
  passes 18 in 34.32 seconds; worker passes 24 in 39.67 seconds; full integration passes 128 with
  1,638 deselected and six expected DataHub upsert warnings in 234.83 seconds; and full acceptance
  passes 20 with 1,746 deselected and one expected DataHub upsert warning in 95.49 seconds;
- deterministic evaluation passes over 11 tables/465 rows with seed SHA-256
  `487388495115265d5fac5a17675e05c236fd4eb4c430613ef02050a5f6010654`; optional live LLM remains
  `not_run`. The first final wheel smoke fails because `schemabridge-semantic-change --help`
  composes services before parsing arguments and returns 1. Parse-before-runtime plus 18 focused
  regressions/2.02 seconds and Ruff correct it; the clean wheel smoke then passes in 23.98 seconds
  from outside the checkout with migrations 1–5 and all three M26 commands;
- current-byte reset/migrate/check passes in 11.72 seconds for runtime, reconciler, migrator, API,
  worker, and catalog at schema v5 with source/control separation. Release audit passes 617
  candidate files, 23 direct dependency licenses, and zero external links with the expected
  `release_tree_dirty` warning;
- the first final `make check` fails only Ruff format on 11 files. After mechanical formatting, the
  clean rerun passes Ruff format over 419 files, Ruff lint, mypy over 225 source files, and pytest
  with 1,625 passed/143 deselected in 63.22 seconds;
- `make coverage` passes 1,768 tests with seven expected DataHub upsert warnings in 471.77 seconds;
  total coverage is 81.64% against the 80% minimum;
- final real PostgreSQL/DataHub M26 lifecycle acceptance passes 1 test in 260.31 seconds
  (real/user/sys 262.27/13.70/2.26 seconds). The browser-session preparation passes 1 in 116.95
  seconds and retains current/review-required/blocked/revalidated counts 1/1/1/3 with maximum
  findings/impacts 32/38 under 0700/0600 state permissions. That session preceded the locator/CLI
  backend fixes; unchanged panel/API presentation bytes are the browser evidence, while final
  backend bytes pass cold acceptance, full integration/acceptance, quality, and coverage;
- the real-upstream internal browser passed at 1440x900 and 390x844 with current, review-required,
  blocked, and remediated reports; finding pages 1/16/31; impact pages 1/19/38; identical 404
  unknown/cross-tenant/tampered/wrong-filter boundaries; zero console warnings/errors, protected
  hits, mutations, approval/dangerous controls, or horizontal overflow. Exact-confirmation cleanup
  removed the dedicated state/database, left 8510/8520 without listeners, and a psycopg
  administrative check found zero retained databases; a secondary `psql` check was unavailable
  with exit 127 and was not claimed as passing;
- the required unit, PostgreSQL/DataHub acceptance, API/worker/full integration, acceptance,
  evaluation, wheel, release-audit, `make check`, coverage, and diff-hygiene commands are listed in
  `plans/M26_JOIN_DRIFT_CHANGE_MANAGEMENT.md` and pass with exact evidence in
  `tasks/M26_HANDOFF.md`;
- the first full `make test-integration` attempt exposed two blockers: one legacy/global DataHub
  recipe marker contaminated semantic scope, and one dynamic-catalog fixture lacked the exact live
  dependency state required by the gate. It failed 2 tests with 126 passed, 1,637 deselected, and
  6 warnings in 241.71 seconds. Their scope-exact and live-dependency-exact fixture corrections
  pass the two focused tests in 6.95 seconds plus directed Ruff format/check without relaxing either
  production gate. The complete rerun then passes 128 integration tests with 1,637 deselected and
  six expected existing DataHub partial-overwrite warnings in 248.82 seconds;
- the first `make test-acceptance` attempt failed 1 test with 19 passed and 1,745 deselected in
  138.65 seconds because checked-in recipe v2 is correctly `stale|plan_changed` after M26 adds
  optional binding fields to the typed plan fingerprint. The recipe was regenerated as v3 from the
  current saved execution through the governed `QueryRecipe.create` path; focused and complete
  acceptance reruns later passed without weakening stale-context behavior. Its
  first targeted run exposed three strict v2 manifest/test expectations (3 failed, 4 passed in
  2.90 seconds); after advancing them to v3, the same selection passes 7 tests in 7.99 seconds;
- the second full `make test-acceptance` attempt failed 1 test with 19 passed, 1,745 deselected, and
  1 warning in 91.63 seconds when the second real `_load_initial_candidates` call hits the existing
  5-second statement timeout. An isolated retained rerun passes 1 in 104.03 seconds, confirming an
  intermittent but structurally unbounded candidate query over 5,434 assets/5,458 fields for 31
  mappings. The final statistics-independent locator correction closes that failure without
  raising the timeout, and the final global acceptance result above passes;
- exact browser report/impact-set fingerprints, final test counts, current-byte checksum, role
  proof, package/audit, coverage, and diagnostic failures/corrections are recorded in
  `tasks/M26_HANDOFF.md`;
- M26 was accepted locally and made M27 eligible; the dirty tree and later productionization
  milestones continued to block a global production/release GO.

M27 complete and accepted locally on 2026-07-27:

- the combined provider-free M27 regression selection passed 454 tests in 689.26 seconds;
- the shared live-evaluation selection passed 75 tests in 701.92 seconds;
- the final attestation/runtime selection passed 84 tests; an independent final review passed its
  35-test cut with P0=0, P1=0, and P2=0 for the bounded synthetic smoke authorization;
- directed Ruff format/check, Ruff lint, strict mypy, and diff hygiene over the changed
  attestation/runtime files passed;
- signed `m27-cheapest-first-campaign-v11` selected Nano with 16 interpretation attempts, 15,715
  input tokens, 1,204 output/reasoning tokens, 30,016 ms evaluator-adapter duration, and EUR
  `0.001394085`. The complete synthetic corpus passed all published quality, ambiguity, core, and
  adversarial gates;
- the accepted content-addressed v2 artifact has file SHA-256
  `f4fceb557b8dd0a2ddf3c05a5f670ba2ea759f32273ef515e0daf0231a117053`,
  attestation SHA-256
  `badceb063169318ab19c1755c3d804aa04f151b2e6fe003f90160d7d1983676f`,
  ledger digest
  `4fe55a8906b04a202a057bc4f6621b83a0b957191a2693c01dbbc9c5ec336e99`,
  signed witness
  `30128ad06cef91b12e1912eb9bbb25a3588447b5ee3cdaec4cc82dec882102fc`,
  and ordinal digest
  `740ac92954ae9831524464fcc532e5ced6039a7523f8ef3a7d519815b4c43fdd`;
- the fake/runtime internal-browser journey passed at desktop and 390x844 over exact 10/75 and
  5,434/41,028 profiles with 31 governed mappings. It recorded a clean fresh console, no
  horizontal overflow, and no key/prompt/provider-payload/protected-identity/credential/SQL-
  parameter/source-value leak;
- the one authorized live-UI attempt was rejected by the internal browser URL policy while
  reloading `http://127.0.0.1:8510`, before page/form/click/submission/provider activity. Policy
  v85 was enabled only from `2026-07-27T17:48:00.841467Z` until disabled policy v86 was applied at
  `2026-07-27T17:50:00.025348Z`; a repeatable-read/read-only PostgreSQL snapshot found zero
  policy-v85 reservations, interpretations, settlements, audits, request-window updates, or
  daily-usage updates. The current policy is disabled v86 with zero active/open/stale reservations
  and zero reserved tokens;
- post-v86
  `.venv/bin/python scripts/m27_browser_acceptance_runtime.py verify-live-attestation
  --attestation-json
  reports/m27-query-studio-live-history/campaign-ledger-attestation-f4fceb557b8dd0a2ddf3c05a5f670ba2ea759f32273ef515e0daf0231a117053.json`
  exited 0 with `verified=true`, schema v2, 16 historical attempts, and exactly one compatible
  matching;
- the exact focused M27 plan passed 552 tests in 624.86 seconds;
- `make control-plane-reset` passed; migration applied immutable versions 1–8; the six runtime,
  reconciler, migrator, API, worker, and catalog credentials all reported current/expected v8,
  no pending migration, and verified source/control database separation;
- the M27 integration selection first passed 20 with one skipped because the retained small
  fixture was absent. Regenerating the exact 10-asset/75-field small fixture made that isolated
  skipped case pass 1/1;
- focused acceptance passed 15 in 19.37 seconds; API passed 18 in 31.69 seconds; worker passed 24
  in 39.15 seconds;
- the first full integration run passed 147 and failed one test-only lease fixture because
  `clock_timestamp()` advanced inside the exact fenced transaction. Only the fixture changed to
  `statement_timestamp()`; the production lease guard remained intact. The focused regression
  passed 1/1 and the complete rerun passed 148 in 246.40 seconds;
- full acceptance passed 35 in 114.62 seconds. Deterministic evaluation passed with
  `live_llm=not_run`; M27's separate signed live evidence remains the provider record. Runtime
  wheel smoke passed;
- release audit exited 0 after scanning 697 candidate files, 23 direct dependency licenses, and
  zero external links. Its dirty-working-tree warning remains and explicitly prevents a
  clean-room release claim;
- final `make check` passed Ruff format/lint, strict mypy, and 2,281 tests in 715.69 seconds.
  Coverage passed 2,459 tests at 81.47% in 2,115.40 seconds. Postflight scale plans passed the
  41,028-field reviewed-index/cold-budget assertions, and `git diff --check` passed.

M28 accepted evidence on 2026-07-28:

- seven focused unit groups passed 427 tests; the selected connector/route/cost integration passed
  37, configured focused acceptance passed 28, and the critical schema/tenant/managed-query cut
  passed 17;
- pristine and v8→v9 paths pass; the control plane reports schema v9 for runtime, reconciler,
  migrator, API, worker, and catalog with no pending migration and verified source/control
  separation;
- full integration passed 164 tests with one retained M27 small-browser-fixture skip and six
  expected DataHub warnings in 662.06 seconds; full acceptance passed 47 with one expected warning
  in 133.50 seconds;
- `make check` passed Ruff, strict mypy over 276 source files, and 2,714 tests with 207 deselected
  in 989.17 seconds; full coverage passed 2,920 with the same retained skip and seven expected
  warnings at 81.76% in 2,626.35 seconds;
- deterministic evaluation passed over 11 tables/465 rows with `live_llm=not_run`; runtime wheel,
  the 751-file/23-license release scan, scale correctness, isolated PostgreSQL 5,434/41,028 plans,
  and whole-tree diff hygiene pass. The release scan retains its dirty-tree warning;
- all nine internal-browser scenarios passed at desktop 1280x720 and mobile 390x844 with clean
  consoles, no overflow/XSS/protected-data hits, distinct tenant readers/results, and exact
  cleanup; and
- D103 accepts M28 locally. Production/release remains NO-GO and M29 is eligible but not started.

M29 final-byte remediation status on 2026-07-30:

- focused M29 unit/security/manifest/observability/backup/web evidence passes 316 tests in
  17.74 seconds. The final release/supply-chain cut passes 158 tests, and
  `make supply-chain-static` passes over 888 candidate files and 23 direct licenses
  with only the expected dirty-tree warning;
- a clean reset/migrate/check reports schema v12, all eight distinct control credentials, no
  pending migration, and verified source/control separation. The focused PostgreSQL
  backup/restore cut passes 6 tests with 5 deselected in 5.96 seconds;
- the Kubernetes contract is 63 resources and includes the hourly backup CronJob plus exact active
  PrometheusRule. Its focused local manifest/render contract passes; target-cluster server-side
  validation and enforcement remain external;
- active observability is closed at eight produced metric families, six alerts, three SLOs, and
  eight panels. SIEM and uncomposed signals are inactive until they have lifecycle-owned producers
  and operated destinations;
- the managed web wrapper, loopback readiness check, and Operations-unavailable state are
  implemented. The final browser record passes all six development states at desktop and mobile,
  renders production unavailable at 1280x720 and 390x844, and ends with a clean console, no
  overflow/XSS/protected-data/dangerous-action finding, and exact cleanup;
- the runtime's exact five-APK amd64/arm64 PostgreSQL-client closure and offline installation policy
  are implemented. Local image
  `sha256:db2a42ce187243c809a9fcc1272246fb914fbeefc9186b7a83b651014d015b79`
  is 182236711 bytes, runs as 10001:10001, exposes `pg_dump`/`pg_restore` 16.14, passes `pip check`,
  `ldd`, all entrypoint smoke, an exact 107-component SBOM, the zero-known-vulnerability
  `pip-audit`, and Trivy HIGH/CRITICAL policy. It was built over stable product bytes before the
  commit and its OCI revision label names prior HEAD
  `9e8b69e1adce8e144b345d3b0d33482558804dc6`, so it is local pre-commit evidence, not a release;
- full integration passes 161 with 11 explicit skips and 3327 deselected in 250.42 seconds; the
  skips are unavailable DataHub credentials plus the retained M27 small fixture. Full acceptance
  passes 43 with 4 DataHub-credential skips and 3452 deselected in 48.51 seconds. Deterministic
  evaluation passes 11 tables/465 rows with `live_llm=not_run`, and the installed runtime wheel
  verifies packaged migrations 1–12 and all current entrypoints;
- scale correctness passes 25 tests in 3.16 seconds; the PostgreSQL postflight passes 8 with
  4 deselected in 4.41 seconds and the correctness policy passes. Recovery policy passes with
  fingerprint `24bdecb8bcb8faab8ba83d64eadf201c0b1d31142ebab773b012735f8f6f34ae`;
  and
- the seven-job release topology passes local Actionlint 1.7.12, ShellCheck 0.11.0, static policy,
  158 adversarial tests, and independent review with P0/P1/P2 = 0 at workflow SHA-256
  `9979c54be6ba39d1d7b606e6d882aa10e9868bb9d003482b30a780ee104d1f26`;
  final `make check` passes 3339 tests with 214 deselected in 1503.89 seconds, Ruff over 608 files,
  and strict mypy over 308 files; and
- current-byte coverage passes 3534 tests with 14 external skips and 1 deselected at 81.17% in
  4149.89 seconds, above the required 80%; and
- initial commit `09c3a2e0f47a7fbadb5297fa6bc4f9aca0d21950`, exact-revision/history secret
  scans, and draft-PR publication pass. Hosted run `30560980711` failed closed on the
  platform-specific virtual `.python-rundeps` identity and was then cancelled; D124's corrective
  candidate and hosted rerun remain pending. D123 records local acceptance. Static
  provider/cluster/release contracts are not operated proof:
  production and release remain NO-GO pending external provider, cluster, telemetry, retention,
  cutover/rollback, exclusive Release/GHCR writers, immutable Releases, protected release, and
  independent review evidence listed in the M29 handoff.

## Known risks

- The standalone Codex CLI package 0.104.0 is present but cannot start because its native arm64
  executable is missing; the active Codex desktop task is unaffected.
- PowerShell bootstrap behavior is statically reviewed but unverified on Windows.
- The draft PR is publication evidence, not a signed release candidate. Strict `make release-clean`,
  exact-commit deployment, external review, and the remaining production gates are still required.
- Repository inspection found no `production-release` environment, unprotected `main`, zero
  rulesets, and Immutable Releases disabled. Exclusive GitHub Release/GHCR package write authority
  is also not operated or proven. All are external fail-closed prerequisites for the manual
  promotion workflow.
- The first hosted GitHub Actions run proved that local installed packages, ignored signed
  evidence, persistent DataHub state, and terminal color could mask clean-checkout failures. The
  tracked correction removes those assumptions; the current PR check status remains the
  authoritative remote result.
- GitHub-hosted runtime and disk headroom must still be judged from the current clean PR run; local
  success does not substitute for that hosted evidence.
- The generic transformation algebra is serializable in M02, but only its explicit approved subset
  is interpreted/compiled. M21 adds timestamp/date/decimal operations needed by the synthetic
  commerce cases; every other transformation still fails closed.
- The M03 guard deliberately rejects wildcard projection, recursive CTEs, self joins,
  `USING`/`NATURAL` joins, unallowlisted functions, and all queries above three physical tables.
- The active DataHub stack leaves approximately 5.7 GiB host disk free, below the official 13 GB
  clean-start recommendation; no cache/image prune was performed because unrelated projects exist.
- The DataHub MCP package emits an upstream experimental-SDK import warning on startup; the pinned
  read checks pass and mutation tools remain disabled.
- The physical source catalog still contains no ingested lineage or dataset-query entities. M05
  reports those states explicitly; M07 adds approved logical-parent relationships and M08 records
  lineage/query usage as missing evidence rather than inferring it from its decision documents.
- M06 pattern/overlap facts and evaluation labels are small synthetic fixtures, not live catalog
  measurements or production validation; lineage and query-usage evidence remain explicitly missing.
- DataHub Core `v1.6.0` did not expose its documented batch logical-relationship OpenAPI route
  locally; the writer uses the documented equivalent `LogicalParent` dataset/schema-field aspect
  path instead. GraphQL exposes direct `setLogicalParent`, but no proposal mutation.
- The stock local DataHub all-users policy grants the writer service account personal-token
  generation in addition to SchemaBridge's bounded privileges; SchemaBridge never calls that
  mutation, and all writer metadata policies are limited to synthetic canonical assets.
- Hugging Face Docker Spaces is the selected M17 free hosted fallback, but no public Space/URL or
  platform cold-start evidence exists until the operator produces and deploys the clean release.
- AccountHolder-to-Account seed rows happen to be unique on both sides; the `many_to_one`
  classification therefore depends on the declared FK direction, with this limitation retained as
  a contract risk instead of pretending the bounded rows demonstrate left-side duplication.
- The active M23 registry remains synthetic integration evidence. It proves durable activation,
  rollback, reconciliation, and complete reconstruction from exact DataHub version documents, not
  production-scale quality, live tenant-aware credential routing, or multi-region operation. The
  legacy M09/M10 adapters remain historical compatibility seams only and are not a fallback.
- M11 verified its legacy live-adapter contract against the installed SDK with an in-memory client
  only. M27 later supplied the separately signed live Nano campaign and measured quality/usage;
  the deterministic fake remains the explicit default and never masquerades as live.
- Historical M12 live acceptance predated complete registry reconstruction; M22 now proves the
  current DataHub-only planning and execution path separately.
- M12 workflow documents intentionally contain fingerprints and approval identity only. They are
  observable audit/context markers, not M13 query-recipe documents and not a store for SQL or results.
- The checked-in M13 recipe intentionally remains recorded historical evidence. The operated M23
  live migration created a distinct DataHub version 44 while preserving historical version 43;
  dynamic bulk recipe migration and drift-triggered blast-radius workflows remain later work.
- M14 intentionally defaults to the deterministic fake intent parser, recorded catalog/candidate/
  registry evidence, recorded or live read-only synthetic PostgreSQL, and fake local publication.
  The UI labels every boundary; selecting live DataHub never silently reconstructs a missing
  registry or falls back to the recording.
- Automated tests cover all required safe UI states and the clean in-app browser journey passed at
  two viewport sizes. Timing/screen recording, live service-stop failure drills, and the
  second-person judge check remain operator acceptance work.
- M15 reports deliberately contain raw counts only. The candidate fixture is seven tuned synthetic
  cases with one retained homonym false positive and one hidden-synonym false negative; it cannot
  support production-quality or statistical-generalization claims.
- Historical M15 optional live-LLM evaluation remains `not_run` in the M15 artifact and separate
  from M27's signed provider campaign. Current generated release artifacts still identify
  `working-tree-uncommitted`; they must be regenerated after the reviewed release commit before
  any score is quoted as release evidence.
- DataHub and the control database remain separate systems without a distributed transaction.
  Registry activation now commits its PostgreSQL authority/outbox first and reconciles DataHub
  explicitly. Other approved external publication families retain exact reservation, idempotent
  retry, and read-back contracts but do not claim cross-system atomicity.
- A scoped writer token minted immediately before terminal provisioning failure might remain valid
  without a saved local reference; revoke it manually after such a failure.
- The clean live-browser timing/recording and second-person judge explanation remain operator work.
- The M18 artifact manifest and evaluation summary are development evidence from a dirty tree. They
  must be regenerated after the release commit; no release tag/checksum sign-off exists yet.
- The clean M14 synthetic screenshots were visually re-reviewed for credentials/proprietary data,
  but no final M18 release/public-deployment screenshots have been captured.
- The timed captions/video script exists, but no video file, duration/checksum, public upload,
  muted-playback review, or rights review has been completed.
- M20 alone isolates workflow control-plane state, not live data-plane credentials. Accepted-local
  M28 adds workspace/connection-qualified source and DataHub routes, but deployments must retain
  the historical single-scope restriction until M29's provider and infrastructure controls are
  operated and independently accepted.
- Streamlit logout clears the current application session but is not enterprise-wide revocation;
  other sessions may remain, and the Streamlit identity cookie lifetime is independent of the ID
  token. SchemaBridge rejects expired claims on rerun, limits accepted token lifetime to one hour,
  and requires 15-minute freshness for live publication.
- M25 now adds durable per-principal/workspace request admission, tenant connection/asset/field/job
  capacity, fair claim, and bounded per-process pool settings. The operated local report measures
  one process/replica profile only; autoscaling, production replica budgeting, alert thresholds,
  queue SLOs, and real traffic remain unverified.
- M28 removes the managed catalog's global DataHub server/token fallback and resolves
  a capability-private route per workspace/connection. M29 now implements an exact-version remote
  resolver contract, but local owner-only files and mocked HTTPS responses are still not an
  operated production secret manager; real provider provisioning, revocation, rotation, and audit
  remain unaccepted.
- Generation activation recomputes complete counts/fingerprints in O(N) PostgreSQL work. It avoids
  API-side materialization but is measured only at 5,434 assets, not at the policy ceiling of
  100,000,000 assets.
- Field/asset full-text GIN indexes are global rather than tenant-prefixed. Exact tenant,
  connection, and generation predicates preserve isolation, but production tenant-distribution
  performance remains unmeasured.
- M24's periodic-heartbeat gap is closed by unit, PostgreSQL-backed, process, and socket
  regressions. The schema-v3 authorization-expiry guard, workspace-wide rotation continuity, and
  correct dead-letter handling for workflow-access store failure all pass fresh final evidence.
  Residual operational risk remains around production scheduling jitter, database/network
  partitions, and real workload duration until later operations/observability gates are operated.
- SQLite remains only a local/recorded single-node adapter. Managed state uses PostgreSQL and the
  operated dual-key workflow preserves historical grants through verified opaque aliases; remote
  IdP key-rotation scheduling, enterprise revocation, and multi-region identity operation remain
  unverified.
- M27 now resolves bounded short Spanish/English descriptions against the active governed
  registry and builds multi-domain typed requests. This does not turn a name, definition, score,
  or model choice into semantic approval: only current approved mappings/contracts enter the
  executable lane, ambiguity remains explicit, and exact signed confirmation is still required.
- M23 supplies the local functional control-plane baseline, and M24's bounded API/durable-worker
  slice is accepted locally after fresh schema-v3, real-socket, role/secret, wheel, container, and
  browser evidence. M25 capacity/indexing is also complete and accepted locally after its full
  quality, service, scale, package, audit, browser, and coverage gates. Remote
  backup retention, SIEM, HA/failover, production SLOs, clean-commit deployment, and external
  security/operator sign-off remain open.
- The 465-row corpus exercises heterogeneous types, invalid keys, homonyms, fanout, and two domains;
  it is deterministic test evidence, not production-scale, concurrency, latency, or data-quality
  validation.
- M25's 10/5,434 fixtures and 75/41,028-field operated local run—including sparse 64-field,
  nested/Unicode/type-drift assets—prove bounded mechanics and regression budgets, not enterprise
  production scale, adversarial real metadata quality, multi-replica availability, or sustained
  traffic. DataHub remains full reconciliation in M25; only a source with a real change feed may
  claim delta. None of this weakens the independent three-table/two-join query limit.
- The M25 browser panel is same-origin acceptance instrumentation, not the production catalog or
  Query Studio surface. Its successful local run is not a production UX or deployment claim.
- `QueryPlan` deliberately remains vendor-neutral; M28 binds the exact catalog
  connection and route to `ResolvedSemanticPlan`, compiler/guard output, workflow/job
  authorization, and cost evidence. Cross-connection queries and federation remain unsupported.
- M28 populates `platform_instance`, `database_name`, and `normalized_type` through
  the PostgreSQL/DataHub connector contract and binds them to immutable generation identity.
  Production metadata diversity and connector-specific type coverage beyond PostgreSQL remain
  unverified.
- M28 resolves execution, catalog, and aggregate-profile credentials dynamically per
  workspace/connection. This is single-source routing, not federation; its same-byte final gate
  and internal-browser acceptance pass locally.
- M26 dependency sources are paginated and heartbeat-protected, but the reconciler constructs the
  complete managed workflow/recipe and dependency artifact sets before one atomic index
  replacement. This is bounded to 10,000 artifacts/100,000 edges and batches inserts by 500;
  populations beyond that cap fail coverage closed and need a later streaming/sharded design.
- Exact catalog evidence now uses a bounded `SECURITY DEFINER` locator lookup rather than
  materializing every projected row in the connection. A real PostgreSQL regression persisted
  5,434 non-empty dependency edges in 5.50 seconds including control-plane migration/setup.
- Same-scope stale workflows/recipes project through exact current logical-field/contract
  identities. Cross-scope, missing-scope, or unresolvable artifacts make coverage incomplete.
- Final PostgreSQL/DataHub acceptance proves that immutable registry source identity changing from
  v1 to v2 does not erase same-scope stale workflow/recipe impacts; exact scope fingerprints govern
  the conservative projection.
- Historical intent-only DataHub recipe documents are not active-scope dependencies. They require
  the governed existing recipe migration/publication flow to obtain scope-qualified current
  identities; no automatic rewrite or fingerprint copy is permitted.
- M26's read-only browser panel is acceptance instrumentation, not production Query Studio.
  Its real-upstream desktop/390x844 record passed locally with zero protected-data/console hits and
  no overflow; this is acceptance evidence for the panel, not production UX evidence.
- Current-byte schema-v5 reset/upgrade, complete six-role grants, package/process, quality,
  coverage, checksum, diff, and handoff evidence pass. M26 is locally accepted.
- M27 live-provider evidence is synthetic only. `store=false` is not Zero Data Retention, and the
  tenant DPA, retention, regional-residency, legal/privacy review, credential rotation, and
  production data-classification decision remain open.
- The internal browser completed the fake/runtime M27 product matrix, but its URL security policy
  blocked the separately authorized live-UI reload before page load/submission. That block was
  correctly not bypassed and provides no live-UI/provider success evidence. The signed provider
  campaign independently proves the bounded live model contract and quality on synthetic data.
- The historical v2 campaign/ledger attestation proves an authenticated unique chronological
  ordinal correlation. Because the original campaign did not persist one shared nonce-bound
  case/request digest, it must not be described as native content-derived per-case identity.
- M27's final whole-tree migration/service/package/quality/81.47%-coverage/scale/diff matrix
  passes. M28's final schema/service/package/quality/81.76%-coverage/scale/diff and desktop/mobile
  browser matrices also pass locally. M29's current schema-v12/service/package/local-image/browser/
  recovery/scale/release-topology/quality/current-byte coverage and precommit secret-scan evidence
  passes, but exact-revision scan, publication, and operated remote production controls remain open.
  Current 81.17% coverage supersedes D115's historical 81.09% baseline.
- M29's evidence verifier accepts only CycloneDX 1.5 and 1.6. This closes the hosted Trivy 1.6
  compatibility found by run `30520807060` without relaxing complete runtime-component,
  artifact-digest, source-revision, lock, or provenance bindings; every other schema version
  remains fail-closed.
- PostgreSQL `EXPLAIN` estimates depend on current planner statistics and are an admission bound,
  not proof of runtime latency, memory, or result correctness. M28 deliberately uses
  `ANALYZE FALSE`; production capacity/SLO evidence and operated statement observability remain
  M29/M30 work.
- PostgreSQL is the sole executable M28 dialect. The explicit unsupported-dialect state is a
  safety property, not a claim of Snowflake, BigQuery, MySQL, or federated query support.
- M32's deterministic language cases and compiler/guard matrix are local synthetic evidence, not
  a live-provider holdout or universal natural-language/SQL accuracy claim. Output is PostgreSQL
  for the same governed database/context; physical-only fields stay in M27
  `needs_mapping_review`, and unsupported SQL families produce no SQL.
- The M32 automated Streamlit acceptance passes. Its original full manual desktop/mobile matrix
  was not run because the in-app browser list was empty; on 2026-08-03 one advanced desktop path
  was later verified manually with empty browser logs. Mobile, hostile-input, download and the
  remaining matrix still require operator checks.
- M33 stops at a non-executable handoff. It has no generic join onboarding, incremental/batch
  authoring, historical audit cursor, operated per-tenant quota, publication/readback or
  activation. `parse_date` is deliberately rejected until shape/calendar validation is total.
  Identity-lineage and PostgreSQL persistence have separate coverage, but a combined real
  PostgreSQL rotation-plus-M33 mutation scenario remains a non-P0/P1 integration gap.
- Source/catalog/type identity changes intentionally make prior catalog evidence non-executable
  until a fresh generation is promoted. Operators must plan this fail-closed refresh window; no
  automated cross-system cutover or distributed transaction is claimed.
- M34's local DataHub tests use fakes because the exact reader/writer credentials were absent. The
  checked-in live v2 test must pass against the operated DataHub instance before production. M34
  supports additive models only; it does not add joins, replace/remove models, onboard in batch,
  prove high-volume queue/catalog performance, or activate automatically.
- The retained M26 live acceptance publishes format-v1 replacement registries and is explicitly
  skipped after M34 because v1 is read-only and cannot produce an `activation_ready` handoff. Its
  green historical CI result is not evidence for the typed v2 lifecycle added later by M35.
- M35 now supplies that typed v2 replacement/remediation contract locally, but its Phase B
  publication receipt is simulated and it deliberately does not activate. A rejected or stale M33
  replacement source is one-shot and must be authored again; batch remediation and live DataHub/
  IAM evidence remain open. The historical M26 live-v1 test stays skipped because it is not proof
  of the new v2 path.

## Next milestone

M25 remains complete and accepted locally with dynamic 10/5,434-asset and 75/41,028-field
inventories. M26 — join drift and semantic change management — is complete and accepted locally;
all 30 criteria and the final evidence matrix are recorded in `tasks/M26_HANDOFF.md`.

M27 — dynamic Query Studio and bounded short-description matching — is complete and accepted
locally with all 50 criteria, signed model selection, ordered ledger attestation, fake/runtime
browser evidence, and the final matrix passing.
M28 — governed connector routing, explicit dialects, immutable generation identity, and query-cost
preflight — is complete and accepted locally. Its exact final automated and Codex internal-browser
desktop/390x844 matrices are recorded in `tasks/M28_HANDOFF.md`.
M29 — operations and supply-chain hardening — has closed its current focused, schema/service,
release/supply-chain, quality, coverage, package, local-image, browser, recovery, evaluation,
wheel, scale, and secret-scan evidence and is accepted locally under D123. Initial commit
`09c3a2e0f47a7fbadb5297fa6bc4f9aca0d21950` is published on draft PR #1; D124's
platform-specific virtual-package correction still requires its exact-revision scan and hosted
rerun. It remains a production/release NO-GO. Its checked-in contracts and local drills are not
operated evidence; external rotation, cluster admission, active alert/SIEM delivery, immutable
retention, external cutover/rollback, exclusive Release/GHCR writer control, immutable Releases,
protected release provenance, M30, and M31 still require separate acceptance.
M32 — bounded simple/advanced natural language to standalone PostgreSQL — is complete and accepted
locally under D125. Its deterministic language, retrieval, v1/v2, compiler, independent guard,
copy renderer, CLI/Streamlit, acceptance, optional read-only PostgreSQL, and global quality gates
pass. A later manual session passed exactly one advanced desktop happy path; the remaining M32
desktop/mobile and blocked-state matrix, a live-provider holdout campaign, other SQL dialects/
destination databases, M30 production evaluation/security verification, and M31 pilot/GA remain
open.
M33 — generic governed semantic onboarding — is complete and accepted locally under D126. Its
exact preflight, tenant-bound schema-v13 persistence, explicit decisions, identity-lineage replay,
distinct-publisher handoff, focused/PostgreSQL/acceptance tests, independent P0/P1=0 review, final
quality gate and Codex internal-browser desktop/mobile matrix pass.
M34 — governed registry publication and activation-ready handoff — is complete and accepted
locally under D128. Its schema-v14 queue, isolated writer/readback, v2 physical authority,
cooperative cancellation boundary, publication observability, exact M23 handoff, focused/fresh
PostgreSQL/deployment/wheel/browser gates and independent review are recorded in
`tasks/M34_HANDOFF.md`.
M35 — governed registry-v2 join and one-model replacement/remediation — is complete and accepted
locally under D129. Its schema-v15 authoring/profile authority, exact M34 publication source,
replacement witness/handoff, HTTP/PostgreSQL/backup/acceptance/browser/full gates and retained
commercial limitations are recorded in `tasks/M35_HANDOFF.md`. M30 remains blocked on operated
M29 prerequisites and live evaluation/security inputs; production/release remain NO-GO.
M30 Phase 0 plus the Phase-1a local candidate provide the exact offline candidate-readiness
contract, NO-GO report, canonical manifest validator and workflow-attested byte authenticator under
D130/D132, and pass the final current-byte local gate. That does not accept M30. The current
development preflight reports four repository failures, 24 missing external controls and 37 exact
source digests. GitHub `main` remains unprotected with zero environments/rulesets/tags, so the
attestation workflow must not be dispatched. Campaign remains blocked on an exact clean tagged-main
candidate, exact-subject hosted artifacts, the blind corpus, operated target controls, independent
assessment and owner signatures.
M17/M18 release work remains independently blocked on a reviewed clean commit, strict
`make release-clean`, exact-commit deployment, public/incognito and cold-start evidence, final
media/links/checksums, and external reviewer sign-off.
